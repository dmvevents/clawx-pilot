# Stakeholder gap analysis — recursive pass, 2026-09-06

Method: 4 chronological mappers + GPT-6 codex-exec adversary + completeness critic; moe.19 build in flight; every claim carries its source.

## 1. Chronological ledger: feedback → change → acceptance criterion → test → verified-on → gap

| ID | Date | Stakeholder | Complaint | Changes | Acceptance criterion | Pinned tests | Verified on | Gap |
|---|---|---|---|---|---|---|---|---|
| CLWX-89 (canary alert fatigue) | 2026-05-17 | owner (selftest cron) | On-device canary intermittently failed with Ollama up and cloud reachable — two full 0/3 wipeouts (06-14, 08-24) with no follow-up anywhere; alerts fire without a triage path so they get ignored | Card CLWX-89 filed 2026-09-03 from the mining pass (Backlog, low); No code change | Canary classifies failure cause (model load vs timeout vs real regression), suppresses expected-degrade windows, and auto-files a register row on 2 consecutive real failures (card acceptance, verbatim) | — | never verified — nothing built | Unbuilt Backlog card; the two 0/3 wipeouts remain unexplained. moe.19 build+VM-verify does NOT close it (no code exists); closes only when the classifier ships and a real-vs-expected discrimination is demonstrated on ~/.openclaw/selftest history |
| RAJ-PLAUD (register PLAUD-ZERO-MIN, finding 35) | 2026-05-19 | Raj Ramdass (Ministry ICT) | Plaud-recording ingestion repeatedly broken — recordings indexed at 0.0 minutes, reported 3x; he pasted the bot's own apology: "It seems you're right, the problem isn't fixed." | No code fix in this repo — no Plaud code exists in the tree (external MCP server); register row PLAUD-ZERO-MIN dispositioned CANNOT-REPRO 2026-09-03 (docs/DEFECT_REGISTER_2026-09-02.md; docs/BLOCKER_BUG_COLLECTION_2026-09-03.md row 35); Superseded by the in-app ASR path: Mac real-whisper transcription proven x2 (skills/laptop/evidence/2026-09-03-whisper-mac-smoke); Windows ASR smoke script authored (windows-pilot pilot-asr-smoke.ps1, P5) — authored, not yet run live (gap C in the V-batch) | PROPOSED: a real meeting recording ingested through the current in-app ASR path yields a transcript with non-zero indexed duration and usable text, on both the Mac lane and a packaged Windows build. | — | Replacement path only: Mac live lane 2026-09-03 (whisper x2). Plaud itself CANNOT-REPRO — external MCP source unavailable. Windows in-app ASR: NEVER verified on a build (gap C). | Original defect never root-caused (external code unavailable) and the fix-promised-in-thread was never confirmed back to Raj; Windows in-app ASR unproven live; ASR transcription quality itself is unbenchmarked (CLWX-87 WER card, owner-attributed). No pinned test anywhere on this row. |
| COMPOSER-OVERRIDE (register B) | 2026-05-25 | owner | Chat picked Flash even though config said Pro (composer-level model override) | Four-store coherence work (applyChannelChange transaction, config-coherence-auditor); 2026-09-03 re-classification: the live 09-02 lookalike was GOOGLE-STORE-400 (fixed @73c9e88c), NOT this class; stores repaired to gemini-2.5-pro | PROPOSED: on a build where openclaw.json defaults say Pro, a fresh composer turn's trajectory records model=gemini-2.5-pro (or the coherence auditor flags drift) across 3 consecutive relaunches | tests/unit/openclaw-auth-google-store-compat.test.ts (adjacent class only) | in-tree coherence machinery only — the historical 05-25 sighting was never reproduced or explicitly closed | Register row stays open solely for the unreproduced 05-25 sighting (UNVERIFIED-stale, likely absorbed by channel-router work). moe.19 does not close it; an honest CANNOT-REPRO disposition or a composer-model assertion in a live lane would |
| ONDEVICE-RETRY-LOOP + K11 (register B; board CLWX-38 Ready contradicted) | 2026-05-25 | owner + Karunesh-class boxes | Agent loop has no identical-failure breaker: 3B on-device model retries an IDENTICAL failing tool call indefinitely (13+ min); cloud Gemini re-issued a byte-identical failing command 5x on 05-25 | CLWX-38 moved to Ready on the board (breaker work); moe.18 K11 (2026-09-06): on-device qwen hallucinated a filesystem read and LOOPED it ~15x with no loop-breaker until the K12 gateway kill interrupted — the class is live on the newest verified build; K11 evidence strengthens the owner trim-unhold call on 7add864b | PROPOSED: on a shipped build, an on-device turn that repeats the same tool+args failure N times (N<=3) is force-terminated with a visible anonymised outcome; K11 email prompt on a no-creds box reaches the email tools' readable refusal instead of a read loop | — | moe.18 VM 2026-09-06 — verified STILL FAILING (K11 finding), despite CLWX-38 Ready | Whatever CLWX-38 shipped does not cover the hallucinated-file-read loop on qwen2.5:3b. moe.19 alone does not close it unless a loop-breaker (or the 7add864b tool-catalog trim, owner-HOLD) rides the cut AND K11 is re-run on the VM; cloud-path K11 bar sits on the Mac live lane (VM gateway is a placeholder) |
| OWNER-ASR-CLWX-87 | 2026-05-26 | Anton (owner) | "So have we updated the ASR model? This one is not that good." — Windows ASR (System.Speech) quality, repeated 4+ times over a month (BLOCKER_BUG_COLLECTION_2026-09-03.md row 5, 2026-05-26T21:18:38Z); he even proposed the measurement method (known-speech WAVs). Went ~3 months uncarded until the 2026-09-03 mining pass filed CLWX-87. | CLWX-20 ffmpeg/packaging fix cleared the pipeline blocker that had masked the quality question; pipeline proven on the moe.15 VM (ASR_SMOKE_OK, first-ever pilot-asr-smoke.ps1 run, V-batch 2026-09-03); CLWX-87 filed from the session-log-miner pass 2026-09-03 (docs/BLOCKER_BUG_COLLECTION_2026-09-03.md row 5); WER bench built + Mac legs measured, commit 610779dd (2026-09-06): scripts/clwx87-wer-bench.mjs (pure word-level WER), eval/fixtures/clwx87-asr-manifest.json (8 MoE/Trinidad-vocabulary synthetic clips), windows-pilot/scripts/pilot-asr-wer.ps1 (System.Speech leg, lint:ps GREEN); Measured: whisper tiny 13.5% / base 13.5% aggregate clean-audio floor; Trinidadian place names the visible weakness ("Tuna Pune" for Tunapuna, 30.8%); bundling costs grounded (whisper.cpp tiny ~75MB / base ~142MB vs System.Speech 0MB) — docs/evidence/CLWX87_WER_2026-09-06.md | Card acceptance (exists): WER measured on a fixed known-speech fixture set (Trinidadian-accent samples included) for System.Speech vs whisper.cpp vs Azure; engine decision recorded; if whisper wins, Windows bundling cost estimated. | tests/unit/clwx87-wer-bench.test.ts (8 guards incl. >100% garbage semantics); scripts/clwx87-wer-bench.mjs (whisper / transcripts / azure-loud-skip lanes; transcripts wiring proven both directions: perfect stub 0.0%, corrupted clip caught); windows-pilot/scripts/pilot-asr-wer.ps1 (System.Speech row — authored, never run) | Mac dev lane 2026-09-06 (whisper legs only, synthetic say audio); Windows System.Speech leg NEVER run — RC VM clawx-win-rc-20260609 TERMINATED | The comparison is half-measured: the System.Speech row (the engine he actually complained about) is unmeasured pending the owner's VM-start spend call (~$0.13/hr); engine decision unrecorded; no Trinidadian-ACCENT real recordings (synthetic TTS is the named coverage gap and accent was the complaint domain); the actual quality upgrade is not shipped — CLWX-87 is In Progress, and the multi-lens review is deferred to the Ready move. |
| MODEL-PIN-STALE (register B, 2026-09-06 addition) | 2026-05-26 | internal (dev tooling) | oh-my-codex subagent lanes silently lost 1-2 workers per wave for a MONTH: 'The requested model gpt-5.3-codex-spark does not exist' — codex doctor validates only the default model | Fixed 2026-09-06 (CLWX-83): stale pin removed from ~/.codex/config.toml; pnpm doctor:agents audits repo tomls + agent-md frontmatter + user config vs scripts/agent-model-allowlist.json; 10-test guard runs in every pnpm test; caught fresh live pin drift twice (gpt-5.6-sol -> gpt-6-astra, 09-06) | doctor:agents exits non-zero on any pin absent from the allowlist; guard runs in every test invocation (met) | tests/unit/clwx83-agent-model-pins.test.ts; pnpm doctor:agents (package.json:48, wired into ga:gate pulse) | Mac dev lane 2026-09-06 (two live catches) — dev-tooling, no build gate applies | — |
| CLWX-87 (Windows ASR quality) | 2026-05-26 | owner (4+ complaints over a month) | 'So have we updated the ASR model? This one is not that good.' — System.Speech WER complaint, never carded until the mining pass | scripts/clwx87-wer-bench.mjs built 2026-09-06 (@610779dd): pure word-level WER, 8 unit guards, 8 deterministic MoE/Trinidad-vocabulary fixtures (Couva, Chaguanas, NSCC...); Mac whisper legs MEASURED; card Todo -> In Progress; Windows System.Speech leg is one command, needs the RC VM started (owner, ~$0.13/hr) | Card: WER numbers for System.Speech vs whisper on the same fixture set + a recorded engine decision. PROPOSED numeric bar: shipped engine WER <= whisper-local WER + 10pts absolute on the 8-clip manifest | scripts/clwx87-wer-bench.mjs (8 unit guards); eval/fixtures/clwx87-asr-manifest.json | Mac live lane 2026-09-06 (whisper legs only); Windows leg NEVER run | Windows System.Speech measurement + engine decision owed; gated on owner starting clawx-win-rc-20260609 (gcloud auth no longer the blocker). moe.19 build does not close it — the VM start + bench run does; an engine swap (if decided) would then need its own build |
| RAJ-LATENCY (LATENCY-UX / CLWX-43) | 2026-05-27 | Raj Ramdass (Ministry ICT) | Turns are too slow — his only twice-volunteered complaint (05-27 field test, 06-27 pre-presentation); per the card it "cost a Minister demo slot". | Quantified, not fixed: docs/evidence/LATENCY_BASELINE_2026-09-02.md — median successful cloud turn ~103s on the moe.14 e2-VM lane, ~7x the proposed p50 <=15s budget (commit 7f6acc0f); Mac-lane in-app measurement, commit b159d13c 2026-09-06: scripts/clwx43-latency-mac-lane.ts — 8.8s no-tool / 17.1s write turn over the host-API chat relay; docs/evidence/CLWX43_LATENCY_MAC_LANE_2026-09-06.md (the read_pdf row timed out — moe.10 install crash reproduction, not a latency number); Contributing fix: retry-loop breaker fee7294d (CLWX-38, 12/12 tests); movers identified but NOT executed: prompt-caching ask #7 (Ministry-side), trim branch 7add864b still on owner HOLD, routing | Card proposal (awaiting owner sign-off): p50 <=15s / p90 <=30s wall-clock for the 3 demo prompts, measured on the pilot laptop lane on a current build. | scripts/clwx43-latency-mac-lane.ts (measurement harness with a real in-app turn path — but NOT a pass/fail gate; no ga:gate row enforces any latency budget) | Measured, not fixed: e2-VM moe.14 lane 2026-09-02 (~103s median) + Mac dev install (stale 0.4.3-moe.10) 2026-09-06 (8.8s/17.1s). Card In Progress. | No latency fix has shipped; budget never signed off by owner; laptop-lane repeat deferred to the next build (the moe.10 crash makes current-install numbers partially unrepresentative); the trim unhold and prompt-caching movers are both stalled on humans; no gate check would catch a regression back to ~103s. |
| CLWX-43 (LATENCY-UX budget + measurement) | 2026-05-27 | Raj | 'Its taking real long to respond now....thinking' — Raj's only twice-volunteered complaint (05-27, 06-27); cost a Minister demo slot | Baseline measured 09-02: ~103s median on the e2 VM (evidence/LATENCY_BASELINE_2026-09-02.md); Mac-lane measurement 2026-09-06 @b159d13c: routine no-tool 8.8s (within proposed p50<=15s), draft+write_docx 17.1s (near budget) via scripts/clwx43-latency-mac-lane.ts; KR6 trim branch 7add864b remains owner-HOLD (the cause-side lever) | Owner-signed p50/p90 budget + measurements on the PILOT LAPTOP meeting it (card: budget NUMBER is owner-gated). PROPOSED: p50<=15s / p90<=45s for the three demo-shaped prompts on the laptop lane | scripts/clwx43-latency-mac-lane.ts | Mac live lane 2026-09-06 (installed runtime); laptop lane never measured | Owner budget sign-off + laptop measurement remain; moe.19 build+VM-verify does NOT close it (e2 VM numbers are CPU-starved and non-representative; the bar is the laptop). The 09-06 run also reproduced a live timeout defect worth its own triage |
| CLWX-47 (slow-turn watchdog TB-3) | 2026-05-27 | Raj (same latency complaint, UX mitigation) | Turns sit on 'thinking' with no feedback — principal cannot tell working from dead | Card filed 2026-09-03 from FLOW_STATE_DIAGRAMS flow 2 (Backlog); Adjacent but distinct: CLWX-78's 90s failover watchdog shipped @2b21d2a8 — that is failover, not the 30s 'still working' notice | Card: renderer notice when a cloud turn exceeds 30s ('still working — you can keep typing') + backend per-turn duration log line for the latency ledger | — | never verified — nothing built | Unbuilt. moe.19 does NOT close it (no code); works at any budget so it is not blocked on the CLWX-43 owner sign-off |
| ACPX-NOISE (register B) | 2026-05-27 | internal (gateway boot) | acpx runtime backend probe fails at every gateway boot; adds handshake latency and log noise (system-presence RPC timeouts x3 in the same window) | Register note only; deliberately not carded (non-fatal) | PROPOSED: gateway boot log on a shipped build contains zero acpx probe failures and zero system-presence RPC timeout retries, or the probe is explicitly disabled for the MoE fork with a code comment | — | never verified — open on every observed boot | Open, low, unowned. moe.19 does not close it; needs a root-cause-or-silence decision |
| CLWX-73 / K1 / K2 (chrome-attach on real tester boxes) | 2026-05-27 | Karunesh (K1 06-05, K2 06-09) + owner (earliest sighting 05-27) | 'I seem to be having the same issue with accessing the email' — fresh-box CDP attach fails; repair path fell back to a MANAGED Chromium profile (profile=user hard-rule violation) | FIXED in-tree 2026-09-04 @e85f0ef7: root cause = Chrome >=136 silently refusing --remote-debugging-port on the DEFAULT user-data-dir; launcher now uses a dedicated NON-default user profile; managed dir never passed to launch args; card Ready; tests/unit/chrome-cdp.test.ts added | Ledger K1/K2 standing criterion: the K1 fresh-box scenario re-runs on EVERY RC and the attach succeeds (or degrades readably) on a real tester box; plus the needsLiveProbe: moe.gov.tt Conditional Access accepts a secondary non-default profile (Raj) | tests/unit/chrome-cdp.test.ts | in-tree only — NEVER verified on a build. moe.18 (cut 09-03, PRE-fix) still failed on Karunesh's real box 09-03 evening: agent could not CDP-attach, gave the plain-language retry instruction, retry still failed | moe.19 carries e85f0ef7 — the VM verify partially closes it, but the honest bar is Karunesh re-running the K1 scenario on HIS box on moe.19, plus Raj's moe.gov.tt CA probe. Until then email on real tester boxes is unproven |
| CLWX-88 (dead Forms Bearer-API tier) | 2026-05-27 | internal (forms) | Bearer-API fill tier returns '401 Required user login' since 05-27 and survives only as a do-not-touch trap note | Card CLWX-88 filed 2026-09-03 (Backlog, low); no code change | Card: remove the tier or fence it behind a hard-disabled flag with the trap note in code; MSFORMS_AUTOMATION.md updated; no script can reach it accidentally | — | never verified — nothing changed | Unpicked. moe.19 does not close it; a small disposition commit does |
| UPSTREAM-GAP (register B) | 2026-05-29 | owner (posture decision) | Fork ~102 commits behind upstream (v0.4.15->v0.5.2); divergence-at-release recurred (80 ahead / 49 behind at a laptop release moment) | 2026-08-20 audit pass: only 3a241cf0 (safeRmSync) merged; 4 other blockers documented as fork-specific (UPSTREAM_MERGE_ASSESSMENT_2026-08-20) | PROPOSED: accepted-risk posture is re-affirmed (or a cherry-pick list scheduled) at each GA milestone, recorded in the readiness doc | — | n/a — accepted-risk posture, not a fix | Open by design (audit-then-cherry-pick, never rebase mid-pilot). moe.19 irrelevant; only an owner posture review moves it |
| PWSH-LINT-GAP (register B, 2026-09-06 addition) | 2026-06-04 | internal (dev tooling; burned 5+ times incl. the PS 5.1 BOM regression) | Mac-side reviews could not parse/lint windows-pilot PowerShell, so PS 5.1-class bugs only surfaced live on the laptop | Fixed 2026-09-06 (CLWX-83): pnpm lint:ps — PSScriptAnalyzer 1.25.0 under pwsh 7.6.5 over 50 files, PSUseCompatibleSyntax pinned 5.1+7.0; Gate fails on Error/ParseError/5.1-incompat; fail-closed on missing settings and zero-file runs; falsifiability proven (PS7 ternary -> FAIL) | lint:ps fails the run on any 5.1-incompatible syntax across windows-pilot/**/*.ps1 (met, falsifiability shown) | pnpm lint:ps (scripts/lint-powershell.mjs, package.json:47) | Mac dev lane 2026-09-06 — dev-tooling, no build gate applies | — |
| K1 | 2026-06-05 | Karunesh (external tester) | "Chrome MCP existing-session attach failed for profile 'user'" on first install | CDP attach path redesigned (chrome-cdp repair flow); CLWX-73 fix commit e85f0ef7: launch CDP on a dedicated non-default user profile; managed-profile fallback made inert (@deprecated, never passed to launch args); spawned Chrome killed on port-bind timeout; moe.18 candidate commit 8fac374f 'close Karunesh trust/email/chrome gaps' (plain-language close-Chrome-and-retry instruction); Board: CLWX-73 State Ready (needsLiveProbe recorded) | Fresh box with Chrome installed but not running with the debug port: app surfaces a principal-readable instruction, NEVER a managed-profile sign-in for tenant flows, and email tools degrade readably (ledger-derived criterion) | tests/unit/chrome-cdp.test.ts (port_bind_timeout kill + never-launch-managed pins, runs in ga:gate unit leg) | moe.18 on Karunesh's real box, 2026-09-03 field report — messaging/no-managed halves held live; attach itself STILL FAILED | The readable-degrade and never-managed halves are externally confirmed, but CDP attach still fails on his real box so email stays unusable (K1 recurred on moe.18); owner live probe outstanding (moe.gov.tt Conditional-Access session on a non-default profile, per CLWX-73 needsLiveProbe); his follow-up image+document with details sit unretrieved in the WA bridge store |
| CLWX-19 + BAKED-KEY-FLOOR (sk-clawx key) | 2026-06-05 | owner (security floor) | Gateway key with sk-clawx prefix pasted in full into WhatsApp 2026-06-05, never rotated; compounded: the moe.15+ installers BAKE the same live key as a plain extractable file (resources/cloud-gateway.key, outside asar) and first boot persists it plaintext, and the shipped baseUrl bypasses the KR6 broker/caps | RCA correction recorded (PROBLEM_ROOT_CAUSE_FIX_MAP_2026-09-04 §5): rotation is a fleet-wide re-key with auto-update OFF, must be owner-sequenced; docs/RELEASE_PROVISIONING_PLAN.md staged (A-hardened -> broker-token pilot -> Entra-native GA, 14 owner decisions); No rotation performed (owner-gated) | Key rotated + installer re-baked + per-machine re-key completed; no shipped artifact carries a plaintext long-lived key (security-floor checkbox on GA_READINESS §4) | — | never — exposure VERIFIED IN THE ARTIFACT (moe.15 exe, 2026-09-03); every build since carries the same floor | Owner-gated, coupled to CLWX-18. moe.19 does NOT close it — worse, moe.19 re-ships the exposed key unless the provisioning plan's step A rides the cut. Closes only with the owner rotation sitting |
| K2 | 2026-06-09 | Karunesh (external tester) | Same attach failure on the updated build ("same issue with accessing the emails") | Same CLWX-73 fix chain as K1 (e85f0ef7, 8fac374f); chrome-cdp unit pins run on every gate pulse (scripts/ga-gate.mjs unit leg) | Regression rerun of the K1 scenario on every RC (ledger criterion — it recurred once already) | tests/unit/chrome-cdp.test.ts (unit-level only; harness-artifact.mjs honest mapping records K1/K2 as living in the Outlook/VM lanes, not the artifact matrix) | In-tree/unit at HEAD 2026-09-06; the only live rerun was Karunesh's own moe.18 run (2026-09-03) — and it failed | No automated live attach rerun exists per RC — the moe.18 VM matrix has no chrome-attach leg (VM has no signed-in tenant Chrome), so the exact regression he reported twice is still only caught by shipping to him; K1 recurrence on moe.18 proves the unit pin does not cover the real-box failure mode |
| K3 | 2026-06-09 | Karunesh (external tester) | PASS baseline: install, online default, gateway ~2 min, file scan + email read OK | CLWX-90 pnpm ga:gate built as THE acceptance gate (scripts/ga-gate.mjs; pulses recorded docs/evidence/GA_GATE_2026-09-04.md, GA_GATE_2026-09-06.md); moe.16/17/18 full-matrix VM verifies (install→launch→doc-read legs); launch-channel fixes 715eab73 + f5a9d4d7 keep the online-default half true on upgraded boxes | install→first-turn→file-scan→email-read stays green on every RC (Ext-val B shape; ledger criterion) | scripts/ga-gate.mjs (T0 legs: typecheck, lint, unit, bundle-verify, doc-tooling harness); moe.18 VM verify matrix (skills/laptop/evidence/2026-09-03-moe18-verify/RESULT.md) | moe.18 VM 2026-09-06 — install/launch/migration/K10-PDF legs PASS; email-read leg NOT runnable (placeholder gateway on the VM) | The email-read leg of his baseline is absent from every automated per-RC smoke (VM lane cannot exercise it; T1 live-lane gate legs are opt-in and were skipped in the recorded 7/0/2 pulses); Ext-val B itself is annotated 'qualifiers unmet — owner acceptance call' with his moe.15 run as nearest evidence (2026-09-06 KR5 reconcile) |
| CLWX-7 (production Forms destination) | 2026-06-10 | Raj / Ministry IT | No production destination defined for the two real MoE forms (SharePoint vs Power Automate) | Fill lane live-proven on the test.fac clone (09-02/03: Suspensions 29/32 + recorded submit; Daily Report 55/57); Elevated to concrete ASK 4 in docs/MINISTRY_ASKS_2026-09-02.md (Power Automate flow URLs recommended, Tier C) | Ministry returns flow URLs (or Tier-A sign-off) + version-freeze/notify-on-edit for the captured 32+57 field schemas; one gated submit lands in the PRODUCTION destination and is verified | tests/unit/forms-browser-submit-gate.test.ts (gate only, clone lane) | test.fac clone lane only (recorded submit 2026-09-03); production destination never exercised | Ministry-gated (KR8 family); not blocking single-school GA by scope call. moe.19 irrelevant; Raj's decision closes it |
| CLWX-8 (production Outlook teacher login + support flow) | 2026-06-10 | Raj / Ministry IT | No production teacher login or support-logging flow exists for pilot schools | Triage 2026-09-02: grouped into the Lane-3 owner packet (GA_SPRINT_PLAN §Lane-3) | PROPOSED: a production @moe.gov.tt teacher/support account exists, the app's email lane is exercised on it once (read-only), and a support-logging path is documented | — | never — Ministry-side action | Ministry-gated. moe.19 irrelevant |
| CLWX-40 (Graph-C: Chrome-less Outlook eval) | 2026-06-10 | Ministry (production auth reality) / internal | Production Outlook for many principals cannot ride the browser-session hack — the GA-durable lane must be proven on Graph transport with no Chrome session | Predecessor CLWX-39 wiring landed 09-03; L1-L3 PASS live on the real tenant (graph-signin-smoke); L4 runner exists: scripts/v2-eval-graph.ts (read rows assert real data; draft/send rows assert graceful refusal; anti-mock guard; structurally cannot dispatch mail) | L4: the 14-row eval re-run on Graph transport passes read rows at the browser lane's 15/15 bar with draft/send showing graceful refusal under read-only scopes; L5: the whole ladder on a Chrome-less fresh install (GRAPH_TEST_PLAN.md) | scripts/v2-eval-graph.ts (staged, never run); scripts/graph-signin-smoke.ts (L1-L3 PASS 2026-09-02) | L1-L3 on the real tenant 2026-09-02; L4/L5 NEVER run | Blocked ONLY on one ~2-min interactive sandbox sign-in to persist tokens, then L5 needs any Chrome-less machine. moe.19 build does not close it; the operator sign-in + eval run does |

| RAJ-1 (G4 / CLWX-34): send-gate failure | 2026-06-21 | Raj Ramdass (Ministry ICT) | Send fails: "draft subject has been changed before it can be sent" (HIGH) — reported the day before the principals demo. | Two-sided root cause found live 2026-09-02 during the outlook.cloud.microsoft migration session: a subject-gate FALSE NEGATIVE (a drifted draft actually went out — self-addressed sandbox) masked by post-dispatch verification failure, which produced Raj's exact error text; Fixed commit a8322ad9 (electron/services/outlook-browser-v2/outlook-actions.ts + inverted contract tests in tests/unit/outlook-actions-safety.test.ts); live 4-step gate proof PASS + 15/15 v2-eval + 73/73 contract units on the new domain; Hardened further 2026-09-06: mandatory subject second-gate on EVERY confirmed send, commit 1e3a9170 (closes the reviewed-path bypass, Codex HIGH via CLWX-71) — in-tree only, first carried by moe.19; Consolidated defects-closed update SENT to Raj 2026-09-03 (ledgered: ~/openclaw-agent/outbound-sent/2026-09-03-raj-graph-working-and-defects-closed-SENT.md, verified on disk) | A confirmed send whose open compose-pane subject differs from args.subject is refused before touching Outlook; a matching subject dispatches; proven by refusal contract units plus the live 4-step two-gate send proof. | tests/unit/outlook-actions-safety.test.ts (87 tests incl. "REFUSES when the open draft subject differs from the confirmed subject (hard gate)") — in unit-suite, ga:gate T0; scripts/v2-eval.ts (ga:gate T1 'outlook-eval 15-row'); scripts/v2-send-test.ts (ga:gate T1, opt-in GA_GATE_SEND=1) | Mac live lane (in-tree driver over user-Chrome CDP, test.fac) 2026-09-02, re-run 2026-09-03; last full T1-live gate GREEN 2026-09-04 (9/0/2). Never proven in-app on a packaged build — VM K11 email leg 2026-09-06 never reached the tools (on-device hallucination loop; cloud path unverifiable on the VM's placeholder gateway). | Driver-layer fix is pinned and live-verified, but an end-to-end in-app send on a packaged build has never run; the 1e3a9170 second-gate hardening is unverified on any build (moe.19 cut 2026-09-06, no verify evidence); the T1 live pins were SKIPPED in the 2026-09-06 static gate and the Chrome CDP attach wedge (2026-09-05) may still block re-running them. |
| RAJ-2 (G4 / CLWX-34): reply misinterprets content | 2026-06-21 | Raj Ramdass (Ministry ICT) | Reply misinterprets email content — meal preferences read as shirt sizes (MED). | Model-layer fidelity scenario built (commit a0f390b4) and run LIVE 2026-09-03: scripts/raj2-reply-fidelity-check.ts — richest real inbox email (101 distinct content words), live cloud summarise+draft-reply, dual deterministic assertion PASSED (coverage 4/8 at the 50% floor, ZERO invented entities) — NOT REPRODUCED at the model layer; nothing dispatched; evidence skills/laptop/evidence/2026-09-03-raj2-fidelity/RESULT.md; By-catch: STALE-READ filed as CLWX-46 (readEmail returned a different email's body — the plausible root cause of Raj's report), then FIXED same sprint: TB-1 settle-on-expected-item bounded poll + TB-2 reading-pane selector 5-step fallback chain in outlook-actions.ts; verified live 3x3 rows with zero leaks; CLWX-46 Ready; CLWX-46 guard promoted into the acceptance gate: ga:gate T1 'stale-read check (CLWX-46 guard)' (scripts/ga-gate.mjs:93) | Existing (from the disposition run): a live summarise/draft-reply turn over a seeded structured email covers >=50% of frequency-ranked distinctive source words with zero invented entities, AND readEmail never returns another email's body (a stale-read leak is the only fail). | scripts/raj2-reply-fidelity-check.ts (re-runnable, exit-code contract 0/1/2/3/4 — not wired into ga:gate); scripts/clwx46-stale-read-check.ts (ga:gate T1 'stale-read check (CLWX-46 guard)'); unit coverage of the fix in the 162-file suite (outlook DOM heuristics/actions tests), ga:gate T0 | Mac live lane 2026-09-03 (fidelity refutation) + full ga:gate T1-live GREEN 2026-09-04. Disposition is refuted-not-fixed; the CLWX-46 stale-read fix is live-lane verified, not packaged-build verified. | Refutation is n=1 on one email; the original misread was never reproduced, so STALE-READ remains a plausible — not proven — root cause; the fidelity check itself is not in any recurring gate; live pins skipped in the 2026-09-06 static gate (CDP lane health unconfirmed since the 09-05 attach wedge). |
| RAJ-3 (G4 / CLWX-34): reply archives the original | 2026-06-21 | Raj Ramdass (Ministry ICT) | Reply action archives the original email (MED). | REFUTED by targeted scenario 2026-09-02 (commit 92015c75): scripts/raj3-reply-archive-check.ts — read inbox, open reply on newest message, discard, re-read: original still in Inbox (exit 0); Re-refuted 2026-09-03 in the stakeholder regression battery (118/118 unit contracts + 15/15 v2-eval on the live tab); By-catch 2026-09-05 (scripts-typecheck census, commit c4e11d4f era): register row RAJ3-FROM-NOOP — the check's from-matching silently degrades to subject-only ('' === '' via ?? '') because InboxMessage declares no `from` field; row is OPEN (low), script excluded in tsconfig.scripts.json with reason | Existing: after a reply flow (discarded or dispatched), the original message remains present in the Inbox list, matched on fields the inbox contract actually declares. | scripts/raj3-reply-archive-check.ts (standalone live check — NOT in ga:gate, and its from-field matcher is a known no-op per register row RAJ3-FROM-NOOP) | Mac live lane 2026-09-02, re-run 2026-09-03. Refutation only — no code was changed for this defect. | The pin is weaker than it claims (RAJ3-FROM-NOOP: subject-only matching, still OPEN, script typecheck-excluded) and is not wired into ga:gate, so nothing recurring guards against a future regression that would reproduce Raj's report. |
| RAJ-4 (G4 / CLWX-34): draft landed in the To: field | 2026-06-21 | Raj Ramdass (Ministry ICT) | Draft response landed in the "To:" field (one-off) (LOW). | Root cause reproduced live 2026-09-02 on the new outlook.cloud.microsoft domain: the verifier classified inbox LIST ROWS as recipient wells (loose aria-label substring selectors); Fixed commit a8322ad9: recipient wells must be editable fields; 15/15 live eval PASS; Consolidated defects-closed update SENT to Raj 2026-09-03 (ledgered, verified on disk) | Existing: reply/forward flows reject ("recipient field" error) rather than typing body text when no editable recipient field is resolvable; body lands only in the contenteditable message-body region; live eval fully green. | tests/unit/outlook-reply-forward-field-targeting.test.ts (rejects on non-editable recipient targets) — unit-suite, ga:gate T0; scripts/v2-eval.ts (ga:gate T1 'outlook-eval 15-row') | Mac live lane 2026-09-02/03 (15/15 eval); last full T1-live gate GREEN 2026-09-04. Same driver code ships in the packaged app, but no packaged-build in-app repetition exists. | Effectively closed+pinned+live-verified; residuals are lane-shaped only — the live pin hasn't re-run since 2026-09-04 (09-06 gate was static; 09-05 CDP attach wedge unresolved on record) and no packaged-build in-app email proof exists for any of the RAJ email rows. |
| K4 | 2026-06-22 | Karunesh (external tester) | Voice note failed: clip-input.wav (raw audio; ffmpeg ffmpeg-not-found) | ffmpeg bundled with the Windows installer, packaged path checked first (electron/main/asr-ipc.ts; commit befeb43f keeps Windows off the Whisper-only path) — in moe.12+; CLWX-20 State Ready; windows-pilot/pilot-asr-smoke.ps1 authored; ASR_SMOKE_OK on the moe.15 VM (V-batch 2026-09-03, skills/laptop/evidence/2026-09-03-vbatch/RESULT-asr.md); CLWX-87 WER bench built (610779dd): scripts/clwx87-wer-bench.mjs + windows-pilot/scripts/pilot-asr-wer.ps1; Mac whisper legs measured 2026-09-06 (tiny/base 13.5%) | Voice-note transcription e2e on the PACKAGED build (both OS) stays in the recurring matrix — the fix regressed from a packaging change once already (ledger criterion) | windows-pilot/pilot-asr-smoke.ps1 (STATE: ASR_SMOKE_OK); scripts/clwx87-wer-bench.mjs (8 unit guards) + eval/fixtures/clwx87-asr-manifest.json; windows-pilot/scripts/pilot-asr-wer.ps1 (System.Speech leg, VM-gated) | moe.15 VM 2026-09-03 (ASR_SMOKE_OK on the packaged runtime); Mac WER legs 2026-09-06 are bench-only, not a packaged voice-note turn | Not a recurring per-RC row: ASR was not re-run on moe.16/17/18 (the only packaged proof is one moe.15 run), the Windows System.Speech WER leg is gated on the owner starting the TERMINATED RC VM, and no Mac packaged voice-note e2e is pinned anywhere |
| ATLAS-15 (forms.preview sign-in interstitial) | 2026-06-22 | internal (demo-eve YELLOW) / production path | forms.preview_* times out / redirects to login.microsoftonline.com when the automation profile is not signed in to the tenant | Sign-in interstitial detection shipped (partial); GA production path explicitly gated on KR7/KR8 identity | PROPOSED: on a box with no tenant session, forms.preview returns a readable needs-sign-in refusal (never a raw timeout), and on a signed-in production identity (post-KR7/KR8) preview succeeds | — | partial detection in-tree; production-auth leg never verifiable (KR8 placeholders) | Auth-state class, not selectors. moe.19 does not close it; KR7/KR8 real values do |
| K5 | 2026-06-23 | Karunesh (external tester) | "Reverted to the original issues" after an update | ga-e2e-regression known-failure matrix skill (.claude/skills/ga-e2e-regression/SKILL.md) + ga-e2e-regression-verifier agent; CLWX-90 ga:gate as the single acceptance command; gate pulses ledgered per tick (GREEN 7/0/2 on 2026-09-06); harness-artifact --fast wired into the package script (every build path inherits 8 pinned rows) | The known-failure regression matrix runs on every RC — no fix ships unguarded (ledger criterion) | scripts/ga-gate.mjs + docs/evidence/GA_GATE_2026-09-04.md / GA_GATE_2026-09-06.md; scripts/harness-artifact.mjs --fast in the package chain (package.json line 81) | moe.18: full-matrix VM verify completed 2026-09-06; gate GREEN at HEAD 2026-09-06 (Mac dev tree) | The bar was procedurally broken on the newest RC: moe.18 was SHIPPED to Karunesh before its full-matrix verify ran (owner override ×3, ledgered) and the verify then found real failures (K13 dir-2 partial fail, K11 new finding, NSCC absence) — so 'no fix ships unguarded' is not yet an enforced gate, only a recorded intention; T1 live legs remain opt-in skips in the routine pulses |
| K6 | 2026-06-23 | Karunesh (external tester) | "Replying: the body is placed on the To: field" + "the email being replied to is moved to the archive folder" (+ Reply Function Errors.docx) | RAJ-4 fixed-verified a8322ad9 (recipient wells must be editable fields; subject-gate false negative closed; survives the outlook.cloud.microsoft migration); RAJ-3 refuted by targeted scenario 92015c75 (scripts/raj3-reply-archive-check.ts exit 0, re-refuted 2026-09-03); CLWX-34 State Ready (all four Raj June defects dispositioned) | v2-eval W5.1 + raj3-reply-archive-check + recipient-well classifier tests stay pinned (ledger criterion) | scripts/v2-eval.ts (W5.1 reply row; 15/15 live); scripts/raj3-reply-archive-check.ts; tests/unit/outlook-actions-safety.test.ts + tests/unit/outlook-reply-forward-field-targeting.test.ts (recipient-well classifiers); tests/unit/outlook-dom-heuristics.test.ts (13/13 incl. recipient-well adversarial pins, CLWX-74 tick) | Mac live lane (test.fac, outlook.cloud.microsoft) 2026-09-03 — 15/15 v2-eval + RAJ-3 re-refuted; 73/73→84/84 contract units at HEAD | Two honest residuals: the raj3 pin itself has open defect RAJ3-FROM-NOOP (DEFECT_REGISTER — its from-matching silently degrades to subject-only, a weaker check than it claims), and the reply flow has never been re-proven on a Windows packaged build/tester box (all live proof is the Mac test.fac lane) |
| K7 | 2026-06-23 | Karunesh (external tester) | Demo day: "everything was working besides the reply feature" | Covered by the K6 fix chain (a8322ad9 + 92015c75; CLWX-34 Ready) | PROPOSED: the reply flow (open reply → body in body field → original stays in Inbox → gated send) passes on the live lane on every RC that touches the Outlook driver | scripts/v2-eval.ts W5.1; scripts/raj3-reply-archive-check.ts | Mac live lane 2026-09-03 (same evidence as K6) | Same as K6 — the pins run only on the Mac live lane, not per-RC on a packaged build; Karunesh has never personally re-confirmed reply working (his moe.18 email retest died upstream at chrome-attach, K1) |
| G5-VERDICT (register B) | 2026-06-23 | owner / Raj (feedback loop) | No post-demo verdict was captured for the 2026-06-23 principals demo; a circulating '0/5' score has NO source | Register rule recorded: never cite '0/5' without a source | PROPOSED: a sourced post-demo verdict (attendee, date, outcome) is filed in the liaison log, or the row is closed as UNRECOVERABLE with the citation ban kept | — | n/a — process gap | Open; only Raj can supply the verdict. moe.19 irrelevant |
| VITEST-OOM (register B, 2026-09-06 addition) | 2026-06-23 | internal (test infra) | Combined Outlook vitest run crashed with FATAL heap exhaustion; lanes worked around by running suites one at a time | Fixed 2026-09-06 (CLWX-83): named target pnpm test:outlook (11 suites/199 tests, 12.6s); heap bounded via TOP-LEVEL test.execArgv --max-old-space-size=4096 (adversarial review proved poolOptions.forks.execArgv is ignored in Vitest 4); Committed worker-side heap probe makes the claim a per-run assertion | pnpm test:outlook runs all Outlook suites in one invocation without OOM, with the execArgv reaching the forks asserted per-run (met) | tests/unit/clwx83-vitest-heap-execargv.test.ts; pnpm test:outlook (package.json:52) | Mac dev lane 2026-09-06 — dev-tooling, no build gate applies | — |
| RAJ-NSCC (CLWX-42): Code of Conduct in memory + his Q&A eval | 2026-06-27 | Raj Ramdass (Ministry ICT) | Have the National School Code of Conduct (NSCC-2026.pdf) in the AI's memory instead of file lookups — his clearest explicit feature ask; he had also supplied a stakeholder-authored eval set (NSCC_2026__Test_QnA.docx, 2026-05-14) that sat unrun until 2026-09-03 (HARD FLAG in the stakeholder sweep). | Eval built + run 2026-09-03 (commit a00ada49): his 20 Q&A rows verbatim -> eval/fixtures/nscc-qna.json + scripts/nscc-qna-eval.ts — 18/20 (90%), 20/20 NSCC citations, wrong-edition guard proven (evidence: skills/laptop/evidence/2026-09-03-nscc-qna-eval/); In-app knowledge pack landed (ae4779c5): data/nscc-2026.txt + principal.nscc_lookup tool + persona steering; hardened through 4 review lanes (d215ebe0 retrieval, b547601b correctness + integrity gate, ebe73bea trust-lens legal-boundary fold, 995ae457 retention guard); tool lane 17/20 (floor 80%), context lane 18/20, deterministic fixture 20/20 (skills/laptop/evidence/2026-09-06-clwx42-nscc-pack/); card READY 2026-09-06; moe.19 cut 2026-09-06 (d0d9bd9a) is the first build carrying the pack | Existing (card): fresh session, NO file attached, in-app — a Code-of-Conduct question answers correctly with an NSCC citation via principal.nscc_lookup, and the stakeholder Q&A eval passes at the 80% floor with NSCC citations. | tests/unit/nscc-lookup.test.ts (unit-suite, ga:gate T0); scripts/nscc-qna-eval.ts + eval/fixtures/nscc-qna.json (ga:gate T1 'NSCC Q&A eval (CLWX-42)' — opt-in GA_GATE_FULL=1 only) | In-tree + Mac live-lane evals only. On the shipped moe.18 VM verify (2026-09-06) the pack was PROVEN ABSENT — K14 answered 5/5 but content-shallow. First build carrying it (moe.19, cut 2026-09-06) — NEVER verified on a build. | The literal thing Raj asked for — a fresh in-app session answering from memory — has never happened on any build: the running gateway predates the tool (next-build/restart gate on record), the field build (moe.18, in Karunesh's hands) demonstrably lacks the pack, the ga:gate NSCC row is opt-in so the default gate never exercises it, and the provenance re-pull of Raj's original NSCC-2026.pdf binary is an outstanding owner ask. |
| CLWX-42 (NSCC knowledge pack in-app) | 2026-06-27 | Raj | 'Is there any way we can have that document [School Code of Conduct] in the AI's memory' — then K14 on moe.18 answered 5/5 generically because the pack was ABSENT from the installed tree | Eval built+run 09-03: 18/20 (floor 80%) on Raj's own Q&A doc; wrong-edition guard proven; Pack SHIPPED in-tree 2026-09-06 (Ready @ebe73bea): data file sha-pinned in the plugin dir + principal.nscc_lookup retrieval tool (top-8 passages <=~12KB, KR6-conscious) + persona steering + not-a-lawyer boundary guard; tool lane 17/20=85% sustained; guards 110/110; all 4 review lanes folded | State-vector NEXT-BUILD bar: one fresh-session no-file K14 question on the first build/restart carrying these commits answers grounded in the NSCC with a citation (human closes on that) | scripts/nscc-qna-eval.ts (NSCC_EVAL_LANE=tool, 17/20 floor 80%); eval/fixtures/nscc-qna.json (20 rows, fixture coverage 20/20) | in-tree + Mac eval lane 2026-09-06; moe.18 VM 2026-09-06 verified the pack ABSENT (pre-fix build) | moe.19 build + one fresh-session K14 question on the VM CLOSES it (or an owner gateway restart on a dev box closes the restart-gated half sooner) |
| K8 | 2026-06-30 | Karunesh (external tester) | "SOMETIMES the agent reads the pdf documents well and other times it gives an error... same behavior when we make requests to create word or excel documents" | Root-caused 2026-09-03 as load-path-dependent parser resolution (canvas binding never shipped + loadDep masking) — the CLWX-72 four-layer fix; CLWX-97 (68e02ee1): CLAWX_APP_RESOURCES=resourcesPath passed to the packaged gateway fork; State Ready, needsLiveProbe recorded; CLWX-77 artifact matrix: K8 intermittence rows repeat 3× in fresh children, electron-env spawn parity twins, real-gateway transport rows, package fast lane (91f1c588, 38fee5c6, 96da61f1, 30a3d46f, 67543cd1) — matrix 45 rows 30/14/0/1 GREEN at HEAD | Doc read/write exercised REPEATEDLY (≥3×) per run on the packaged runtime from every load context (chat turn, host-API, cold gateway); disagreement = FAIL INTERMITTENT (ledger criterion, encoded in the harness) | scripts/harness-artifact.mjs (kLedger:'K8' rows, repeat:3, @electronlike twins, staged-gateway transport); scripts/verify-openclaw-bundle.mjs (package-chain gate); tests/unit/config-sync.test.ts (CLWX-97 env pin); harness fast lane inside pnpm package | Harness at HEAD (Mac, in-tree) 2026-09-06; doc READS externally confirmed by Karunesh on shipped moe.18 ("working fine now", 2026-09-03 field report) | The Windows-lane harness run is still owed (RC VM TERMINATED; NODE_OPTIONS backslash nuance recorded), CLWX-97's own live probe (packaged mac / relocated Windows read) is owed, and the WRITE half of his complaint has no packaged in-app proof (live in-app write turn = CLWX-65, still open); CLWX-77 remains In Progress |
| KAR-PDF / G6a (register B) | 2026-06-30 | Karunesh via Raj | 'Sometimes the agent reads the pdf documents well and other times it gives an error' — no repro detail at the time | Root-caused 3 months later as the canvas-binding/packaging chain: CLWX-72 (supportedArchitectures + DOMMatrix polyfill + verify-openclaw-bundle gate), CLWX-92 (workerSrc), CLWX-97 (CLAWX_APP_RESOURCES load path @68e02ee1); moe.18 K10 fresh-session PDF read PASS on the VM; Karunesh field report 09-03: documents 'working fine now' | PROPOSED: register row flipped CLOSED-BY-SUCCESSOR citing K10 PASS + the external confirmation; K8/K10 rows stay in the standing regression matrix per RC | scripts/verify-openclaw-bundle.mjs (package-chain gate); K10 row in the moe.18 verify matrix (skills/laptop/evidence/2026-09-03-moe18-verify/RESULT.md) | moe.18 VM 2026-09-06 (K10 PASS) + Karunesh's real box 2026-09-03 (external confirmation) | Substantively closed; residue is bookkeeping (register row still says UNVERIFIED vs original) + CLWX-76 leg-3's mammoth/xlsx truthful-error live re-run which rides moe.19 |
| DOCSEARCH / G6b (register B) | 2026-07-17 | Karunesh via Raj | Document-search inconsistency — no repro detail ever supplied | None attributable; adjacent doc-tooling fixes shipped but this report was never reproduced | PROPOSED: obtain one concrete repro from the tester (file + query + expected hit) and either fix or close CANNOT-REPRO with the ask documented | — | never — UNVERIFIED for lack of repro | Open-unverifiable. moe.19 does not close it; only a tester repro (or an honest disposition) does |
| RAJ-REDIRECT/ENTRA (G7 / CLWX-39/40) | 2026-07-20 | Raj Ramdass (via Ansari, Ministry ICT) | "The only thing I absolutely need is the redirect URIs" — app registration approved, read-only delegated scopes named; we said "on it" and delivered nothing for ~6 weeks (the structural finding: last inbound on this product was this approval). | Answered 2026-09-01 (condensed reply) and 01b delta SENT 2026-09-02 with owner GO: enumerated permissions list + dev redirect URI http://localhost:53682/callback (ledgered: outbound-sent/2026-09-02-raj-permissions-delta-SENT.md, verified on disk); Ministry registered the dev URI + delivered the real client id; Graph ladder L1-L3 PASS live 2026-09-02 against the real tenant (9590bb09..., pure PKCE, no client secret stored anywhere): scripts/graph-signin-smoke.ts, evidence skills/laptop/evidence/2026-09-02-graph-signin-L1-L3/RESULT.md; tenant discovery: moe.gov.tt + fac.edu.tt are ONE tenant, so no twin registration needed; In-app Graph lane landed 2026-09-03 (CLWX-39 comment: read-only default scopes, Settings transport toggles, scope-aware compose refusal, Graph-403 -> structured refusal); L4 runner staged: scripts/v2-eval-graph.ts (anti-mock guard, structurally cannot dispatch mail); Graph read-only transport behind flag also landed per CLAUDE.md capability row (CLWX-39) | Existing (CLWX-39/40 ladder): sign-in -> token (stable oid claim) -> Graph read PASS (met at L1-L3); full bar: the Graph-transport Outlook eval (L4) green after one interactive sandbox sign-in, then L5 on a Chrome-less install; production redirect URI recorded at the KR8 working session. | scripts/graph-signin-smoke.ts (re-runnable L1-L3 harness; --persist stages L4); scripts/v2-eval-graph.ts (L4 runner, staged — never run); tests/unit/outlook-routes-graph.test.ts, tests/unit/microsoft-graph-send-safety.test.ts, tests/unit/microsoft-graph-outlook-adapter.test.ts (unit-suite, ga:gate T0) | Mac live lane 2026-09-02 (L1-L3 vs the real Ministry tenant, test.fac PKCE). In-app Graph sign-in + L4: never run. Chrome-less install (L5): never run. | The 6-week external gate is cleared but the ladder stops at L3: L4 waits on one ~2-minute operator sandbox sign-in; CLWX-39 held below Ready pending the live in-app sign-in; CLWX-40 (Chrome-less eval) still Todo; production redirect URI keys off the hostname decision that only the KR8 working session can settle — ball with Raj since 2026-09-02. |
| K9 | 2026-07-21 | Karunesh (external tester, relaying Raj's suite) | ClawX Agent Tests.zip — Raj's 5-prompt Ministry suite scored 0/5 | KR1 doc-tooling chain fixed; CLWX-24 State Ready with in-app PASS on moe.12 (docs/evidence/KR1_INAPP_RUN_2026-09-02.md) + moe.17 in-app proof; harness/run.ts — 5-prompt doc-tooling E2E harness (deterministic prompts from tests/e2e/prompts.json) wired into ga:gate T0 and pnpm preflight | The 5-prompt suite is a permanent regression fixture (in-app, packaged build), not a one-time gate (ledger criterion) | harness/run.ts + tests/e2e/prompts.json (pnpm harness:doc-tooling-e2e); scripts/ga-gate.mjs row 'doc-tooling harness (KR1 proxy)' | In-app: moe.17 (2026-09-03/04); per-RC: harness PROXY only, green at HEAD 2026-09-06 | The automated per-RC leg is a harness proxy (direct doc-tooling entrypoints), not the in-app packaged-build run the criterion names; the in-app leg is manual and was NOT re-run on moe.18 (its VM verify ran K14's prompts, not Raj's five) |
| RAJ-0OF5 (K9 / KR1 / CLWX-24): the 2026-07-21 5-prompt Ministry suite | 2026-07-21 | Raj Ramdass (Ministry ICT) | ClawX Agent Tests.zip / Prompt Tests.docx: all 5 Ministry prompts (docx read, docx rewrite/save, PDF summary, xlsx grading, image OCR) scored 0/5 — agent refused each with missing-tooling apologies ("the necessary tools to read Word documents are not available... 'pandoc' and 'python'"). | Doc-tooling steering fixed: c1b18125 (route document.* over Python skills + widen discovery) + 73514b4f (tool-selection/discovery eval pipeline); Replay 2026-08-20: all five prompts 7/7 checks vs the 07-21 result, against the shipping handlers — but on RECONSTRUCTED fixtures (Raj never supplied the real files): skills/laptop/evidence/2026-08-20-raj-prompt-replay/ (replay-raj-prompts.mjs + REPORT.md); KR1 in-app live-LLM doc turn PASS on shipped moe.12 (Windows IAP VM, 2026-09-02): docs/evidence/KR1_INAPP_RUN_2026-09-02.md — incl. OneDrive-KFM resolve; first-pass KFM theory honestly corrected (malformed fixture, resolution works); CLWX-24 Ready; Doc-tooling harness promoted to the acceptance gate: ga:gate T0 'doc-tooling harness (KR1 proxy)' + 'bundle-verify (CLWX-72 gate)' (both PASS in GA_GATE_2026-09-06.md); moe.18 VM verify 2026-09-06: K10-PDF + K13-dir1-core PASS on the shipped build | Existing (K9 ledger): the 5-prompt suite is a permanent regression fixture run IN-APP on a PACKAGED build — not a one-time gate; KR1's bar (one in-app live-LLM doc turn) is met, the verbatim-suite bar is not. | pnpm run harness:doc-tooling-e2e (ga:gate T0 'doc-tooling harness (KR1 proxy)'); scripts/ga-gate.mjs T0 'bundle-verify (CLWX-72 gate)' (4 parsers loadable on host — guards the refusal class); skills/laptop/evidence/2026-08-20-raj-prompt-replay/replay-raj-prompts.mjs (fixture replay, plugin-direct — not wired into any gate) | moe.12 VM in-app 2026-09-02 (KR1 single-prompt PASS); moe.18 VM 2026-09-06 (K10-PDF/K13 legs PASS); harness leg green in ga:gate 2026-09-06. | The verbatim 5-prompt suite has never been run in-app on a packaged build (KARUNESH_GUIDED_TEST names that run as "the strongest possible closure of the 0/5 record"); fixtures remain reconstructions — asking Raj for the real test files is an open GA_PLAN item; the original 0/5 log wasn't preserved, so the root-cause split between steering and the parser class is probabilistic (PROBLEM_ROOT_CAUSE_FIX_MAP downgrade note); K8 latent path-resolution risk on macOS-packaged/relocated installs is on record. |
| CLWX-18 (public repo + leaked test password) | 2026-07-27 | owner (estate hard rule) | dmvevents/clawx-pilot is PUBLIC with the FULL SOURCE (901 files) and the test.fac password in 3 public files — but pilot users download installers from its Releases | Working tree scrubbed @f99f2c1f (0 literals); Wiki decision doc (REPO_AND_RELEASE_MAP.md @59e1ba26): 3-decision split (rotate password / releases-public+source-private / hold Lane-C reply); No visibility change (owner-gated; destructive + breaks tester downloads) | Repo private (or source stripped to releases-only) + history purged (git filter-repo) + test.fac password rotated — security-floor checkbox on GA_READINESS §4 | — | exposure re-verified live 2026-09-01; nothing shipped fixes it | Owner-gated one-sitting fix (couples with CLWX-19). moe.19 does NOT close it and cannot — this is repo/credential surgery, not a build |
| OWNER-TRIM-K11 | 2026-08-03 | Anton (owner) | Standing HOLD on trim branch fix/tool-catalog-trim (7add864b, authored 2026-08-03): "HOLD, not self-approved, not merged. Owner gate: unblocking the trim review is Anton's call (do not push/merge)" (CLWX-29); persona doc records it as a standing decision "on HOLD pending latency-budget sign-off". The scope work since is DECISION EVIDENCE assembly for his unhold call. | docs/SCALE_ANALYSIS_2026-08-20.md (commit 982fd5d4): 7,550-token fixed floor = ~71% of every cloud turn; 100M/mo dies at ~20 schools — the economics case; Trim live-verified on Windows moe.11 (2026-08-20): tool-cascade 6/6 -> 0/6, residual exec self-recovers (memory: on-device tool-cascade blocker CLEARED); K11 finding, moe.18 VM verify 2026-09-06 (skills/laptop/evidence/2026-09-03-moe18-verify/RESULT.md row 8): on-device qwen2.5:3b hallucinated a filesystem read and looped it ~15x with no loop-breaker, never reaching the email tools — K1/K5 tool-cascade class, explicitly recorded as strengthening the trim-unhold call; CLWX-43 Mac-lane latency (b159d13c, 2026-09-06): real in-app turns 8.8s routine / 17.1s draft+write vs ~103s VM baseline — evidence the proposed p50<=15s budget is attainable; Caps half (5b) landed flag-gated without waiting on the trim: services/model-broker/usage-meter.mjs, commit deff5c7d | PROPOSED: owner records the unhold + latency-budget sign-off (p50 <=15s / p90 <=30s) on CLWX-29/CLWX-43; trim merges to main; per-turn floor then MEASURED <=2,500 tokens on a live cloud turn (the KR6 box), with the measurement added as a ga:gate check. | tests/unit/model-broker-caps.test.ts (28 tests — pins only the caps half, flag OFF by default) | Trim behavior live-verified only on Windows moe.11 (2026-08-20); K11 evidence on moe.18 VM 2026-09-06; branch itself never merged — main untouched | The owner decision is still open (KR6 box unchecked, GA_READINESS G9 still RED); the trim is unmerged so every shipped build still carries the ~7,550-token floor AND the K11 on-device hallucination-loop class; no automated check measures the token floor, so even post-merge the <=2,500 bar has no pin; CLWX-29 remains In Progress owner-gated. |
| RAJ-INFRA (G8 / CLWX-31 / KR8): the 20-placeholder infra handoff | 2026-08-18 | Raj Ramdass (via Ansari, Ministry ICT) | MOE Email AI Assistant Handoff doc: APIM+Foundry (100M tok/mo, UserId header), PostgreSQL, Entra app — with 4 design conflicts vs the desktop product (app-server vs desktop, client secret vs PKCE, read-only scopes, redirect URI) and ALL 20 values placeholders (redirect URI, APIM hostname, PostgreSQL hostname included); nothing probeable. | Handoff committed verbatim: docs/MINISTRY_INFRA_HANDOFF_2026-08-18.md; every placeholder dispositioned in docs/MINISTRY_ASKS_2026-09-02.md (headline: nothing Ministry-side is connectable today, and single-school GA doesn't need it); Condensed reply SENT via WhatsApp 2026-09-01, owner-authorized (first entry in the new ~/openclaw-agent/outbound-sent/ ledger — closes G15; entry verified on disk); 01b delta SENT 2026-09-02 (permissions + dev URI) — cleared the owed-deliverable half; the Entra thread of the handoff is now unblocked through L1-L3 (see RAJ-REDIRECT row); 9-ask working-session agenda finalized (desktop-direct ratification, APIM real values + 429 policy, prompt caching, Forms Power-Automate destinations, credential-handoff mechanism); KR8 accept criteria on CLWX-31 (In Progress); Related guard landed: tests/unit/cloud-gateway-userid-header.test.ts pins the UserId-header behavior the handoff's APIM contract requires (KR7-adjacent) | PROPOSED (from CLWX-31/KR8 accept): a held working session whose notes record the app-server-hostname/architecture decision, the final production redirect URI, and the prompt-caching answer; every remaining placeholder either replaced with a probeable real value or formally dropped for desktop-direct. | tests/unit/cloud-gateway-userid-header.test.ts (unit-suite, ga:gate T0 — the only machine-testable sliver; the endpoints themselves are unverifiable placeholders) | Not verifiable on any build or lane — all 20 handoff values remain placeholders (docs memory: no probe is possible; don't re-attempt). | The working session has never been scheduled — ball with Raj since 2026-09-02 with no inbound on this product since 2026-07-20 (except the relayed "demo version for Monday" signal); the credential link expired unopened ~2026-08-26 and reissue is unanswered; hostname/architecture, APIM values, per-user 429 policy, and prompt caching all open; downstream, the G9 token-budget ceiling (~71% fixed floor per turn, fleet 429 at ~20 schools) stays unresolved pending KR7 identity + the owner's trim unhold. |
| KR6 / CLWX-29 (token floor + caps) | 2026-08-18 | Ministry (APIM handoff) + owner | ~7,550-token fixed floor = ~71% of every turn; shared 100M/mo key gives fleet-wide 429 at ~20 schools | Per-user caps landed @deff5c7d behind CLAWX_PER_USER_CAPS (soft daily cap + 90% fleet-reserve -> structured 429 the degrade path consumes); 28 tests green; Trim branch 7add864b (~2,000 floor) remains owner-HOLD; moe.18 K11 loop evidence recorded as strengthening the trim-unhold case | Card: (a) trim merged (owner unhold), (b) fleet verification with real UserId attribution against the real APIM hostname showing per-school metering — both currently impossible without KR7/KR8 | tests/unit/model-broker-caps.test.ts (28 tests) | in-tree only, flag OFF — NEVER verified on a build; shipped lane bypasses the broker entirely (BAKED-KEY-FLOOR) | Blocked on: owner unhold (trim), Ministry KR8 values, and the provisioning-plan lane switch so shipped builds actually route through the broker. moe.19 does not close it |
| KR7 / CLWX-30 (Entra sign-in + UserId) | 2026-08-18 | Ministry | Per-user attribution requires an identity the desktop app did not carry (UserId header on every gateway request) | Agent-buildable half DONE 09-03: UserId=<oid> stamped at seed + re-stamped live on Graph sign-in/out (gateway reload threaded — adversarial-review catch); broker forwards sanitized UserId; seam unit-tested headers->openclaw.json->outbound | Ministry-side App-Insights shows per-user attribution under real KR8 values (config swap + verification once values arrive); client-stamped header accepted as trust-limited for pilot | UserId seam unit tests (openclaw-auth / broker suites, cited in the 09-03 tick) | in-tree + sandbox tenant sign-in only — never on Ministry infrastructure | Externally gated on KR8. moe.19 does not close it |
| KR8 / CLWX-31 (Ministry handoff values) | 2026-08-18 | Ministry (Raj) | All 20 handoff values are placeholders including hostnames; credential link expired unopened; app-server/Docker assumption conflicts with the desktop app | Condensed reply SENT 2026-09-01 (ledgered); 01b permissions-delta SENT 2026-09-02; the 07-20 owed deliverable CLEARED — ball genuinely with the Ministry for the first time since July | Raj returns real values + a session; probe of one real endpoint succeeds (per project memory: values are currently unprobeable, do not re-attempt) | — | n/a — external | Awaiting Raj (session + values). Gates KR7 -> KR6 fleet-verify and ATLAS-15 production auth. moe.19 irrelevant |
| CLWX-39 (Graph-B flag-gated sign-in, L4 run) | 2026-08-18 | Ministry conflict -> internal pull-forward | Graph grants are read-only and the in-app wiring didn't exist — the durable Outlook lane was unproven | In-app Graph lane LANDED 09-03: read-only scope baseline, Settings transport toggles, scope-aware compose refusal, Graph-403 structured refusal, graph:-id refusal on browser-only actions, attachment fidelity, stub force-parked; 3 review findings fixed same tick; suite 161 files green | L4 eval run green (read rows real data, draft/send graceful refusal) — one ~2-min operator sandbox sign-in is the only blocker | scripts/v2-eval-graph.ts (staged); scripts/graph-signin-smoke.ts (L1-L3 PASS) | L1-L3 real tenant 2026-09-02; in-app wiring in-tree only | One operator sign-in then the L4 run; card then feeds CLWX-40 L5. moe.19 not the gate |
| K10 | 2026-09-02 | Karunesh (external tester) | "Error when trying to read PDF Document" (NSCC-2026.pdf; log + screenshot) | CLWX-72 four fix layers (supportedArchitectures, DOMMatrix polyfill + loadDepDetailed, bundle hard-fail, verify-openclaw-bundle gate) — Ready, verified on the shipped moe.16 artifact (CLWX72_SHIPPED_VERIFY=PASS); Second distinct defect CLWX-92 (pdfjs workerSrc in the Electron UtilityProcess) fixed — Ready; K10 variant rows (scanned/password/large PDF) tagged in the CLWX-77 harness | Drag-PDF→summarise on a FRESH INSTALL joins the release smoke; scanned/protected/large PDF variants live in the CLWX-77 matrix (ledger criterion) | scripts/verify-openclaw-bundle.mjs (in package chain + ga:gate); scripts/harness-artifact.mjs kLedger:'K10' rows (pdf-scanned-notext, pdf-password, pdf-large); moe.18 VM matrix K10-PDF leg | moe.18 VM 2026-09-06 (K10-PDF PASS) + Karunesh's own moe.18 field report 2026-09-03 ("working fine now") — the strongest-closed row in the ledger | Small remainder: the literal in-app drag-GESTURE cell is still an open CLWX-77 trail item (the VM leg used the file-open mechanism), and the K10 variant rows have only run on the Mac harness lane (Windows-lane harness run pending VM start) |
| K11 | 2026-09-02 | Karunesh (external tester) | Email send chain: "trouble locating New mail" → "saved draft without a recipient" → timeouts | CLWX-70 (Ready): sweeper f3653589 ran live (14 automation drafts deleted), eval postcondition bfeb2cf1, exit-path invariant 9aabc8f6; CLWX-58 (Ready): recoverComposeState auto-recovery 9aabc8f6, live verifier 3512f02f wired as a permanent ga:gate T1 row; CLWX-74 (Ready): readable VLM-unavailable error c139ecc3 + deterministic recipient-well DOM tier before the VLM fallback (2026-09-05 tick); CLWX-59 SplitButton compose-pane fix | Draft/reply/send e2e on a mailbox SEEDED WITH LITTER (stale draft + confirm dialog + docked chip) AND on a box with NO AWS creds — both must pass or refuse readably (ledger criterion) | scripts/clwx58-recovery-check.ts + scripts/clwx58-negative-check.ts + scripts/clwx58-compose-recovery-check.ts (ga:gate T1 'compose auto-recovery' row); scripts/outlook-drafts-sweeper.ts; tests/unit/outlook-actions-safety.test.ts (84/84 incl. cold-grounder tier-order proof); tests/unit/outlook-dom-heuristics.test.ts | Mac live lane 2026-09-03/04 (seeded-litter recover+protect PASS live, twice); NEVER on a Windows build — moe.18 VM cloud path unverifiable (placeholder gateway) | Three open distances: (1) the no-AWS-creds live draft e2e (CLWX-74 leg 3) has never run — Chrome :18792 attach-wedged 4 consecutive ticks; (2) moe.18 VM K11 produced a NEW finding — on-device qwen hallucinated a read tool-call loop ~15× with no loop-breaker and never reached the readable refusal (tool-cascade class; trim-unhold 7add864b is an owner call); (3) Karunesh himself has never re-tested email successfully (blocked at K1 chrome-attach on moe.18); cloud K11 bar formally moved to the Mac live lane, not yet run there post-moe.18 |
| K12 | 2026-09-02 | Karunesh (external tester) | Red "Disconnected" badge while turns visibly worked (2 screenshots) | CLWX-75 fixed 2ce37055 (badge now derives from the same gateway status stream as the footer, not a renderer-side HEAD probe) + unit specs b24a1a6c; State Ready; ships in moe.16; CLWX-104 review made the K12 e2e row falsifiable (4bdc2dcd — the row had been vacuous) | UI-state assertion in the recorded use-case harness: header badge must agree with gateway/turn state through a full turn (ledger criterion) | tests/unit/gateway-store-health.test.ts; tests/unit/connection-status.test.tsx; tests/e2e/chat-task-visualizer.spec.ts (K12 row, de-vacuoused in CLWX-104) | moe.18 VM 2026-09-06 — K12 PASS live (badge truthful through gateway kill→restart: connected→Reconnecting pill+disabled composer→connected) | The specific criterion vehicle is unmet: the recorded use-case harness that should carry this assertion does not exist in valid form (CLWX-55/56 Backlog; CLWX-57 declared INVALID) — the badge behavior itself is fixed and live-verified, but its guard is unit/e2e + one manual VM leg, not the recorded harness |
| K13 | 2026-09-02 | Karunesh (external tester) | On-device turns dead: "ollama ... Connection error." ×6 + no failover | CLWX-78 (Ready): /connection error/i added to UNREACHABLE_PATTERNS + 90s watchdog (2b21d2a8); loadHistory silent-death failover hole closed (5020c39c); CLWX-104 (Ready): D0/D1/D2 display fixes — stale-banner guard, banner de-dup, channel-neutral copy, raw 'Connection error.' blanked from the expander (f37ec9a6 + 4bdc2dcd); CLWX-95/96 filed (degraded-turn orphan, post-degrade wedge — Backlog); CLWX-105 filed (in-line error chip — Todo) | Degrade matrix: cloud-down→on-device AND on-device-down→cloud, on Windows, with the OpenAI-SDK "Connection error." surface specifically (ledger criterion) | tests/unit/channel-degrade.test.ts + tests/unit/chat-channel-degrade.test.ts (11/11, incl. the exact real-surface strings); tests/unit/error-display.test.ts; tests/e2e/chat-task-visualizer.spec.ts (stale run-error banner spec, CLWX-104) | moe.18 VM 2026-09-06 — direction-1 core PASS (correct anonymised degrade prompt at ~23s); direction-2 PARTIAL FAIL (contradictory co-rendered run-error banner + raw "Connection error." behind Technical details) | The dir-2 residual is fixed only in-tree (CLWX-104's NEXT-BUILD gate: live re-verify on the next cut's VM matrix — never verified on any build); CLWX-95 orphaned-degraded-turn and CLWX-96 recovery-wedge remain open in Backlog; stale banner was observed surviving gateway restart on moe.18 (clears only on app relaunch), so his exact both-directions-on-Windows bar is not yet green on a shipped build |
| K14 | 2026-09-02 | Karunesh (external tester) | His 5-prompt matrix result: a, b, d, e Worked / c Failed (moe.15, 2026-09-02 ~22:54-23:07 AST) | Prompt c (PDF summarise) closed via the CLWX-72/92 chain (see K10); K14 prompts executed manually in the moe.18 VM verify (5/5 answered, 0 raw errors) — which exposed the substance gap: NSCC pack absent from the installed tree; CLWX-42 NSCC knowledge pack shipped in-tree (ae4779c5 + b547601b + ebe73bea): data file + principal.nscc_lookup + persona steering + legal-boundary guard; card Ready after all four review lanes | His exact 5 prompts become a named fixture (karunesh-matrix) run per RC alongside Raj's suite (ledger criterion) | scripts/nscc-qna-eval.ts + eval/fixtures/nscc-qna.json (tool lane 17/20 = 85%, floor 80; substance half); tests/unit/nscc-lookup.test.ts; moe.18 VM matrix K14 leg (manual) | moe.18 VM 2026-09-06 (5/5 form, content-shallow substance); NSCC fix is in-tree only — NEVER verified on a build | The named 'karunesh-matrix' fixture was never created — no file by that name exists in the repo (harness honest-mapping explicitly parks K9/K14 as 'in-app'), so the per-RC automation the criterion demands is absent; the NSCC substance fix carries an explicit NEXT-BUILD/RESTART gate (fresh-session no-file K14 question on the first build carrying ae4779c5..ebe73bea, plus the running gateway predates the tool) — until a new cut is verified, K14's substance bar is open |
| CLWX-22 (OKR anchor — GA declaration) | 2026-09-02 | owner (human-only close) | GA itself: survive Raj's 07-21 0/5 test provably, install unattended, work offline, scale without fleet outage | All KR machinery + pnpm ga:gate (CLWX-90) exist; static gate GREEN 7/0/2 on 2026-09-06 (docs/evidence/GA_GATE_2026-09-06.md); moe.18 full-matrix verify completed 09-06 — NOT all-green (K11 finding, K13 dir2 partial fail, NSCC absent) | Every GO/NO-GO box on docs/wiki/GA_READINESS.md §4 green incl. security floor; a HUMAN declares GA (agent ceiling is Ready) | pnpm ga:gate (scripts/ga-gate.mjs, package.json:101) | moe.18 VM 2026-09-06 (partial matrix pass) | moe.19 build + a clean full-matrix VM verify is the named #1 gate but NOT sufficient: security floor (CLWX-18/19), KR6/7/8 Ministry legs, and the external-tester email leg remain open |
| CLWX-25 / KR2 / BOOT-EMPTY (recorded assisted clean-VM install) | 2026-09-02 | owner (KR2) | Fresh/empty-config install left the composer disabled ~4-5 min on moe.11 AND moe.12; the supported assisted-screen flow was never proven recorded | Fix 61be816e shipped in moe.13; clobber fix 38085ba3 in moe.14; KR2 RECORDING EXECUTED 2026-09-03, frame-verified VALID: 1920x1080 26-min chain — uninstall -> fresh ASSISTED install ZERO manual steps -> gateway ready -> composer ENABLED 10.2s -> fresh default 'On this device' holding (sha256 1ee2e2b9) | KR2 (card): clean VM, assisted screens, 0 manual dependency steps, complete tree, both ports bind, gateway healthy — screen recording + exit 0 (met by the 09-03 recording) | skills/laptop/evidence/2026-09-03-kr2-recording (frame-verified) | moe.14-era VM recording 2026-09-03; moe.18 silent-upgrade + no-clobber legs re-PASSed 2026-09-06 | Evidence complete; card sits In Progress in the mirror — residue is the human close (and optionally re-recording on the GA tag). moe.19 adds nothing required |
| EXT-TESTER (register A) | 2026-09-02 | GA gate (external validation) / Karunesh | No independent tester has completed download->install->first-turn from the public Release (original card CLWX-11 was Cancelled 06-10) | moe.15 tester handoff SENT 09-03 (signed URL, byte-verified); Karunesh moe.18 field report 09-03: install + documents PASS ('working fine now'), EMAIL BLOCKED at chrome-attach (pre-CLWX-73-fix build); image+document evidence sent but unretrieved from the WA bridge store | One independent tester completes download->install->first-turn INCLUDING the email lane on a shipped build, evidence on file (GA_READINESS §4 external-validation checkbox) | — | Karunesh's real box on moe.18 (partial: docs yes, email no) | moe.19 (carrying e85f0ef7) + one Karunesh re-run plausibly CLOSES it — the email leg is the remaining distance; his unretrieved image+document may hold new findings |
| VM-CPU-STARVE (register B) | 2026-09-02 | internal (test infra) | On-device qwen2.5:3b turns do not complete on the e2-standard-4 VM — incomplete-turn threads; on-device evidence cannot be anchored there | Register note; V-batch and moe.18 legs designed around it (timeouts attributed to CPU allowance) | PROPOSED: all on-device turn evidence is anchored to the laptop lane or a GPU VM, stated per-artifact; no on-device verdict cites the e2 VM | — | n/a — standing lane constraint | Open constraint; moe.19 VM verify inherits it (on-device K11/K13 legs will stay noisy on the e2 VM). Laptop-lane anchoring closes it |
| STEER-READ (register B) | 2026-09-02 | internal (KR1 follow-up) | On read_docx parse failure the agent falls back to generic workspace-rooted read instead of surfacing the parse error — persona covers Python fallback but not generic-read fallback | Adjacent: CLWX-101 readDocx tiered refusals landed in-tree (legacy .doc/.rtf principal-language refusals) — narrows but does not pin the steering rule | PROPOSED: persona/plugin unit row asserting a failed document.read_* never triggers a generic read fallback on the same path; harness matrix row for the behavior | — | in-tree adjacent fixes only | Low, open. moe.19 ships the CLWX-101 wording but the steering assertion itself is unwritten |
| FIXTURE-HYG (register B) | 2026-09-02 | internal (demo prep) | Stale malformed fixture copies in Downloads/Documents shadow the valid Desktop copy (resolver prefers Downloads) | Process rule recorded: purge + re-seed via the fixed seeder before demos (seeder itself fixed: Assert-OpenXmlPackage + atomic replace) | PROPOSED: demo-prep checklist step (purge+reseed) encoded in the demo skill and executed before each recorded demo; seeder refuses to leave invalid packages | windows-pilot seeder validation (pilot-seed-demo-documents.ps1 Assert-OpenXmlPackage path) | process-level only | Open process row; moe.19 irrelevant |
| DRIVER-SETTLE (register C) | 2026-09-02 | internal (test tooling) | Chat-turn driver accepted placeholder text as a settled answer — false-positive evidence risk | Fixed @ecf31c4b (placeholder rejection + 9s stable window) | Register: one green Lane-2 run under the fixed driver doubles as the regression proof | — | in-tree; moe.17/moe.18 verify lanes ran driver-based turns with pixel evidence but no artifact explicitly closes this row | Almost certainly satisfied by the moe.17/18 driver runs but never explicitly closed — one line of bookkeeping citing a specific green run; moe.19 verify would also do it incidentally |
| CLWX-86 (tool<->host-API version skew) | 2026-09-02 | internal (moe.15 live finding) | Agent advertised browser.diagnose against an install lacking the route — raw 'No route for POST /api/browser/diagnose' leaked | Ready with evidence 09-05: capability handshake/route-presence check; fail-open on indeterminate probes; 404 handling fixed (only explicit capability-disabled keeps allowlist wording); index.mjs drift-guard + 18-row tier-1 positive control after the review's mutation MAJOR; 44/44 across three suites | On a shipped build, a tool whose route is absent parks with a readable capability message and never leaks a raw route error; tier-1 ships app-side with the same build | 44/44 across the three CLWX-86 suites (state vector tick 2026-09-05); CLWX-77 registration rows (probe fails open, listener-independent) | in-tree only — NEVER verified on a build (running app + seeded plugin carry old behavior) | moe.19 build+VM-verify CLOSES it (explicitly NEXT-BUILD-gated; tier 2 already exercised by current installs by design) |
| CLWX-64 (forms schema-drift detector) | 2026-09-02 | internal (forms migration already visible on the clone) | A silent Microsoft Forms schema drift would corrupt statutory submissions with all tests green | Ready @4ba98d76 (09-05): fingerprints stamped into both driver schema JSONs + verified at fill time BEFORE any fillField; readable refusals; strict only on definitive drift; re-stamp CLI; review MAJOR (dead CSS fallback on implicit ARIA) closed with 3-tier locator; 24-row suite + falsifiability (label edit -> 3 fail) | Fill against a drifted form refuses readably; fresh-form simulation passes on both real forms (proven in-tree); residual: one live-form run of the gate | tests/unit/forms-schema-fingerprint.test.ts (24 rows); scripts/forms-stamp-fingerprint.ts | in-tree + recorded-trace verification only; live-form run never executed (Chrome wedge) | moe.19 ships it, but the named residual is the Chrome-restart-gated live-form run, not the build; prefix-40 blind spot documented |
| OWNER-CORNER-CLWX-77 | 2026-09-03 | Anton (owner) | "Look around the corner" (CLWX-72 holistic-RCA tick): after moe.15 shipped with a dead pdf reader that every workspace-side test had passed, he asked for the systemic close of the test-the-workspace blind spot — an artifact-grade doc-type x command matrix against the PACKAGED runtime. | CLWX-77 filed 2026-09-03; 14 FIXED-UNGUARDED mined findings + Karunesh K-ledger criteria (K8 intermittence, K10 drag-PDF variants, K14 fixture) folded in as normative acceptance; pnpm harness:artifact slice 1 (19343f95 + review 8a57ff4b, 2026-09-05): stages shipped plugin + REAL gateway bundle outside the repo tree; negative control proven (hiding mammoth in the staged bundle flips the row to FAIL); pdf password/large/corrupt rows (7fc38563); registration-smoke leg — 31-tool inventory set-equality, outlook kill-switch row, no-hostapi/no-config degradation (91f1c588 + 38fee5c6); Electron-env spawn parity (@electronlike twins reproducing the CLWX-92/moe.16 failure env), staged-gateway transport rows, K-ledger rows (96da61f1 + 30a3d46f); all three Claude-lens MAJORs closed (67543cd1) incl. proving the electronlike fake actually applies; --fast (8 pinned rows, ~43s) wired into the package script after verify-openclaw-bundle — package:mac/win/build:win/release all inherit it (verified in package.json); Matrix now 45 rows, full run GREEN 30 PASS / 14 REFUSED-READABLY / 0 FAIL / 1 NO-TOOL (docs/evidence/HARNESS_ARTIFACT_2026-09-06.md) | Card acceptance (exists): every doc type x every command exercised against the PACKAGED runtime (not the workspace), refusals in principal language, first full run documented with gaps filed as cards, package preflight wired. | scripts/harness-artifact.mjs + scripts/harness-artifact-child.mjs (pnpm harness:artifact; 45-row matrix); harness-artifact --fast in the package script (every package/build/release path); unit guards 66/66 incl. the vitest drift guard parsing registerTool literals out of index.mjs (31/31); docs/evidence/HARNESS_ARTIFACT_2026-09-06.md (committed run report) | Dev Mac against the staged REAL bundle (build/openclaw), 2026-09-06 — artifact-grade by construction, but not yet a Windows packaged-node run | Two named legs open: the Windows-lane run (true packaged node.exe + real utility-env gateway — VM TERMINATED, owner spend call; NODE_OPTIONS backslash nuance recorded) and the in-app K10 drag-gesture cell; card is In Progress, not Ready, and no matrix row has ever run on an installed Windows build. |
| OWNER-GATE-CLWX-90 | 2026-09-03 | Anton (owner) | "Formulate a test that checks every single thing... this test gets run as the acceptance criteria." (verbatim on the CLWX-90 card) | scripts/ga-gate.mjs + pnpm ga:gate shipped (CLWX-90 -> Ready): T0 static / T1 live Mac lane / T2 VM pointer, every check mapped to a GA scorecard box, K-ledger criterion, or fixed-bug guard; report to docs/evidence/, loud SKIPs naming their unlock; Run 1 RED (7/2/2) proved the gate works — caught 4 pre-existing lint errors + 2 eval-harness brittleness defects (W3.1, W4.1), all fixed same tick; Run 2 GREEN 9/0/2 (docs/evidence/GA_GATE_2026-09-03.md); Full T0+T1 live run GREEN 2026-09-04 (e2f084d5, docs/evidence/GA_GATE_2026-09-04.md); Wiring: preflight += lint:check; GA_READINESS §4 carries the mechanism + GREEN-<=24h-tag rule; persona doc rule: card acceptance that the gate can't see doesn't exist at release time; Opt-in tiers grown since: GA_GATE_E2E (CLWX-91), pwsh lint + model-pin doctor rows (CLWX-83 — the strict pin default caught live drift twice, incl. gpt-6-astra on 2026-09-06) | Card acceptance (exists, met for Ready): one full T0+T1 run GREEN with committed report; wired into the release path; every scorecard box names its gate check id; coverage-growth rule in force. Standing bar: a GA tag requires a FULL GREEN run <=24h old. | scripts/ga-gate.mjs (pnpm ga:gate); docs/evidence/GA_GATE_2026-09-03.md / GA_GATE_2026-09-04.md / GA_GATE_2026-09-05.md / GA_GATE_2026-09-06.md (committed run reports) | Mac live lane full T0+T1 GREEN 2026-09-04; latest run 2026-09-06 is STATIC-ONLY GREEN 7/0/2 (live-lane row = SKIP GA_GATE_STATIC=1, verified in the report) | The GA-tag bar (full GREEN <=24h) is currently unattainable: the T1 live lane has been blocked since 2026-09-05 by the Chrome :18792 attach wedge whose only fix is the owner restarting the signed-in Chrome; send-proof and NSCC remain opt-in flags; the e2e tier is red at baseline (13 fork-drift specs, CLWX-102); owner/Ministry boxes are by design outside the gate. |
| OWNER-MONDAY-BUILD | 2026-09-03 | Anton (owner; deadline signal relayed from Raj via the Karunesh field-report thread) | "Raj wants a demo version for Monday" (2026-09-03 evening field report) — standing owner Monday-build ask thereafter (state vector: "corroborates the Monday-build owner ask — and this dev Mac needs the new install"); Monday = 2026-09-08. | Fix train staged in-tree for the build: CLWX-99 cron tz 6b446d13 (state vector: "the Monday demo build must include 6b446d13 for the reminder to fire on the principal's clock"), CLWX-100 566a7504, CLWX-80 148c4e53, CLWX-104 run-error dedup f37ec9a6+4bdc2dcd, CLWX-71 send-gate subject fix 1e3a9170, CLWX-42 NSCC pack ae4779c5+b547601b+ebe73bea; CLWX-85 hash-manifest gate (a5db3b1f + 5dd5f26f) built explicitly so "the demo build must verifiably carry 6b446d13+566a7504+148c4e53" — auto-runs after every electron-builder invocation; CLWX-43 Mac-lane run (b159d13c) proved the urgency: the dev Mac's installed app is still 0.4.3-moe.10 and reproduces the long-fixed CLWX-72/92 silent gateway kill; Version bumped 0.4.3-moe.19 (d0d9bd9a, 2026-09-06 21:38+04): "first cut carrying the post-moe.18 fix train" — package.json only | PROPOSED: a moe.19+ installer is actually built through the package chain (inheriting harness --fast + verify-openclaw-bundle), its hash-manifest published (pnpm release:manifest:publish), installed on the demo machines, and the NEXT-BUILD gates re-run green (scripts/clwx99-tz-check.ts regression leg, scripts/clwx67-reminder-e2e.ts UI assertions, fresh-session NSCC in-app turn, CLWX-104 D0/D1/D2 rows) before Monday 2026-09-08. | scripts/release-hash-manifest.mjs (verify/publish; hard-stop on published-rebuild bit drift); harness-artifact --fast in the package script (pre-ship packaged-runtime gate); scripts/clwx99-tz-check.ts (leg A flips from defect-documentation to regression proof on the new build); scripts/clwx67-reminder-e2e.ts (next-build UI assertions) | in-tree only — NEVER verified on a build: moe.19 is a version-bump commit at HEAD; no build artifact, no manifest publish, no install visible at analysis time (2026-09-06 evening) | The build itself is not cut: HEAD stops at the moe.19 bump; owner must run the package + release:manifest:publish; every currently-running install predates the entire fix train (dev Mac proven on moe.10); all NEXT-BUILD gates (cron tz, reminder UI, NSCC fresh-session, run-error dedup, send-gate subject) are unexercised on any artifact — with ~2 days to the Monday deadline. |
| CLWX-48 (UserId coherence rule TB-4) | 2026-09-03 | internal (RESILIENCE-PACK flow 5) | KR7 UserId stamp is best-effort/warn-only — a failed re-stamp silently drops per-principal attribution | Card filed (Backlog); no rule written | Card: config-coherence-auditor rule — IF clawx-microsoft-graph.json has an account THEN moe-cloud-gateway provider carries headers.UserId == account oid; drift flagged like the four-store rules | — | never — nothing built | Unbuilt; moe.19 does not close it. Low urgency until KR7 goes live on real identity |
| CLWX-49 (loud frame-capture failure TB-5) | 2026-09-03 | internal (evidence integrity) | Windows chat-turn driver can silently produce zero-frame evidence (the class behind the invalid 09-03 usecase video) | Card filed (Backlog); the loud {ok,detail} contract exists in scripts/forms-submit-recorded.ts as the pattern | Card: frameCaptureFailed counter + non-zero exit on zero-frame runs in pilot-chat-turn-driver.js | — | never — nothing built | Unbuilt; moe.19 does not close it; blocks trust in any future recorded evidence until Recorder v2/this lands |
| CLWX-50 / ATLAS-DOCGAP (Atlas §16-18) | 2026-09-03 | internal (future debugging front door) | Hidden-launch Gateway trap, extension path move, firewall silent-drop are known but unwritten Atlas sections — behaviors only codified as flight checks | Flight checks IF-4/PF-3 codified in the state vector; doc never written | Card: the three Atlas sections written with symptom -> root cause -> fix -> detection-agent (matching §1-§15 format) | — | n/a — doc-only | Doc backlog; moe.19 irrelevant |
| CLWX-54 (stale Playwright handle attach failure) | 2026-09-03 | internal | Outlook browser attach fails with a stale Playwright handle despite live CDP + a live Outlook tab (card is a TODO-stub: no steps/evidence filled in) | None; plausibly the same wedge later seen 09-04/05 as the :18792 ping-up/attach-down condition (three independent confirmations) — unconfirmed link | PROPOSED: reproduce or fold into the Chrome-wedge class: after a renderer crash, the driver re-resolves the browser target instead of reusing the stale handle, proven by a kill-tab-then-attach test | — | never — no repro captured | Card needs triage: fill the stub or close as duplicate of the CDP-wedge condition once the owner restarts Chrome. moe.19 irrelevant |
| CLWX-55 (use-case demo reel UC1-UC10) | 2026-09-03 | owner (Raj wants a demo version) | No trustworthy recorded proof of the agent doing every key principal function as a real user experience | Design doc docs/VIDEO_CAPTURE_OKR_2026-09-03.md; partial raw material exists (W3 recorded suspensions submit, Daily Report recorded run, KR2 recording); Gated on Recorder v2 (CLWX-56) | Card: each of UC1-UC10 recorded meeting all 7 clip criteria (app window only, Connected, typed+sent, visible work, completed correct artifact, continuous take), frame-by-frame verified | — | never — 0/10 clips accepted (the 09-03 attempt was declared INVALID) | Needs Recorder v2 + Chrome restart (UC1-2) + VM lane. moe.19 build helps only by being the build recorded; the reel work itself is unstarted |
| CLWX-56 + CLWX-57 (Recorder v2; invalid 09-03 video) | 2026-09-03 | internal (evidence integrity) | The 09-03 Windows use-case video recorded the wrong window at 640x480, clipped, showed Disconnected, never sent the prompt — and was declared good from driver JSON instead of pixels | CLWX-57 filed as the INVALID finding (coverage claim downgraded); CLWX-56 specs the fix: gdigrab by window title at true size, display >=1920x1080, mandatory in-harness frame-verification gate (>=8 frames + first/last, FAIL unless AC1-AC7 met) | Card: only a frame-gate-passing run writes RESULT.md and registers a clip as proof | — | never — Recorder v2 not built (the KR2 recording used the pattern manually and passed frame verification) | Unbuilt harness gate; moe.19 irrelevant. CLWX-57 itself is a record-keeping card a human can close/cancel |
| CLWX-60 (duplicate of CLWX-59) | 2026-09-03 | internal | waitForComposePane latched a hidden Fluent SplitButton wrapper — filed twice due to the PLANE_PROJECT env bleed | Fix applied + live-verified on CLWX-59 (visible-only guards, role+aria first, 2-gate send re-proven live); Card self-identifies as filed in error; the env-bleed root cause fixed via CLWX-103 (scripts/plane-comment-post.mjs) | Human cancels the duplicate card (agent ceiling prevents it) | live 2-gate re-run PASS recorded on the card (scripts/v2-send-test.ts) | Mac live lane (CLWX-59 fix verified); this card is administrative | Administrative only — needs a human cancel. moe.19 irrelevant |
| CLWX-61 (Outlook eval gaps: forward + attachment) | 2026-09-03 | internal (finish-sprint deficit vs email objective) | Forward e2e, live download_attachment, and attachment-metadata assertion rows are missing from the Outlook eval — capability #1 claims live coverage it does not have | None landed; the live half is blocked on the Chrome :18792 wedge (ping-up/attach-down, three independent confirmations 09-04/05; lsof shows no foreign client — wedge inside Chrome's DevTools session) | Card: eval rows for forward (with body/recipient checks) + a live hard-confirm download_attachment + attachment metadata assertion, green on the live lane | scripts/v2-eval.ts (existing 15 rows — the new rows do not exist yet) | never — blocked before first run | Owner must restart the signed-in Chrome on :18792 (closes principal's open tabs), then the rows must be written+run. moe.19 not the gate |
| CLWX-63 (document-to-form extraction chain e2e) | 2026-09-03 | internal (demo item 2 chain) | The suspension-letter -> 32 extracted fields -> prefilled form chain has never run end-to-end as one flow | Probe-only leg ran 09-05 (proved NOT attachable — evidence for the wedge, not the chain); Byproduct: the agent-find that became CLWX-98 (infraction alias flips, FIXED in-tree with the 140-option roundtrip test) | Card: one live run — real letter in, extraction, prefill on the test.fac clone, gate refusal, screenshots — with field-level accuracy asserted | tests/unit/moe-suspensions-option-roundtrip.test.ts (alias correctness only, not the chain) | never end-to-end; components proven separately (extraction schema, fill 29/32 live) | Owner Chrome restart, then the chain run. moe.19 not the gate; NOTE the CLWX-98 alias fix DOES ride moe.19 and its statutory-correctness proof is currently unit-level only |
| CLWX-68 (leave/attendance registers + inventory, post-GA) | 2026-09-03 | Raj (product capabilities #4/#7) | register_update/register_query and school_inventory are named product capabilities with zero board presence | Carded for visibility 09-03; parked by PM scope call (post-GA, do not pull into finish sprint) | Card: design first (SQLite vs Ministry SharePoint List — ask Raj), then schema + tools + e2e | — | never — deliberately unstarted | Parked by design; moe.19 irrelevant; not a GA blocker |
| CLWX-95 (degraded turn orphans at prompt.submitted) | 2026-09-03 | internal (moe.17 addendum) -> principal trust | After a mid-turn cloud->on-device switch the interrupted turn never completes — renderer spins 'still-thinking' 420s, answerText=null | Seam landed @2b21d2a8: syncDefaultProviderToRuntime skipGatewayRefresh param (default off, NOT wired); CLWX-94 run-ownership token reduces replay surface; REPRODUCED LIVE on moe.18 (09-06): send RACED the gateway reload on switch-during-outage — NO_RESPONSE, no notice; scoped to the restart-vs-per-run refresh seam (clean outage without switch does NOT orphan) | Card: after a mid-turn degrade, the turn is resumed on the new channel exactly once OR terminated with a visible anonymised outcome — never an indefinite spinner (stricter bar explicitly owner-arbitrated) | — | moe.18 VM 2026-09-06 — verified STILL FAILING (live confirmation the card waited on) | Fix not written (wiring through settings.ts + channel-router.ts + the restart-vs-per-run decision needs an owner/live probe). moe.19 does NOT close it |
| CLWX-96 (post-degrade recovery wedge) | 2026-09-03 | internal (moe.17 addendum) -> principal trust (the 3:45pm dropped-connection scenario) | After connectivity returns, Online turns silently fail (NO_RESPONSE, stale offline banner) until a full app relaunch | moe.18 verify (09-06) re-observed the class: post-restore auto-recovery ABSENT (runtime stuck on qwen while UI said Online; repaired by manual reassert); stale run-error banner persisted across new chats AND a gateway restart; Display-layer sibling CLWX-104 fixed the banner staleness in-tree; the silent-send/auto-recovery half remains on the CLWX-95 seam | Card: next Online turn after connectivity returns succeeds without app restart; banner clears on provider-probe success; silent send treated as its own defect class with a watchdog | — | moe.18 VM 2026-09-06 — still failing (auto-recovery half) | moe.19 closes only the stale-banner display half (via CLWX-104); the recovery/silent-send half needs the CLWX-95 wiring — not written |
| CLWX-71 (MCP adapter) | 2026-09-03 | owner (interoperability direction) | No MCP surface exposes the gated forms/outlook tools to external MCP clients without bypassing the two-gate/kill-switch server-side safety | Adapter BUILT + live-proven 09-06 (@0d8b0fbb...): stdio server, 9 tools proxied verbatim, gates stay server-side, env-only token, no-confirm send REFUSED end-to-end; Its review lane found the PRODUCTION two-gate hole (see SEND-GATE-SUBJECT-SKIP row) | Card remaining for Ready: sandbox positive legs + a Claude-Code-client leg + full review lenses | scripts/clawx-mcp-server.mjs live handshake proof (docs/evidence/CODEX_ADVERSARIAL_REVIEW_2026-09-06_CLWX-71.md) | Mac live lane 2026-09-06 (against the running moe.10-era Mac install — forms routes 404 recorded honestly) | Positive legs are Chrome/build/operator-gated; moe.19 on the Mac would let the forms-route legs run. Not GA-critical |
| CLWX-77 (artifact-grade doc matrix vs PACKAGED runtime) | 2026-09-03 | owner ('check every single one') + Karunesh K8/K9 lineage (06-30, 07-21) | Doc-type x command coverage was claim-based, not artifact-based, against the packaged runtime — the class that let K8/K9 ship | Harness matrix grown 16 -> 22 rows, full run GREEN 09-06 (14 PASS / 7 REFUSED-READABLY / 0 FAIL / 1 NO-TOOL; docs/evidence/HARNESS_ARTIFACT_2026-09-06.md); Registration-smoke leg (31-tool inventory, kill-switch row, no-hostapi degradation), @electronlike UtilityProcess fake verified non-vacuous, readPdf principal-language refusals (password/corrupt/oversize); 4-lane reviews folded; unit guards 39/39 | Card trail: packaged-node parity leg, gateway-transport leg, K-ledger rows, and the Windows-lane run of the same matrix — then Ready | harness matrix 22 rows (docs/evidence/HARNESS_ARTIFACT_2026-09-06.md); tests/unit registration/fast-lane drift guards (39/39, commits 91f1c588+38fee5c6) | Mac staged bundle 2026-09-06; Windows leg NEVER run (VM start owner-gated) | moe.19 + the Windows-lane matrix run closes most of the trail (readPdf wording is NEXT-BUILD and rides moe.19); gateway-transport + packaged-node parity legs still need building |
| CLWX-99 / CRON-TZ (reminders fire at wrong wall-clock) | 2026-09-05 | principal-facing (3:45pm statutory deadline) — found by the CLWX-67 e2e | A local-time cron expr produced nextRun a full YEAR out because the gateway parsed it in its boot-time tz; the seeded 3:30pm Daily Report reminder inherits this; agent defer writes stored naive ISO as UTC (4h late observed) | FIXED in-tree @6b446d13: cron-tz.ts systemTimeZone() fresh per call; POST/PUT/seed pin schedule.tz; seed repairs already-seeded installs; persona rule for explicit UTC offsets on agent at-writes; 10/10 units with falsifiability; live leg A documents the defect on the pre-fix build (22:29 vs 23:59 local) as the next-RC regression proof | Register: leg-A re-run on the next RC shows nextRun at the correct local wall-clock; seeded reminder fires at 3:30pm local on a VM in a non-gateway-boot tz | tests/unit/cron-tz.test.ts (10/10); evidence/CLWX99_2026-09-05.md leg A (pre-fix defect documentation = the regression baseline) | in-tree only — running app carries the defect until build+restart | moe.19 build + the leg-A re-run CLOSES it |
| CLWX-100 (cron-session cosmetics) | 2026-09-05 | principal trust (low) | User bubble leaks [cron:<uuid>]+instructions+tz header; think-block render; composer model-id anonymisation unconfirmed on RC | Fixed in-tree (card Ready); NEXT-BUILD class per state vector | PROPOSED: on the next shipped build, a fired reminder renders as a clean principal-language chat prompt (no cron uuid/header), and the composer shows only Online/On this device | — | in-tree only — NEVER verified on a build | moe.19 build + one fired-reminder look CLOSES it |
| CLWX-101 (readDocx surfaces jszip internals) | 2026-09-05 | principal trust | Legacy .doc / .rtf files produced raw jszip internals instead of a readable refusal | Fixed in-tree (card Ready): tiered principal-language refusals; pattern extended to readPdf in the CLWX-77 slice | PROPOSED: harness matrix rows for .doc/.rtf return REFUSED-READABLY on the packaged runtime of a shipped build (rows exist in the 22-row matrix) | harness matrix REFUSED-READABLY rows (docs/evidence/HARNESS_ARTIFACT_2026-09-06.md) | Mac staged bundle 2026-09-06; never on an installed build | moe.19 build + the matrix (or one in-app legacy-doc turn) CLOSES it |
| RAJ3-FROM-NOOP (register B) | 2026-09-05 | internal (eval integrity) | raj3-reply-archive-check matches on a from field the InboxMessage contract does not declare — both sides degrade to ''==='' so the matcher is silently subject-only (weaker than claimed) | Script excluded in tsconfig.scripts.json with reason (found by the scripts-typecheck census) | Register: match on a contract-declared field, delete the exclude line, re-run the RAJ-3 refutation once | scripts/raj3-reply-archive-check.ts (the artifact under repair) | n/a — the RAJ-3 refutation itself stands but on a weaker matcher than documented | Low; one small fix + re-run. moe.19 irrelevant. Until fixed, the RAJ-3 'REFUTED' evidence is subject-match-only — cite it accordingly |
| OWNER-FLAG-RETENTION | 2026-09-06 | Anton (owner) | Gate flag: the CLWX-42 content-retention guard's 0.9 -> 0.85 loosening left 4.7 points of slack (measured retention 0.897) — a lossy splitter could drop real Code-of-Conduct content and still pass. | Commit 995ae457 (test(nscc), "owner gate flag" in the message): floor tightened to 0.89 AND replaced-in-spirit with a structural drop-only-noise assertion — every non-noise source line must survive into some passage; a dropped body line fails BY NAME (verified in tests/unit/nscc-lookup.test.ts lines 198-206); Falsifiability demonstrated: a lossy splitter dropping 111 'suspension' body lines lands at ratio exactly 0.850 — the old floor passed it silently; BOTH new guards catch it; guards 46/46; Synced to board + state vector (3eaa17b5) | The guard fails on any dropped non-noise source line (by name) and on aggregate retention <0.89 — met and falsifiability-proven with the lossy-splitter probe. | tests/unit/nscc-lookup.test.ts (0.89 aggregate floor + 'drops ONLY known noise classes' structural assertion) | Unit lane, dev Mac 2026-09-06 (the guard IS the deliverable — test-infra, so the unit lane with a demonstrated falsifiability probe is the terminal bar) | — |
| OWNER-FLAG-KR5 | 2026-09-06 | Anton (owner) | Owner-directed "KR5 ahead of any more cards" — the GA_READINESS scorecard was stale on KR5: the G10 row still said per-user caps/outbox "designed, not built" 4 days after the outbox had landed and CLWX-28 had gone Ready. | Commit 7d9fca04 (docs(ga), owner-directed): G-outbox tests re-run 9/9; wiring re-verified in SOURCE (audit-first writes in electron/api/routes/{outlook,forms}.ts, boot drain electron/main/index.ts:603 — landed ebc4be75); GA_READINESS G10 row corrected to CLOSED with the re-run evidence; Scorecard reconciled against the board: KR5 checked; KR1 (moe.12 + installed-moe.17 in-app proofs), Ext-val A (CLWX-34 Ready) and GA-packet (CLWX-10 Ready) checked with inline evidence; Ext-val B annotated honestly as an owner acceptance call (Karunesh moe.15 run misses the 'public Release / no help' qualifiers); tag-time hygiene box annotated cannot-pre-check; Net: Product KRs 2/8 -> 4/8; every remaining unchecked box is an owner/Ministry call or at-the-tag | KR5 box (exists): offline action persists to an outbox, survives kill -9 + restart, flushes idempotently on reconnect — with the named evidence recorded in GA_READINESS §4, and the scorecard never lagging Ready-card evidence by days. | tests/unit/outbox.test.ts (9/9: durable incl. the stubbed-persistence negative control required by OFFLINE_ARCHITECTURE §5.4, idempotent replay = one send, drain oldest-first + bounded retry + backoff + concurrent-drain-no-double-send) | Unit lane + source-wiring reads on the dev Mac 2026-09-06; the wiring commit ebc4be75 ships in moe.16+ trees | The staleness complaint itself is fixed (G10/KR5 reconciled same day). Honest residual on the KR5 claim: verification rests on unit 9/9 + source reads + the CLWX-28 Ready-card's 2026-09-02 restart-test comment — a kill-9 + reconnect drain has not been demonstrated on an INSTALLED build's live lane in any evidence I could re-inspect; and nothing structural prevents scorecard/board drift recurring (the reconcile was manual, no gate row diffs GA_READINESS against Ready-card evidence). |
| OWNER-FLAG-STALE-PILE | 2026-09-06 | Anton (owner) | Owner flag that the In Progress pile was stale — directive recorded as "last real work was Sept 3" (ninth tick). | All 11 In Progress cards classified (b159d13c tick + sync 0c741e73): CLWX-87/77 active this session; CLWX-61/63 Chrome-wedge-gated (Sept-5 authored); CLWX-39/30/31/29/25 owner- or Ministry-gated — gates re-verified without comment spam, none cleared; CLWX-22 = anchor; The ONE agent-executable stale card (CLWX-43, last real work Sept 2) advanced same tick: scripts/clwx43-latency-mac-lane.ts fired 3 real in-app turns — 8.8s routine (within the proposed p50<=15s) / 17.1s draft+write vs ~103s VM baseline; the read_pdf silent death root-caused live as the moe.10 pre-fix install (CLWX-72/92 class), corroborating the Monday-build ask; Evidence: docs/evidence/CLWX43_LATENCY_MAC_LANE_2026-09-06.md | PROPOSED: no In Progress card idles >72h without either real progress or a same-tick classification comment naming its re-verified gate + gate-owner; any agent-executable card in the pile is advanced before new cards are opened. | — | Dev Mac live lane + board mirror, 2026-09-06 (the CLWX-43 measurement ran on the installed — moe.10 — app; the classification is board-level) | The pile is classified, not cleared: CLWX-61/63 still blocked on the owner's Chrome :18792 restart (three independent wedge confirmations), CLWX-29/30/31/25/39 on owner/Ministry action; the anti-staleness discipline is a tick convention with no machine check pinning it, so recurrence is possible the moment driver ticks stop. |
| CLWX-102 (e2e baseline debt, 13 red specs) | 2026-09-06 | internal (gate integrity) | 32 pass / 13 fail / 2 skip at e2e baseline — red classes are fork-decision drift (deleted locales, anonymised provider labels, channels copy), so the GA_GATE_E2E=1 opt-in tier cannot flip formally green | CLWX-91 root-caused+fixed the mocked-IPC blank-window class and wired the e2e tier into ga:gate; the 13 remaining reds filed as this card with per-class dispositions proposed | Card: each red spec dispositioned (delete/fork-skip-with-reason vs fix expectation vs real defect); full renderer e2e green so GA_GATE_E2E=1 is formally green | pnpm test:e2e (the failing baseline itself) | Mac dev lane 2026-09-06 (moe.18-era tree) | ~13 ten-minute dispositions; moe.19 irrelevant (dev-lane). Until done the e2e gate tier is informational only |
| SEND-GATE-SUBJECT-SKIP (register C, 2026-09-06) | 2026-09-06 | owner hard rule (two-gate send) — found by the CLWX-71 Codex review lane | A bare {confirm:true} sent the open draft with ZERO second-gate subject check — the current-reviewed branch dropped args.subject and validate didn't require one (in-app reachable) | FIXED @1e3a9170 (3 layers: required subject assertion + snapshot match on the current-reviewed path + click-time DOM subject filter); legacy tests encoding the vulnerable contract updated; safety suite 87/87 | Register: NEXT-BUILD gate — installed apps carry the old gate until a build ships; a live 2-gate proof (subject-mismatch refusal + matched confirmed send) on the shipped build | outlook safety suite 87/87 (incl. the new required-subject rows); scripts/v2-send-test.ts (live gate proof pattern) | in-tree only — NEVER verified on a build; every installed app (moe.18 and earlier) still carries the bypassable gate | moe.19 build + one live gate proof CLOSES it. Highest-severity NEXT-BUILD item: until the cut ships, the flagship two-gate safety claim is false on installed apps |
| CLWX-104 (run-error de-dup D0/D1/D2) | 2026-09-06 | moe.18 VM verify (K13 dir2 PARTIAL FAIL) -> principal trust | Stale red run-error banner persisted across new chats and a gateway restart; a co-rendered banner contradicted the correct amber degrade prompt with wrong-direction copy; 'Technical details' expander surfaced raw 'Connection error.' | Fixed at display layer @f37ec9a6+4bdc2dcd (Ready): D0 active-own-send-only banner seeding, D1 suppression only while the amber notice explains a failure, D2 raw-string blanking + channel-neutral copy; 3-lane review: 2 Codex HIGHs (attachment-only silent death, failed-resend hidden) fixed same tick; e2e re-pinned and run live (1 passed); suite 1535/6-skip | State vector: live re-verify on the next cut's VM matrix — K13 dir2 re-run must show the amber prompt with NO contradicting banner and NO raw 'Connection error.' anywhere incl. the expander | unit + e2e rows per docs/evidence/CLWX-104_RUN_ERROR_DEDUP_2026-09-06.md (mutation-probed: reverted seed-gate fails exactly 4 guard rows) | in-tree only — NEVER verified on a build (moe.18 predates the fix and exhibits the defect) | moe.19 build + K13 dir2 VM re-run CLOSES it |
| CLWX-105 (in-line error chip for error-stopped messages) | 2026-09-06 | internal (CLWX-104 review lanes, both models independently) | An error-stopped assistant message with empty content renders NOTHING — now that D0 removed the stale banner, a historical failure has no surface at all on session re-open | Card filed (Todo); errorDisplay mapping exists to build on; active-turn failures remain surfaced (pinned) | Card: in-line principal-language error chip on error-stopped assistant messages (anonymised, raw string only behind the collapsed expander rules), covering re-open and adopted console/cron turns | — | never — nothing built | Unbuilt; moe.19 does NOT close it. Named residuals also open: degrade notice is store-global (not session-scoped); neutral copy names both channels without availability knowledge |

(90 rows. Note: GPT-6's adversary pass — section 2 — found the CLWX-105 "nothing built" claim stale: tests/unit/chat-message-error-chip.test.tsx exists per its read; treat that row's changes/gap as superseded pending re-check.)

## 2. GPT-6 adversary findings (gpt-6-astra via codex exec)

Verdict note: GPT-6 (codex exec, read-only sandbox) overall verdict: "The map is not reliable enough to authorize closure." It reports reviewing all 86 gap rows against the repository, verifying the actual moe.19 installer hash (3a533a0f... — contradicting the map's 'no build artifact' claim, which is now stale), and REPRODUCING one false-green: scripts/ga-gate.mjs returns GREEN/exit 0 when required live legs are skipped. Its headline closure boundary: the scheduled moe.19 VM verify can close at most 5 rows (CLWX-42, CLWX-99, CLWX-100, CLWX-104, CLWX-105) and only via interactive legs with attested evidence; the other 81 cannot close from that verify. Execution caveats: (1) the first codex run with the FULL 90-row map hung for 31 minutes with zero CPU/no session file (blocked before starting work, likely on stdin/startup) and was killed; the successful pass ran per the retry instruction on the 86-row excerpt (rows with non-empty gap only — the 4 gap-empty rows MODEL-PIN-STALE, PWSH-LINT-GAP, VITEST-OOM, OWNER-FLAG-RETENTION were not adversarially reviewed) plus the first 12 notCovered items. (2) The reviewer ran read-only: it inspected ga-gate logic in-memory but did not execute builds, live lanes, or the VM script, so file/line claims were spot-checkable but live-lane health was not exercised. (3) Repo-path citations (ga-gate.mjs:82, harness-artifact.mjs:84/740/1101, vm-verify-moe19.sh:86/158, nscc-qna-eval.ts:271, pilot-asr-wer.ps1:31, release-manifests/0.4.3-moe.19.json, PRODUCT_PRINCIPAL_ASSISTANT.md:43, LIAISON_LOG.md:25, KARUNESH_GUIDED_TEST_2026-09-03.md:128) come from GPT-6's own reads and were not independently re-verified by this wrapper. Token usage: ~211K.

### F1 [HIGH] Acceptance gate can pass GREEN when its required live lane never runs
GPT-6 reproduced a false-green with an in-memory negative control: with static checks simulated as passing and Chrome unavailable, scripts/ga-gate.mjs returned GREEN exit 0 under the default (non-static) invocation. The gate header promises that an entirely skipped required tier fails, but the implementation counts only FAIL rows; renderer E2E, send-proof, and NSCC remain opt-in. package.json preflight/build:win/release do not enforce the documented full-GREEN-within-24h rule. Evidence: scripts/ga-gate.mjs:82, package.json:80.
- Affected rows: OWNER-GATE-CLWX-90; K5; CLWX-22; CLWX-102; RAJ-1 (G4 / CLWX-34): send-gate failure; RAJ-NSCC (CLWX-42): Code of Conduct in memory + his Q&A eval
- Proposed acceptance: With Chrome unavailable, the release-acceptance command exits nonzero. Release eligibility requires every mandatory check to pass against the candidate's recorded identity within 24 hours; a skipped check cannot satisfy its associated row.

### F2 [HIGH] moe.19 does not uniquely identify the reviewed artifact; several map rows are stale
The moe.19 installer EXISTS (verified SHA256 3a533a0fa1733fac6243fba19e4f1bc21d70c57eb498e1121a969b025bc48888) — the map's 'no build artifact' claim is stale — and docs/release-manifests/0.4.3-moe.19.json also records an earlier superseded SAME-VERSION build. CLWX-105 is implemented and unit-pinned (tests/unit/chat-message-error-chip.test.tsx) contradicting 'nothing built'; CLWX-61/63 test surfaces were authored Sept 5; the two CLWX-77 rows disagree on matrix size (22 vs 45 rows) and on whether transport/parity legs exist. scripts/vm-verify-moe19.sh checks only the running executable's version string, and its GCS upload decision checks only filename existence — neither distinguishes the two same-version builds.
- Affected rows: OWNER-MONDAY-BUILD; CLWX-105 (in-line error chip for error-stopped messages); CLWX-61 (Outlook eval gaps: forward + attachment); CLWX-63 (document-to-form extraction chain e2e); CLWX-77 (artifact-grade doc matrix vs PACKAGED runtime); OWNER-CORNER-CLWX-77
- Proposed acceptance: Every closure records source revision, installer SHA, installed app.asar SHA, test revision, and execution timestamp. Local, distributed, and installed artifacts must match the selected manifest; evidence citing the superseded same-version hash fails validation.

### F3 [HIGH] Packaging-regression class still lacks a guard against testing the wrong files
Pattern: workspace passes -> shipped parser fails -> packaging fix -> Electron-context failure -> another fix (K8/K10/CLWX-72/92/97 chain). The proposed Windows harness command uses --node-bin, but scripts/harness-artifact.mjs still stages the CHECKOUT's plugin and build/openclaw (staging roots at line 84) — changing the executable does not make those files the installed bundle. Additionally --fast reduces K8 repetitions to one (line 1101) and excludes the legacy .doc/.rtf refusal rows (fast selection line 740), so the package-chain fast lane does not exercise the intermittence criterion or the CLWX-101 rows.
- Affected rows: K8; K10; KAR-PDF / G6a (register B); OWNER-CORNER-CLWX-77; CLWX-77 (artifact-grade doc matrix vs PACKAGED runtime); CLWX-101 (readDocx surfaces jszip internals)
- Proposed acceptance: Run the full matrix against resources extracted from the exact installer, with repository dependency resolution unavailable. Repeat read/write ops 3x through chat, Host API, and cold Gateway contexts. Removing a parser from an isolated copy of those packaged resources must fail the relevant row. Include actual drag-and-drop and both legacy-format refusals.

### F4 [HIGH] RAJ-2 fidelity criterion can pass the exact meal-preferences-as-shirt-sizes defect
The 4-of-8 frequent-word coverage plus 'no invented entities' heuristic does not establish correct meaning: swapping two people's preferences, negating a request, or introducing lowercase 'shirt sizes' can all evade it. scripts/raj2-reply-fidelity-check.ts also generates reply text directly through a Bedrock client rather than exercising an installed app's reply workflow, so its claim to reproduce 'exactly' the reported defect class is unsupported, and it is wired into no recurring gate.
- Affected rows: RAJ-2 (G4 / CLWX-34): reply misinterprets content; K6; K7
- Proposed acceptance: Seed separate meal-preference and shirt-size messages with overlapping names. An installed-app summary and draft must preserve every person->preference association, quantity, negation, and requested action. Swapping either message body or any association must fail the test. Run it in the recurring gate.

### F5 [HIGH] RAJ-3 refutation can report 'not reproduced' without ever successfully replying
Beyond the acknowledged nonexistent 'from' field (RAJ3-FROM-NOOP), scripts/raj3-reply-archive-check.ts merely LOGS reply.status — a failed reply followed by an unchanged inbox passes. If the original is archived but another message shares its subject, the subject-only fallback matcher can also pass. The script is typecheck-excluded and outside ga:gate, so nothing recurring guards Raj's archive complaint.
- Affected rows: RAJ-3 (G4 / CLWX-34): reply archives the original; RAJ3-FROM-NOOP (register B); K6; K7
- Proposed acceptance: Require successful reply creation before checking retention. Seed two messages with identical subjects but distinct identities; verify the original remains in Inbox after discard and, separately, after an authorized send. Both 'reply failed' and 'archive original, retain duplicate' mutations must fail. Restore script typechecking and gate execution.

### F6 [HIGH] Send fix/refix chain lacks one test covering safety and usability together; product doc still advertises the bypassable contract
Pattern: legitimate send refused -> reviewed-draft shortcut introduced -> subject-check bypass discovered (SEND-GATE-SUBJECT-SKIP) -> subject made mandatory again (1e3a9170). docs/PRODUCT_PRINCIPAL_ASSISTANT.md:43 still advertises {confirm:true} alone as the normal send contract. The pinned live test (scripts/v2-send-test.ts:28) calls OutlookActions directly with explicit assertions, so it does not prove a principal saying 'send it' reaches the corrected contract through the packaged agent.
- Affected rows: RAJ-1 (G4 / CLWX-34): send-gate failure; SEND-GATE-SUBJECT-SKIP (register C, 2026-09-06); K7; CLWX-71 (MCP adapter)
- Proposed acceptance: On the installed candidate, exercise new compose, inline reply, and forward: missing confirmation, missing subject assertion, and changed subject each produce zero sends; after reviewing an unchanged draft, an authorized 'send it' produces exactly one sent message with the reviewed contents. Pin both outcomes and reconcile the documentation.

### F7 [HIGH] Readable failure is being substituted for restored functionality on the K1/K2 email class
K1's acceptance criterion allows email to remain UNUSABLE provided the refusal instruction is readable — exactly the outcome observed on moe.18 after June's repeated attach failures. COMPOSER-OVERRIDE similarly passes if the auditor merely flags the wrong model instead of the turn using the configured one. Neither criterion resolves the stakeholder's operational complaint ('accessing the email').
- Affected rows: K1; K2; CLWX-73 / K1 / K2 (chrome-attach on real tester boxes); K3; EXT-TESTER (register A); COMPOSER-OVERRIDE (register B)
- Proposed acceptance: On Karunesh's box and a fresh equivalent profile, the candidate successfully reads the intended mailbox after supported sign-in across three relaunches, with the tenant Conditional Access proof. Separately, three fresh composer turns use the configured model; detected drift is a failing result until repaired. Keep readable-refusal tests as distinct negative cases.

### F8 [HIGH] Runtime failure recurrence (retry loop / orphaned turn / recovery wedge) cannot be closed by display fixes or tool trimming
Pattern from moe.18 evidence (skills/laptop/evidence/2026-09-03-moe18-verify/RESULT.md:31): breaker declared Ready -> ~15 repeated failed reads; failover declared working -> orphaned turn; connectivity restored -> UI says Online while runtime remains on Qwen. CLWX-104/105 improve presentation only; the owner-HOLD tool-catalog trim does not establish a general retry bound. No pinned test injects the three outage points.
- Affected rows: ONDEVICE-RETRY-LOOP + K11 (register B; board CLWX-38 Ready contradicted); K11; K13; CLWX-95 (degraded turn orphans at prompt.submitted); CLWX-96 (post-degrade recovery wedge); CLWX-104 (run-error de-dup D0/D1/D2); CLWX-105 (in-line error chip for error-stopped messages)
- Proposed acceptance: On both cloud and local routes, three equivalent failed tool calls prevent a fourth and terminate visibly. Inject outages before send acknowledgment, during tool execution, and during channel switching: each turn completes once or visibly terminates within the declared watchdog bound. After restoration, the next Online turn succeeds without relaunch or manual provider reassertion.

### F9 [HIGH] The ASR engine comparison is not comparing engines on the same audio
Mac legs use specified `say` voices/rates (scripts/clwx87-wer-bench.mjs:99) while the Windows leg SYNTHESIZES NEW audio with System.Speech and instantiates a recognizer directly instead of invoking the installed helper (windows-pilot/scripts/pilot-asr-wer.ps1:31); identical reference text is not identical acoustic input. The WER grader (clwx87-wer-bench.mjs:224) reports a number without enforcing any quality threshold. Even a successful VM measurement therefore cannot establish the proposed engine-decision quality bar — and the accent domain of the owner's complaint has no real recordings at all.
- Affected rows: OWNER-ASR-CLWX-87; CLWX-87 (Windows ASR quality); K4
- Proposed acceptance: All candidate engines consume byte-identical, hash-pinned WAVs including real Trinidadian-accent recordings. Record per-clip and aggregate WER, reject missing clips, enforce one approved numerical threshold, document the engine decision, and repeat through the chosen engine in the packaged app.

### F10 [HIGH] NSCC and the two stakeholder prompt suites are being conflated; the gate's NSCC row can bypass retrieval
One cited fresh-session answer cannot establish the 20-question >=80% requirement; five NSCC questions establish neither Karunesh's EXACT five-prompt fixture nor Raj's document-task suite. Even with GA_GATE_FULL=1, scripts/nscc-qna-eval.ts:271 defaults to full-text-provided context, bypassing the principal.nscc_lookup retrieval path unless NSCC_EVAL_LANE=tool is explicitly selected — so the gate row can go green without exercising the thing Raj asked for.
- Affected rows: RAJ-NSCC (CLWX-42): Code of Conduct in memory + his Q&A eval; CLWX-42 (NSCC knowledge pack in-app); K14; K9; RAJ-0OF5 (K9 / KR1 / CLWX-24): the 2026-07-21 5-prompt Ministry suite
- Proposed acceptance: Maintain three separately identified fixtures: Raj's 20 NSCC questions, Raj's five document prompts/files, and Karunesh's exact five prompts. Require >=16/20 correct NSCC answers with valid citations through installed retrieval (tool lane), plus 5/5 correct outcomes for each stakeholder suite. Reconstructed files stay explicitly labelled substitutes.

### F11 [MED] Replacing Plaud transcription with in-app ASR does not close Plaud ingestion
'Usable text' is undefined in the proposed criterion, and nonzero duration through a DIFFERENT application does not prove the recordings Raj reported are correctly ingested, indexed, or retrievable. The fix-promised-in-thread was never confirmed back to Raj and no stakeholder acceptance of the replacement path is recorded.
- Affected rows: RAJ-PLAUD (register PLAUD-ZERO-MIN, finding 35)
- Proposed acceptance: Re-ingest a previously failing recording through the actual supported ingestion route; indexed duration matches independently measured duration within 1s, the transcript meets a declared accuracy threshold, and the recording is retrievable. If Plaud is retired, record stakeholder acceptance of the replacement separately and retain the original defect's unresolved disposition.

### F12 [MED] The latency rows disagree on what constitutes success and can hide the failure mode Raj reported
The same complaint carries two different p90 limits (30s in RAJ-LATENCY/OWNER-TRIM-K11 vs 45s in CLWX-43). Three individual prompts do not establish a meaningful p90 distribution, and successful-turn-only reporting can omit the timeout that most directly represents Raj's 'thinking' complaint (the 09-06 run's read_pdf timeout was excluded as 'not a latency number').
- Affected rows: RAJ-LATENCY (LATENCY-UX / CLWX-43); CLWX-43 (LATENCY-UX budget + measurement); OWNER-TRIM-K11; CLWX-47 (slow-turn watchdog TB-3)
- Proposed acceptance: Use one versioned budget for both aliases. Run each of the three demo prompts at least 20 times on the pilot laptop/current build, recording cold/warm conditions. Count timeouts as failures and measure submission-to-usable-completion. Exceeding the approved p50/p90 limit fails acceptance; a watchdog notice alone cannot close latency.

### F13 [MED] Known stakeholder asks are missing from the map: Raj's meeting-minutes feature and Karunesh's supplied doc-search repro
Raj's May 25 seven-feature request explicitly includes meeting minutes (docs/wiki/LIAISON_LOG.md:25) — the map has no transcript-to-minutes row; transcription plus generic document writing does not prove minutes generation. Separately, docs/KARUNESH_GUIDED_TEST_2026-09-03.md:128 already supplies Karunesh's document-search opener and an exact recursive-listing comparison, contradicting the DOCSEARCH row's claim that only a new tester repro can move it.
- Affected rows: DOCSEARCH / G6b (register B); RAJ-PLAUD (register PLAUD-ZERO-MIN, finding 35); CLWX-55 (use-case demo reel UC1-UC10); CLWX-68 (leave/attendance registers + inventory, post-GA)
- Proposed acceptance: Add a minutes-workflow row: from a fixed transcript, produce a saved document preserving every annotated decision, action owner, and deadline, with zero invented attendees. For search, compare two consecutive in-app enumerations against a filesystem manifest including nested files; require exact set equality. Unretrieved attachments remain unknown, not inferred findings.

### F14 [MED] External-validation and clean-install qualifiers have been silently weakened vs the scorecard
EXT-TESTER drops the GA_READINESS scorecard's 'public Release' and 'no help' qualifiers (docs/wiki/GA_READINESS.md:75) while adding email; KR2's map criterion drops the scorecard's completed on-device turn. The moe.19 VM script (scripts/vm-verify-moe19.sh:86) upgrades an existing installation silently, which cannot prove an assisted fresh-user installation.
- Affected rows: EXT-TESTER (register A); CLWX-25 / KR2 / BOOT-EMPTY (recorded assisted clean-VM install); K3
- Proposed acceptance: Preserve the original qualifiers or record an explicit scope amendment. External validation requires an independent tester's public-download->install->first-turn evidence without coaching. KR2 requires a fresh user/config, assisted-screen recording, zero manual dependency installs, healthy services, and a completed on-device turn.

### F15 [MED] The Monday deadline in the map is wrong by one day
September 7, 2026 is Monday; September 8 is Tuesday. The OWNER-MONDAY-BUILD row explicitly equates 'Monday' with 2026-09-08, granting an extra day against Raj's relayed demo deadline.
- Affected rows: OWNER-MONDAY-BUILD
- Proposed acceptance: Record the demo's dated, timezone-qualified start time; the mapped date must resolve to Monday, September 7. Required publication, demo-machine installation, and verification timestamps must precede that deadline.

### F16 [HIGH] Exact moe.19 closure boundary: the scheduled VM verify can close at most 5 of the 86 rows, and only via its interactive legs
scripts/vm-verify-moe19.sh:158 finishes by writing an UNCHECKED interactive checklist and asking for a subsequent RESULT.md; its cited acceptance doc docs/STAKEHOLDER_GAP_ANALYSIS_2026-09-06.md is absent — so scripted success closes nothing by itself. Only 5 rows have interactive legs capable of closing their stated gap on the attested candidate: CLWX-42 (fresh-session no-file NSCC answer via real principal.nscc_lookup), CLWX-99/CRON-TZ (correct next-run AND actual 3:30pm local firing in a non-boot tz, incl. seeded-job repair), CLWX-100 (clean fired reminder + anonymised composer), CLWX-104 (both failure directions with no stale/contradictory banner or raw error), CLWX-105 (error-stopped messages visible on reopen). The other 81 rows cannot fully close from this verify: real-tester/tenant attach evidence missing (K1/K2/K3/CLWX-73/EXT-TESTER); installed-app mailbox scenarios and authorized dispatch absent (RAJ-1..4, K6, K7, K11, CLWX-61, SEND-GATE-SUBJECT-SKIP); Graph L4/L5 not run (CLWX-39/40, RAJ-REDIRECT); production/live Forms legs (CLWX-7/8, ATLAS-15, CLWX-64, CLWX-63); MCP positive legs (CLWX-71); ASR/Plaud measurement gaps (RAJ-PLAUD, CLWX-87 rows, K4); installed-resource matrix/fixture gaps (K8, KAR-PDF, K9, RAJ-0OF5, K10, K12, K14, CLWX-77 rows, CLWX-101); RAJ-NSCC 20-question bar; runtime-loop/recovery classes (ONDEVICE-RETRY-LOOP+K11, K13, CLWX-95/96); unbuilt/unreproduced items (CLWX-89, COMPOSER-OVERRIDE, CLWX-47, ACPX-NOISE, CLWX-88, STEER-READ, CLWX-86 — which has NO missing-route test in the checklist, CLWX-48/49/54); human/Ministry-gated decisions (latency budget, trim, KR6/7/8, security floor CLWX-18/19, UPSTREAM-GAP, G5-VERDICT, CLWX-22, KR2 human close, CLWX-60); and process/tooling rows (K5, DOCSEARCH, FIXTURE-HYG, DRIVER-SETTLE, OWNER-GATE-CLWX-90, OWNER-MONDAY-BUILD publication legs, CLWX-50, CLWX-55/56/57, CLWX-68, RAJ3-FROM-NOOP, OWNER-FLAG-KR5, OWNER-FLAG-STALE-PILE, CLWX-102).
- Affected rows: CLWX-42 (NSCC knowledge pack in-app); CLWX-99 / CRON-TZ (reminders fire at wrong wall-clock); CLWX-100 (cron-session cosmetics); CLWX-104 (run-error de-dup D0/D1/D2); CLWX-105 (in-line error chip for error-stopped messages); CLWX-86 (tool<->host-API version skew); OWNER-MONDAY-BUILD; CLWX-22 (OKR anchor — GA declaration)
- Proposed acceptance: The verification result enumerates all 86 row IDs as CLOSED, PARTIAL, or NOT_EXERCISED. Every CLOSED entry references the exact artifact hash and evidence satisfying every clause of that row; unchecked checklist steps, script exit 0, file presence, and version labels alone cannot close any row.

## 3. Completeness critic: what the framing itself missed

### MISS-1 — The three demo moments have never been rehearsed as one continuous session
- Date: 2026-09-08 (demo Monday; assessed 2026-09-05/06) | Stakeholder: The principals themselves (demo audience)
- Complaint: The three demo moments (email, doc-to-form, cron reminder) have each been proven once, separately, on different dates/builds/lanes — but NEVER rehearsed back-to-back as one continuous session on the machine/build/account the principals will actually see. The only demo runbooks and dress-rehearsal phases in the repo are dated 2026-05-26/29 (the May demo). GA_DEMO_READINESS_ASSESSMENT_2026-09-05.md itself recommends P2 (demo pre-flight + recovery runbook) and P3 (freeze the recorded backstops) — neither was ever carded (board ends at CLWX-105; no such card exists).
- Changes made: docs/GA_DEMO_READINESS_ASSESSMENT_2026-09-05.md written (analysis only, 'nothing started'); Recorded Suspensions submit exists as a loose artifact (video+trace); CLWX-67 cron reminder Mac-leg proof landed 2026-09-05 (as a harness, not a demo clip)
- Acceptance criterion: One continuous 20-minute rehearsal of all three moments on the actual demo build+machine, timed, with a written 2026-09-08 pre-flight/recovery runbook and the two backstop clips staged as named demo assets
- Pinned tests: — | Verified on: never (per-moment proofs only; latest demo runbook is docs/DEMO_RUNBOOK_2026-05-26.md)
- Gap: Card + execute a full dress rehearsal before Monday; author DEMO_RUNBOOK_2026-09-08.md (pre-warm attach, reset sequence, fallback order); freeze moment-2 and moment-3 clips into docs/evidence/ as demo assets

### MISS-2 — No Mac installer newer than moe.10 has ever been cut; the demo-machine class runs long-fixed defects
- Date: 2026-09-06 (live-reproduced) | Stakeholder: Principals + owner (Mac demo machine)
- Complaint: Time bomb: no Mac installer newer than 0.4.3-moe.10 (May) has ever been cut — the moe.18 release manifest explicitly skips two stale mac trees whose asar reads moe.10. Every moe.11–18 fix (CLWX-72/92 PDF parsers, CLWX-99 cron tz, CLWX-58/46 Outlook recovery/stale-read, CLWX-78/104 degrade, CLWX-42 NSCC pack) exists only in-tree or in Windows builds. Live proof of the hazard: on 2026-09-06 the installed Mac app's read_pdf turn silently hard-killed its gateway twice (CLWX-43 addendum) — the long-fixed class, live on the machine class the demo runs on. The 'Monday demo build' exists only as owner-ask notes inside CLWX-43/85 comments, not as a card.
- Changes made: Fixes all landed in-tree; scripts/release-hash-manifest.mjs gate ready (CLWX-85); CLWX-43 addendum documents the moe.10 reproduction and the 'this dev Mac needs the new install too' finding
- Acceptance criterion: A packaged Mac 0.4.3-moe.18 (or newer) installed on the demo Mac, manifest-published, with the live smoke (v2-chatbot-e2e + one read_pdf turn + one cron reminder) green ON THE INSTALLED BUILD, not the dev tree
- Pinned tests: docs/evidence/CLWX43_LATENCY_MAC_LANE_2026-09-06.md (failure reproduction); docs/release-manifests/0.4.3-moe.18.json (shows mac trees = moe.10) | Verified on: Mac last packaged at moe.10; all Mac live evidence since runs on the unpackaged dev tree
- Gap: Card the Mac RC cut: pnpm package:mac:local, install, pnpm release:manifest:publish, then re-run the live smokes on the installed app before 09-08

### MISS-3 — The AI Tool Usage Agreement was never reviewed against the actual architecture
- Date: 2026-05-26 (document received) | Stakeholder: MoE legal/compliance + every principal whose data the pilot touches
- Complaint: The 'AI Tool Usage Agreement MPAAI↔MoE.pdf' — called 'the legal frame for the pilot' in the 2026-09-02 stakeholder report — was NEVER reviewed against our actual architecture (turn data egressing to Google AI / AWS Bedrock / the Cloud Run broker, the WhatsApp liaison capture, credentials persisted on device, the public clawx-pilot repo). Zero board cards mention it; it sits in the 'never-acted-on documents' list with no owner and no disposition.
- Changes made: — 
- Acceptance criterion: A FACTS vs ANALYSIS conformance memo: each agreement clause mapped to the real data flows, with any conflicts (e.g. third-party model providers, telemetry, credential storage) flagged to the owner before fleet rollout
- Pinned tests: — | Verified on: never
- Gap: Card an agreement-conformance review; feed conflicts into the Ministry working-session agenda (it is the one stakeholder document with contractual force)

### MISS-4 — The live Outlook proof set is dated 2026-09-02 (moe.15-era code); the code being demoed has never passed a live send
- Date: 2026-09-02 → ongoing (evidence pinned 2026-09-02) | Stakeholder: Principals + Raj (the live email/forms surface)
- Complaint: Time bomb: the live Outlook proof set (15/15 v2-eval, 4-step gate proof, recorded forms submit) was produced 2026-09-02 on moe.15-era code. Since then the Outlook driver changed materially (CLWX-58 compose auto-recovery, CLWX-69/70 exit paths, CLWX-46 stale-read guard) — and NO live re-proof exists: the moe.17 VM verify left six checks PENDING including send, and every ga:gate run since (through GA_GATE_2026-09-06.md) is static-only with the T1 live email+forms lane SKIP/wedged behind the Chrome :18792 restart. The code being demoed Monday has never passed a live send.
- Changes made: Static T0 gate GREEN 7/0/2 daily; moe.17/moe.18 verify agents in flight; outlook contract units 73/73 at HEAD (mocked, not live)
- Acceptance criterion: One full T1 live-lane GREEN (pnpm ga:gate without GA_GATE_STATIC) + v2-eval 15/15 + 4-step gate proof executed on the moe.18 tree, dated after the last Outlook-driver change
- Pinned tests: docs/evidence/GA_GATE_2026-09-06.md (live-lane SKIP); scripts/v2-eval.ts; scripts/v2-send-test.ts | Verified on: moe.15-era code, 2026-09-02
- Gap: After the owner Chrome restart, immediately re-run the full live lane and re-pin the email/forms evidence to the demo code; treat the 09-02 evidence as expired

### MISS-5 — Raj's go-live-before-school-opens / 20-principal cohort ask has silently expired uncarded
- Date: 2026-07-17 (Raj ask; deadline passed ~2026-09-01) | Stakeholder: Raj/Ministry + the 20-principal test cohort
- Complaint: Raj's 2026-07-17 ask — 'must go live before school opens', '20 principals to test a week' — has silently expired: school opened ~Sep 1 and no card anywhere covers cohort rollout logistics (installer distribution with auto-update OFF so every version goes through MoE IT, per-laptop signed-in Chrome setup across 7 districts, training, a support/escalation channel). CLWX-8 covers only the production login + support LOGGING flow; nothing covers getting 20 machines running and supported, and no honest revised-schedule answer to Raj exists in the closeout pack.
- Changes made: CLWX-8 (production Outlook teacher login + support logging) exists, Unstarted; Windows install runbook exists for the single pilot laptop
- Acceptance criterion: A carded rollout plan (distribution mechanism, per-school onboarding checklist, support channel, cadence for shipping fixes with auto-update OFF) plus a dated schedule answer sent to Raj
- Pinned tests: — | Verified on: never
- Gap: Card the 20-principal rollout plan; add an eighth closeout draft acknowledging the missed go-live date with the revised plan

### MISS-6 — Only 2 of Raj's all-forms Drive corpus were ever schematised; source PDFs not vaulted
- Date: 2026-05-01 / 2026-05-14 (Drive folder shared) | Stakeholder: Raj + principals (forms capability breadth)
- Complaint: Of the all-forms Drive corpus Raj shared, only 2 forms (Suspensions 32-field, Daily Report 57-field) were ever schematised — the stakeholder report flags 'only 2 of the corpus ever schematised' and the liaison log lists the real MoE forms as 'referenced but absent (retrieve/ask)'. The remaining daily/statutory forms were never pulled, inventoried, or carded, and the original form-PDF BLOBs are not vaulted locally (only a 'CLWX-32-adjacent' follow-up note on CLWX-41), so clone fidelity against the real forms cannot be re-verified.
- Changes made: Suspensions + Daily Report cloned, schematised, live-proven (CLWX-62 recorded); Bridge-download vault pass noted as a follow-up comment on CLWX-41 (not a card)
- Acceptance criterion: A committed inventory of the full forms corpus (name, fields, priority) with a schema backlog card per form, and the two source PDFs vaulted with hashes in the chain-of-custody baseline
- Pinned tests: — | Verified on: 2 of N forms only
- Gap: Pull/inventory the Drive corpus (or ask Raj to re-share), card the remaining schemas, execute the BLOB vault pass under CLWX-32

### MISS-7 — Raj's AI_Email_Management_ideas.docx was never folded into any roadmap
- Date: 2026-06-27 (document received) | Stakeholder: Raj (email capability direction)
- Complaint: AI_Email_Management_ideas.docx — Raj's explicit email-capability expansion asks — was never folded into any roadmap and has no card. The 2026-09-02 stakeholder report soft-flagged it ('never folded into an email roadmap') and nothing has happened since; the email lane's open cards (CLWX-61/46/58) all derive from OUR defect mining, not from his feature document.
- Changes made: —
- Acceptance criterion: Each ask in the docx extracted, dispositioned (build/defer/decline) on a card, and the disposition relayed to Raj (he responds to concrete artifacts)
- Pinned tests: — | Verified on: never
- Gap: Mine the docx into a carded email-roadmap review; pair the answer with the NSCC closeout draft since both are 06-27 asks

### MISS-8 — KR3 (offline) is a CHECKED scorecard box on pre-moe.11 evidence while its subsystems were rebuilt
- Date: ~2026-08-20 (evidence date) vs moe.18 today | Stakeholder: Owner (GO/NO-GO scorecard integrity) + offline principals
- Complaint: Time bomb: KR3 (on-device + local docs fully offline, egress blocked) is one of only two CHECKED boxes, but its evidence (lane G 5/5, bde78d94 era) predates moe.11 — since then the packaging and doc pipeline were rebuilt (supportedArchitectures/canvas bindings, bundle hard-fail gate, DOMMatrix polyfill, doc-tools loadDepDetailed, NSCC pack, plugin inventory 31→32). The offline claim has never been re-proven on any build ≥ moe.16, while the exact subsystems it depends on all changed.
- Changes made: Bundle-verify gate now runs in T0 (parsers loadable on host); KR4 degrade Windows gap found+fixed via V-batch (shows the Mac-only-evidence pattern bites)
- Acceptance criterion: Lane G re-run (network blocked, on-device channel, local doc read + answer) on the current packaged build, or the KR3 box annotated as stale pending re-proof
- Pinned tests: original lane G evidence (5/5, pre-moe.11) | Verified on: pre-moe.11 build (~2026-08-20)
- Gap: Schedule the offline lane G re-run into the next V-batch / Mac RC verify; until then mark KR3 evidence-stale on the scorecard

### MISS-9 — The 2026-04-29 Minister/2000-laptop ask has no disposition on record
- Date: 2026-04-29 (Raj inbound) | Stakeholder: Raj / the Minister's office
- Complaint: The 2026-04-29 ask — Minister receiving 2000 laptops at a ceremony and wanting to test 'Sov AI' content generation — is a dated inbound ask in the ClawX section of the liaison log with no card, no disposition, and no entry in the 7-item dropped-ball closeout pack; the '2000-laptop plan' is also on the liaison log's referenced-but-absent list. It may be scoping-era or the other workstream, but NOTHING records that decision, which is exactly the class of silent drop the structural finding warned about.
- Changes made: —
- Acceptance criterion: An explicit disposition on record: either a card (in-scope/post-GA) or a one-line closeout note to Raj classifying it (e.g. video-project side or superseded), so the archive shows a decision rather than silence
- Pinned tests: — | Verified on: never
- Gap: Disposition it in the next liaison sweep: confirm which workstream owns it and add closeout draft 08 or a declined-scope note

### Not covered (critic's own declared blind spots)
- Karunesh's raw WhatsApp thread (192 messages) and his attached test documents live in the local liaison archive / bridge store and were not re-read; the ledger and derivative docs were taken as the record of his words, spot-checked against board cards and the state vector but not against the primary chat store.
- The image + document Karunesh sent with his 2026-09-03 moe.18 field report are recorded as 'in the WA bridge store, unretrieved' — their contents (possible new findings) could not be mapped.
- His moe.15 app log (clawx-2026-09-03.log) and the 2 screenshots are 'local liaison archive; not committed' — card claims citing them could not be independently verified.
- Whether the moe.18 installer shipped to Karunesh actually contained the CLWX-73 chrome-cdp fix was inferred from field-report behavior (plain-language instruction, no managed profile), not from a build manifest.
- The live Plane board (localhost:8090) was not queried; card states come from the 2026-09-06 mirror export, which the repo itself flags as non-authoritative vs the JSON/live board.
- Section B of the ledger (video-generator QA rows) is a separate project by liaison rule and was deliberately not mapped.
- Whether the full ga:gate with live T1 legs (9/0/2) has re-greened since 2026-09-04 could not be established — the 2026-09-06 pulses are 7/0/2 with the two live legs as opt-in skips.
- The moe.18 VM verify RESULT.md (skills/laptop/evidence/2026-09-03-moe18-verify/) was not opened directly; its findings were taken from the state-vector tick summaries and CLWX-78 card comments, which quote it extensively and consistently.
- ASR quality complaints: the mined Windows System.Speech WER complaint (CLWX-87, 4+ sightings) is attributed to the OWNER, not Raj — no Raj-attributed ASR-quality row exists; the only Raj-adjacent audio item is the Plaud-ingestion report (row RAJ-PLAUD above).
- The 2026-06-23 principals-demo "0/5 score" rumor (G5): distinct from the 07-21 prompt suite; LIAISON_LOG confirms "0/5 feedback NOT FOUND in any captured channel" and no post-demo verdict was ever captured — no row can be built (complaint text/date unverifiable); the question to Raj remains queued.
- Current health of the Mac live lane: the Chrome CDP attach wedge (third-confirmed 2026-09-05) may still block every T1 live pin (v2-eval, stale-read, raj2/raj3 checks); the 2026-09-06 ga:gate ran static-only (T1 SKIP), so the last full live-pin run on record is 2026-09-04 — whether the pins are re-runnable today is unestablished.
- moe.19 (cut 2026-09-06, first build carrying the NSCC pack + the mandatory send second-gate): no verify evidence exists in the tree at analysis time — every "in-tree" claim gated on it stays unverified.
- Raw WhatsApp archive not consulted directly (worked from LIAISON_LOG, DEFECT_REGISTER, ledgers, and evidence dirs per policy); outbound record before the 2026-09-01 sent-ledger is structurally incomplete (~61% inbound captured, our side largely unstored), so pre-September promises to Raj cannot be fully reconstructed.
- Whether Raj received/acknowledged the 09-01/09-02/09-03 sends: ledger entries prove dispatch (bridge success), not receipt or acknowledgment; no inbound from Raj on this product is captured after 2026-07-20 apart from the "Raj wants a demo version for Monday" signal relayed 2026-09-03 through the Karunesh thread.
- RESULT.md tail of the 2026-08-20 raj-prompt-replay (per-check table body) was not read line-by-line; the 7/7 claim rests on the register/ledger cross-references plus the report header confirming reconstructed fixtures and the replay method.
- The ASR '4x' count and the 2026-05-26 first-complaint date come from the mined BLOCKER_BUG_COLLECTION row (session-log-miner attribution); the underlying Codex session logs/WhatsApp were not re-inspected to independently recount the four instances.
- The exact date and verbatim wording of the owner's original trim-HOLD directive were not found in any scoped doc; hard anchors are the branch commit date (7add864b, 2026-08-03) and the 2026-08-20 SCALE_ANALYSIS/memory trace — the row is dated to the branch commit.
- Whether a moe.19 build artifact was produced after the d0d9bd9a version bump: other agents are active in this session and a package run may be in flight, but nothing was visible in the tree/manifest at read time (2026-09-06 evening). [GPT-6 subsequently verified an installer exists — see F2.]
- CLWX-28's claimed live kill -9 + restart test (2026-09-02) was not re-inspected beyond the Ready-card comment; the KR5 row's verification statement leans on the re-run unit 9/9 plus source-wiring reads.
- Live Plane board state — only the committed mirror (docs/plane-board/CLWX-board.md) was read; the CLWX-103 comment-orphan class is documented as fixed but the live board was not re-audited.
- Owner directives recorded only in raw session logs or WhatsApp outside the three scoped docs + board mirror (e.g. the verbatim phrasing of 'KR5 ahead of any more cards' and 'last real work was Sept 3' exists here only as quoted in GA_SPRINT_STATE_VECTOR.md).
- The K11 cloud-path half (readable email refusal on the cloud channel) is recorded as unverifiable on the VM's placeholder gateway; whether it has since run on the Mac live lane could not be established from the scoped sources.
- Whether specific fix commits are inside the shipped moe.18 artifact was inferred from state-vector dates (moe.18 cut 09-03; fixes 09-04+), not from a per-build manifest diff — no installer was hashed or unpacked.
- Karunesh's 09-03 image + document with failure details sits unretrieved in the WhatsApp bridge store — it may contain open items this map does not know about.
- CLWX-54 has no reproduction details anywhere findable (the card is a TODO-stub); its suggested link to the :18792 CDP wedge is inference, not evidence.
- Ready-state cards were treated as terminal per the task scope; their human-close residuals were only rowed when a register/NEXT-BUILD entry named them (e.g. CLWX-76 leg-3's mammoth/xlsx live re-run on a packaged install rides moe.19 but got no dedicated row; ditto KR5-WIRING's optional live kill-9 smoke and CLWX-46/58/69/70's next-lane live clicks).
- No live probes, tests, or builds were run for this map — verification claims rest on the evidence docs plus file-presence spot-checks (all cited scripts/tests/evidence paths confirmed to exist; contents not re-executed).
- The K11 cloud-path readable-refusal bar (unverifiable on the placeholder-gateway VM) is assigned to the Mac live lane by the verify doc; whether that lane run is scheduled anywhere was not established.
- Plane board live state beyond the mirror (docs/plane-board/CLWX-board.md) was not queried; if the mirror lags (a known past defect, since fixed via CLWX-103), card states here could be stale by up to one export.
- CLWX-52/53 (raw model id / raw 400 wording) are Ready cards whose live confirmation is folded into the CLWX-100 'confirm RC composer anonymises' clause and the moe.18 trust sweep (PASS except D2); not rowed separately and the composer anonymisation was not independently verified on any build.
- The exact pinned-test filenames for CLWX-104 (unit + e2e) and the KR7 UserId seam are cited via their evidence docs rather than enumerated — those evidence files were not opened to extract the spec paths.
- DOCSEARCH (G6b) — Raj's 2026-07-17 document-search inconsistency has a defect-register row (UNKNOWN/UNVERIFIED) but no CLWX card and no repro; technically inside the 'open register items' framing, so not filed as a row — but it is the only Raj-lineage defect with neither card nor disposition.
- June-23 principals-association demo verdict + closeout drafts 02–07: verified COVERED — carded under CLWX-45 (draft-and-hold, item 07 chases the verdict), owner-GO-gated; do not double-count.
- PDF read (Karunesh-via-Raj 06-30) and Windows ASR: verified COVERED by CLWX-72/92/97 and CLWX-87 (WER evidence 2026-09-06).
- Karunesh's separate curriculum-video QA reply (unsent since 2026-08-25, P0 defects drafted in VIDEO_QA_TRACKER): explicitly NOT a ClawX deliverable per the liaison rule, so excluded from rows — but it is the same liaison relationship and 12+ days of silence risks bleeding into ClawX goodwill.
- SSMD Practitioner Series Day-9 agenda: project attribution still unresolved (park-or-ask note in LIAISON_LOG §D); minor, no row.
- SEA Typical School Results_2024.xlsx never committed as a regression fixture (stakeholder-report soft flag): arguably absorbable into CLWX-77's doc-type matrix rather than a new gap.
- On-device default-channel quality for demo-shaped prompts: judged adequately boarded via CLWX-26/27/36/38 and the register's tool-cascade rows; not filed.
- Could not verify what account/tenant the 09-08 demo will use (test.fac assumed); if a real @moe.gov.tt session is planned, none of the email/forms evidence transfers — everything ever proven is sandbox-tenant (CLWX-8 covers the production-login gap on the board).

## 4. The moe.19 VM-verify checklist (derived)

Preconditions (before any row can claim closure — GPT-6 F2/F16):
- [ ] **Artifact attestation first.** Record source revision, installer SHA256, and installed `app.asar` SHA against docs/release-manifests/0.4.3-moe.19.json (`node scripts/release-hash-manifest.mjs verify`). The manifest records TWO same-version moe.19 builds — evidence citing the superseded hash is invalid. GPT-6 verified an installer at SHA256 3a533a0f… exists; `scripts/vm-verify-moe19.sh` checks only the version string, which is insufficient.
- [ ] The verify RESULT.md must enumerate row IDs as CLOSED / PARTIAL / NOT_EXERCISED; script exit 0, file presence, and version labels close nothing by themselves (F16).
- [ ] Lane caveat: on-device turns are unreliable on the e2-standard-4 VM (VM-CPU-STARVE) — on-device verdicts must be marked lane-constrained or anchored to the laptop.

### 4a. In-app visual checks (interactive legs — the only legs that can CLOSE rows per GPT-6 F16)

| # | Row(s) | Check | How |
|---|---|---|---|
| V1 | CLWX-42 / RAJ-NSCC (in-app half) / K14 substance | Fresh session, NO file attached: ask a Code-of-Conduct question (K14-style). Answer must be grounded in the NSCC with a citation, via `principal.nscc_lookup` (confirm the tool call in the gateway trajectory, not just answer text — F10: context-lane evidence does not count). | Visual + trajectory/log cross-check |
| V2 | CLWX-100 | Fired reminder renders as a clean principal-language chat prompt: no `[cron:<uuid>]`, no instructions/tz header, no think-block; composer shows only "Online" / "On this device". | Visual, at the seeded reminder's fire time (or a near-term test cron) |
| V3 | CLWX-104 / K13 dir-2 | Re-run the degrade matrix both directions (kill cloud reachability; kill Ollama). Amber degrade prompt appears with NO co-rendered contradicting run-error banner, NO raw "Connection error." anywhere including the Technical details expander; banner does not survive new chats or a gateway restart. | Visual through both outage directions |
| V4 | CLWX-105 | Force an error-stopped assistant message, close and re-open the session: an in-line principal-language error chip is visible (raw string only behind the collapsed expander). Map said "unbuilt"; GPT-6 found tests/unit/chat-message-error-chip.test.tsx — verify the behavior on the artifact either way. | Visual on session re-open |
| V5 | CLWX-86 | Missing-route probe: trigger a tool whose host-API route is absent (or simulate skew) — must park with a readable capability message, never a raw "No route for POST …" leak. NOTE (F16): `vm-verify-moe19.sh` has NO such step today; add it manually. | Visual + host-API log |
| V6 | CLWX-101 / part of CLWX-77 | One in-app turn on a legacy `.doc` and one on `.rtf`: principal-language refusal, no jszip internals. | Visual |
| V7 | RAJ-0OF5 / K9 (strongest-closure leg, does not need email) | Run Raj's verbatim 5-prompt suite in-app on the packaged build (docx read, docx rewrite/save, PDF summary, xlsx grading, image OCR) — the run KARUNESH_GUIDED_TEST names "the strongest possible closure of the 0/5 record". Fixtures are reconstructions; label them as such (F10). | Visual, 5 prompts, seeded via windows-pilot/pilot-seed-demo-documents.ps1 (FIXTURE-HYG: purge+reseed first) |
| V8 | K10 residual | The literal drag-GESTURE PDF cell (drag a PDF into chat → summarise), not the file-open mechanism. | Visual |
| V9 | K12 (re-confirm only) | Badge truthful through gateway kill→restart (connected → Reconnecting pill + disabled composer → connected). Already PASSed on moe.18; re-run as regression. | Visual |
| V10 | CLWX-95 / CLWX-96 (regression OBSERVATION — cannot close) | Repeat the moe.18 step-7 legs: switch-during-outage orphan and post-restore silent-send. EXPECTED STILL FAILING (no fix is in moe.19); record honestly, do not claim closure. | Visual |
| V11 | MISS-8 / KR3 (optional add, closes the staleness) | Offline leg on the packaged build: network blocked, on-device channel, local doc read + grounded answer. First lane-G-shape proof on any build ≥ moe.16. | Visual with the VM NIC disabled (subject to VM-CPU-STARVE caveat) |

### 4b. Log checks

| # | Row(s) | Check | How |
|---|---|---|---|
| L1 | CLWX-99 / CRON-TZ | `pnpm exec tsx scripts/clwx99-tz-check.ts` leg A on the installed build: nextRun at the correct LOCAL wall-clock (the pre-fix baseline documented 22:29 vs 23:59); seeded 3:30pm Daily Report reminder shows a sane nextRun in a non-gateway-boot tz; seed repaired the already-seeded install. | Script + cron store/log inspection |
| L2 | CLWX-42 | Gateway log/trajectory shows `principal.nscc_lookup` registered and invoked (pack present in the installed plugin dir, sha-pinned data file). | Log + file hash |
| L3 | ACPX-NOISE (observe only) | Gateway boot log: count acpx probe failures + system-presence RPC timeouts. Expected still present (uncarded); record for the disposition decision. | Log |
| L4 | Hard-rule floor | No body content / recipients / passwords in logs on any exercised turn; subject truncated to 120 chars. | Log sweep |
| L5 | CLWX-19 (record only — cannot close) | Confirm whether resources/cloud-gateway.key still ships plaintext in the moe.19 artifact (it will, unless provisioning step A rode the cut). Owner-gated; record the fact. | File check on the installed tree |

### 4c. Harness / script commands

| # | Row(s) | Command | Pass bar |
|---|---|---|---|
| H1 | K8 / K10 / KAR-PDF / CLWX-77 Windows leg | `pnpm harness:artifact` FULL matrix (NOT `--fast`) on the Windows lane with the packaged node (`--node-bin`), NODE_OPTIONS backslash nuance applies. GPT-6 F3 caveat: staging uses the CHECKOUT's plugin/build-openclaw — point staging at resources extracted from the exact installer, or record the run as parity-limited. `--fast` runs K8 rows once and skips the .doc/.rtf rows — it cannot satisfy the ≥3× intermittence criterion. | 0 FAIL; K8 rows repeat 3×; refusals principal-language |
| H2 | K4 recurring-matrix criterion | `windows-pilot/pilot-asr-smoke.ps1` on the moe.19 packaged runtime (only ever run on moe.15). | ASR_SMOKE_OK |
| H3 | CLWX-87 / OWNER-ASR (VM start is the gate, not moe.19) | `windows-pilot/scripts/pilot-asr-wer.ps1` — the System.Speech WER leg against eval/fixtures/clwx87-asr-manifest.json. GPT-6 F9 caveat: it synthesizes new audio rather than consuming the hash-pinned WAVs — record as directional until byte-identical inputs exist. | Numbers recorded + engine decision drafted for the owner |
| H4 | Office write runtime (CLWX-65-adjacent) | `windows-pilot/pilot-office-runtime-check.ps1` (OFFICE_WRITE_OK class) on the installed build. | OFFICE_WRITE_OK |
| H5 | CLWX-97 live probe | Relocated-install / packaged doc read exercising CLAWX_APP_RESOURCES (the card's recorded needsLiveProbe). | Read succeeds from the relocated path |
| H6 | KAR-PDF residue / CLWX-76 leg-3 | mammoth/xlsx truthful-error live re-run on the packaged install (rides moe.19 per the register). | Truthful error surfaces, no silent success |
| H7 | KR2 upgrade legs (regression) | Silent-upgrade + no-clobber legs (re-PASSed on moe.18); note F14: `vm-verify-moe19.sh:86` upgrades silently — a fresh assisted install is a SEPARATE leg if KR2-shape evidence is wanted. | PASS both |
| H8 | DRIVER-SETTLE bookkeeping | Any green driver-based turn in this verify closes the register row — cite the specific run in RESULT.md. | One line of bookkeeping |
| H9 | Mac-lane twin (not VM, same build train): SEND-GATE-SUBJECT-SKIP / RAJ-1 hardening | After the owner Chrome :18792 restart, on the installed moe.19-class Mac build: `pnpm exec tsx scripts/v2-send-test.ts` (subject-mismatch refusal + matched confirmed send) + `pnpm exec tsx scripts/v2-eval.ts` 15/15 + `scripts/clwx46-stale-read-check.ts`. This is the NEXT-BUILD close for the two-gate hole; the VM cannot run it (no tenant Chrome). | Live 2-gate proof PASS on the shipped build |

### 4d. Rows moe.19 CANNOT close (owner named)

- **Owner (Anton), Chrome :18792 restart:** CLWX-61, CLWX-63, CLWX-64 live-form run, CLWX-71 positive legs, MISS-4 live re-pin, full T1 ga:gate green (OWNER-GATE-CLWX-90's ≤24h bar), CLWX-54 triage.
- **Owner (Anton), decisions/sittings:** CLWX-18 + CLWX-19 security floor (repo/key surgery — moe.19 re-ships the exposed key), OWNER-TRIM-K11 / KR6 trim unhold (CLWX-29), CLWX-43 latency budget sign-off + laptop measurement, RC-VM start spend call (CLWX-87 H3, CLWX-77 H1), UPSTREAM-GAP posture review, CLWX-22 GA declaration, CLWX-25 human close, CLWX-60 human cancel, CLWX-57 close/cancel.
- **Karunesh (external tester):** K1/K2/CLWX-73 real-box attach re-run on moe.19 (the honest bar), EXT-TESTER email leg, K6/K7 personal reply re-confirmation, retrieval of his 09-03 image+document from the WA bridge store.
- **Raj / Ministry:** CLWX-7 Forms destination, CLWX-8 production login/support, KR8/CLWX-31 values + working session, KR7/CLWX-30 attribution verify, ATLAS-15 production-auth leg, G5-VERDICT, moe.gov.tt Conditional-Access probe (CLWX-73 needsLiveProbe), real 07-21 test files (RAJ-0OF5 fixtures), NSCC-2026.pdf provenance re-pull, RAJ-PLAUD acceptance of the replacement path (F11).
- **Operator, one ~2-min sandbox sign-in:** CLWX-39 L4 (`scripts/v2-eval-graph.ts`), then CLWX-40 L5 on a Chrome-less machine.
- **Unbuilt code / process (no artifact to verify):** CLWX-89, CLWX-47, CLWX-48, CLWX-49, CLWX-50, CLWX-55/56, CLWX-88 disposition, CLWX-95/96 fixes, ONDEVICE-RETRY-LOOP breaker for the hallucinated-read class (F8), ACPX-NOISE, STEER-READ, CLWX-102 dispositions, RAJ3-FROM-NOOP fix, RAJ-2 fidelity gate wiring (F4), K14 `karunesh-matrix` fixture, CLWX-68 (parked), FIXTURE-HYG encoding, COMPOSER-OVERRIDE disposition, DOCSEARCH (see proposed card), G5-VERDICT, MISS-1/3/5/6/7/9 (see §5).

## 5. Proposed new cards

Cross-checked against docs/plane-board/CLWX-board.md (mirror @105 cards, 2026-09-06 export): none of the following is covered by an existing card.

1. **ga:gate skip-fail semantics + release enforcement (GPT-6 F1).** Acceptance: with Chrome unavailable, the default (non-static) `pnpm ga:gate` exits nonzero — a required tier that entirely SKIPs is a FAIL unless GA_GATE_STATIC=1 is explicit; the release/package path refuses a tag without a full GREEN ≤24h against the candidate's recorded artifact identity. Why new: CLWX-90 is Ready (the gate exists); CLWX-102 covers only the e2e red baseline; no card covers the reproduced false-green (scripts/ga-gate.mjs:82 counts only FAIL rows) or the unenforced 24h rule.
2. **Monday demo dress rehearsal + DEMO_RUNBOOK_2026-09-08 (MISS-1).** Acceptance: one continuous timed rehearsal of all three demo moments on the actual demo build+machine+account; runbook (pre-warm attach, reset sequence, fallback order) committed; the two backstop clips staged as named demo assets. Why new: GA_DEMO_READINESS_ASSESSMENT_2026-09-05.md recommends P2/P3 but neither was carded; the only runbooks are dated 2026-05-26/29.
3. **Mac RC cut + install + manifest publish (MISS-2).** Acceptance: `pnpm package:mac:local` (or signed) at moe.19+, installed on the dev/demo Mac, `pnpm release:manifest:publish`, then v2-chatbot-e2e + one read_pdf turn + one cron reminder green on the INSTALLED app. Why new: no Mac installer newer than moe.10 exists; the ask lives only in CLWX-43/85 comments, not as a card.
4. **AI Tool Usage Agreement conformance review (MISS-3).** Acceptance: FACTS-vs-ANALYSIS memo mapping each clause of 'AI Tool Usage Agreement MPAAI↔MoE.pdf' to the real data flows (Google AI / Bedrock / Cloud Run broker egress, WhatsApp liaison capture, on-device credentials, public repo), conflicts flagged to the owner and fed into the KR8 working-session agenda. Why new: zero board cards mention the one stakeholder document with contractual force.
5. **20-principal rollout plan + revised schedule answer to Raj (MISS-5).** Acceptance: carded plan covering distribution with auto-update OFF, per-laptop signed-in Chrome setup across 7 districts, onboarding checklist, support/escalation channel, fix-shipping cadence; plus a dated revised-schedule note to Raj (closeout draft 08). Why new: CLWX-8 covers only production login + support logging; nothing covers cohort logistics, and the go-live date silently expired.
6. **Forms corpus inventory + source-PDF vault (MISS-6).** Acceptance: committed inventory (name, fields, priority) of Raj's full forms Drive corpus with a schema backlog card per form; the two source PDFs vaulted with hashes. Why new: only a follow-up comment on CLWX-41 exists; CLWX-62/CLWX-7 cover the two already-cloned forms only.
7. **Email roadmap review of AI_Email_Management_ideas.docx (MISS-7).** Acceptance: every ask in Raj's 06-27 docx extracted and dispositioned (build/defer/decline) with the disposition relayed to Raj. Why new: all open email cards (CLWX-61/46/58) derive from internal defect mining, not his feature document; no card references the docx.
8. **KR3 offline lane-G re-run on a current packaged build (MISS-8).** Acceptance: network blocked, on-device channel, local doc read + grounded answer, zero non-loopback egress — on a build ≥ moe.16; until then the KR3 scorecard box is annotated evidence-stale. Why new: KR3's checked evidence predates moe.11; no card tracks the re-proof (checklist item V11 executes it, this card owns it).
9. **DOCSEARCH: reproduce using the guided-test repro, or disposition (F13 + G6b).** Acceptance: run Karunesh's document-search opener from docs/KARUNESH_GUIDED_TEST_2026-09-03.md:128 — two consecutive in-app enumerations compared against a filesystem manifest (nested files included), exact set equality; else CANNOT-REPRO disposition with the ask documented. Why new: the only Raj-lineage defect with neither card nor disposition; GPT-6 showed a repro already exists in-repo, contradicting the wait-for-tester framing.
10. **RAJ-2 content-fidelity fixture in the recurring gate (GPT-6 F4).** Acceptance: seeded meal-preference + shirt-size messages with overlapping names; installed-app summary/draft preserves every person→preference association, quantity, negation, and requested action; association-swap mutations fail; wired into ga:gate. Why new: CLWX-34 is Ready (dispositions done); the fidelity check exists only as a standalone n=1 script outside any gate.
11. **Minister / 2000-laptop ask disposition (MISS-9).** Acceptance: an explicit on-record disposition — a card (in-scope/post-GA) or a one-line closeout note classifying the 2026-04-29 ask (video-project side or superseded). Why new: dated inbound ask with no card, no disposition, absent from the closeout pack.
12. **Recorded-evidence integrity: frame-gate the chat-turn driver (companion to CLWX-49/56 — only if those stay Backlog past the demo).** Acceptance: zero-frame runs exit non-zero; only frame-gate-passing runs may register clips. Why conditional: CLWX-49 and CLWX-56 DO exist (Backlog) — this is a pull-forward flag, not a new gap; listed here so §5 is honest that the demo-week evidence risk is already carded but unscheduled.

---
*Compiled 2026-09-06 by the stakeholder-gap synthesizer (4 chronological mappers + GPT-6 codex-exec adversary + completeness critic). Ledger: 90 rows (§1). Adversary findings: 16 (§2). Critic rows: 9 + declared blind spots (§3). Sources as cited inline; nothing invented.*






