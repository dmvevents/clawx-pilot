# CLWX-135 — moe.26 OpenClaw lifecycle blocks Gateway startup

Status: confirmed installed regression; urgent release blocker. Owner: Claude Fable 5/Bedrock author in `/private/tmp/clawx-moe26-lifecycle-20260908`, branch `fix/moe26-lifecycle-gate`, base `8bb7a77907faf35fb8fc4136073ce340ea9a9aaa`. Root owns Windows/cloud operations, integration and Plane. Related: CLWX-106/107 package verification, CLWX-125 startup, CLWX-133 release receipt. Core lifecycle and fixture repairs independently approved; moe.27 native installation passes but upgrade startup FAILS because old runtime files survive replacement. The installer repair is active; independent/native validation remains pending.

## Reproduction and impact

On September 8, 2026, install the exact moe.26 artifact from hosted run 34256868050 over the original GCP Server 2022 standard-user QA profile. The normal assisted installer completes; installed ASAR `a81d7dd0cf030df2ab934068aaacf8d5d9edcb70d984c2a07dce72854b7c62bd` and EXE `44ff6a6f90fc5872b38626f544e7f19166185ecf2e5bb607f66dc3ddf386f902` match the verified manifest. Launch the installed Desktop shortcut at 18:45:23.501UTC. CDP 9224 and renderer become available, but Gateway never becomes ready and the chat composer stays disabled through the 360-second observer.

Expected: prepared packaged backend reaches stable readiness and an ordinary Online prompt completes. Actual: Gateway repeatedly exits with code 1; the app retries and remains unavailable. `first-startup-readiness.json` finishes `NOT_READY`; observer native result 1, last sample 359885ms. This result is distinct from the installer observer's missing native exit, documented on CLWX-25.

The first root observation treated the delay as still unresolved startup, comparable to the previous slow build. Subsequent Host API error and stderr establish a real runtime failure. No chat, Outlook or Forms turn was dispatched on the failed candidate. Root requested a graceful app quit from the verified idle QA renderer to end repeated retries; no broad process kill or marker deletion was used.

## Confirmed evidence versus hypotheses

Gateway stderr repeatedly states:

> OpenClaw package postinstall did not complete its lifecycle marker

Immediate cause is HIGH confidence: `openclaw.mjs` checks `.openclaw-lifecycle-pending` or the legacy install guard before normal CLI startup; `completePendingPackageLifecycle` runs pre/postinstall then throws if a marker remains. Native Windows and the immutable extracted package both retain `.openclaw-lifecycle-pending` (SHA256 `52d26753462488ad21852bc6718e21b84835f53765304d0a1e1b89d05a2a71b1`); the legacy guard is absent. Native postinstall source hash `7e1f81b571636d0f0102ff7120b5de54973a12e1ece8044fe6f608efaf49f061` matches the extracted source.

Artifact comparison explains the changed backend boundary: working moe.25's extracted package is OpenClaw 2026.4.23 with neither marker; moe.26 is OpenClaw 2026.9.2 with the pending marker. Their launcher and postinstall hashes differ. This is not evidence of a Microsoft login or cloud-model credential failure. Why the lifecycle invocation returned without clearing the marker remains UNKNOWN until reproduced; no disabled-postinstall environment override was found in the inspected app source. Direct invocation detection, Electron child execution semantics and package preparation are investigation paths, not established causes.

Native build 2309 tests and all 9 artifact harness rows passed. Their pass did not establish the actual installed launcher lifecycle. The corrective gate must exercise that boundary and reject a pending/incomplete artifact; do not weaken the upstream guard or delete a marker on the installed machine and relabel moe.26 as accepted.

## Repair and acceptance

1. Preserve immutable moe.26 payload and runtime evidence. Reproduce using an isolated fixture copy; do not execute mutating lifecycle scripts against the evidence package or shared node_modules.
2. Trace bundle preparation and actual launcher/child environment against the working artifact. Add the required backend communication task spec before changing owned packaging/runtime code.
3. Complete required lifecycle during package preparation and verify the resulting package, with a pending-marker negative control and actual launcher execution. Repair the narrow owning boundary; no user-side dependency installation workaround.
4. Focused regression checks and applicable lint/typecheck/harness, then independent review. Record source versus native evidence separately.
5. Build one corrected candidate with exact source and hashes. Repeat standard-user shortcut readiness and ordinary chat, then required Microsoft/document/local/recovery/client/stakeholder acceptance. Original moe.26 remains a failed release candidate.

## Evidence and next action

Private root receipts: `artifacts/windows-vm/20260908-moe26/`, especially `gateway-failure-redacted.log`, `gateway-failure-private-tail.json`, `startup-terminal-and-lifecycle-markers.json`, `first-startup-readiness.json`, and quit request/readback. Original payload: `/private/tmp/clawx-moe26-run-34256868050/extracted-app/resources/openclaw`; comparison: `/private/tmp/clawx-moe25-run-34203201042/extracted-app/resources/openclaw`. Raw logs may contain secrets; use only the redacted excerpt for author/reviewer input.

Active supervised task: `moe26-lifecycle-author`, receipt directory `artifacts/ga-fable-20260908/moe26-lifecycle-author`. The independent durable-controller review is a separate operations lane and cannot satisfy this product blocker. GA remains RED.


Native read-only diagnostic (18:59:15UTC) used the installed Electron42 binary as Node with explicit `ELECTRON_RUN_AS_NODE=1`: native exit0, Node24.15.0, environment flag retained, and upstream postinstall's direct-invocation predicate returns true for its exact file path. It did not invoke the product launcher or package cleanup. This rules out a blanket claim that this Electron binary always drops that flag or cannot recognize the postinstall path; it does not explain the actual lifecycle child failure. The author worktree retains `ROOT_NATIVE_OBSERVATIONS.json` privately. Graceful quit readback at18:56:45UTC found no app processes or13210/18789/9224 listeners; QA Chrome PID2748, Session2 remains.

## Author repair checkpoint — 19:22 UTC

Claude Fable5/Bedrock completed source `99e672cb0eec12ea3bfb87b1d7b777ae37b7f6be`. It reproduced the install-side cause: OpenClaw9.2 includes the pending marker in its package, while the project omitted `openclaw` from pnpm10 `onlyBuiltDependencies`; its required postinstall was ignored, and bundling copied the unfinished package. The nine-row harness loaded staged runtime modules without exercising `openclaw.mjs`, so it missed this entrypoint guard. The observed failure of native startup self-finalization remains distinct from the inferred child-environment explanation; the explicit Electron-as-Node diagnostic above still applies.

The repair approves the upstream package scripts, completes any still-pending lifecycle in the owned build copy before extension-dependency mirroring, and rejects a pending final bundle. The verifier exercises the prepared shipped launcher and an isolated incomplete-lifecycle negative control. It refuses a pending input before execution, preserving the artifact instead of repairing it during verification. Local source proof:10 focused lifecycle tests plus11 harness-spec tests, script typecheck/focused lint, bundle verification and all9 fast artifact rows PASS; a planted marker is rejected and retained. These are author receipts, not installed proof.

Independent Claude review `moe26-lifecycle-review` is running in `/private/tmp/clawx-moe26-lifecycle-review-20260908`; its verdict is pending. Root is running a separate focused native Windows check on auto-d before any new hosted build. Author accidentally included the sanitized private `report.md` in its local source commit; exclude it from the publishable candidate. No new installer or card promotion yet.

At **19:25:19 UTC**, root independently ran the10 focused lifecycle tests on auto-d Windows Server2022 using native Node24.20.0 and actual OpenClaw2026.9.2:10PASS,0FAIL,0SKIP, native exit0. Both changed source/test files were SHA-bound to99e672cb and executed in a new isolated harness directory with the existing native dependency set. Both real-launcher controls ran. This is focused Windows package proof, not a complete candidate checkout or installed result. Private receipt: `artifacts/ga-fable-20260908/windows-lab/native-lifecycle-99e672cb/native-focused.json`.

## Independent review — changes requested

Reviewer task `moe26-lifecycle-review` finished with REQUEST_CHANGES on99e672cb. It independently passed10 focused tests, lint/script typecheck, the prepared bundle launcher and mutation/refusal controls, and confirmed the core build-time repair. F1: the new task spec omitted required `comms`; actual `harness validate` failed even though generic harness-spec units passed. F2: one fixture comment still claimed Electron never runs scripts; qualify this as a modeled failure shape. F3: the private report was tracked in the unpublished author commit; preserve it privately and remove it from publishable history. No secret was found in that report.

The existing author session is resumed as `moe26-lifecycle-correction`, limited to these three findings, actual task validation/dry-run and an amended unpublished commit. Runtime implementation changes are not requested. Independent recheck follows the correction; original99e672cb and the failed review remain preserved locally. These findings do not invalidate the scoped native10/10 behavior proof, but prevent integration until resolved.

## Approved replacement candidate — 19:37 UTC

Author amended99e672cb to `7ea6969c29993f5eda99e55d5fe9366c355505a7`; independent recheck APPROVE confirms F1–F3 resolved, fresh task validation/dry-run passing, no executable-code delta and private report absent from candidate history. Root integrated as `d343f23d3b1d60bb5fb2dc1941ae277af7713d7b` (moe.27), verified tree equality to reviewed source except app version, and pushed only `release/moe27-lifecycle` to the pilot remote. Original failed source/review/private report remain retained locally. [Hosted build34270069669](https://github.com/dmvevents/clawx-pilot/actions/runs/34270069669) began19:37:43UTC with keyless-public profile and no publication inputs. Full native preflight, installer and installed recovery are pending.

## Replacement preflight failure — 19:43 UTC

Run `34270069669` at `d343f23d` failed before runtime preparation or packaging: 2,317 tests passed, two failed and 57 skipped. Both failures are `tests/unit/clwx92-bundle-fixture.test.ts`: its explicitly copied isolated checkout does not include the newly imported `scripts/openclaw-package-lifecycle.mjs`, so the verifier exits with `ERR_MODULE_NOT_FOUND` before either PDF assertion. This is a confirmed integration-test fixture omission introduced by the new verifier dependency. It does not prove a new installed failure; no moe.27 installer was produced. Earlier focused lifecycle tests did not cover the neighboring full-verifier fixture, exposing a validation-selection gap.

Repair owner: supervised Claude Fable 5/Bedrock task `moe27-fixture-author-retry`, isolated `/private/tmp/clawx-moe27-fixture-20260908` at d343f23d. Scope: repair the fixture's complete verifier/launcher contract, retain production guards and positive/negative PDF checks, reproduce before editing, then focused lifecycle plus PDF tests, independent review and root native verification before a replacement build. Related CLWX-106 and CLWX-133 record this same failure; original failed run remains immutable. Private logs: `artifacts/ga-fable-20260908/moe27-build/failed-preflight.log` and `preflight-failure-summary.txt`.

The first correction CLI launch failed before spawning because its wrapper path was relative to a different working directory. No model work ran in that attempt. Root retried with absolute wrapper/prompt/output paths; the failed supervisor receipt remains retained. Do not treat process launch as implementation acceptance.

At 19:56:36 UTC, root's Windows after-test passes **16/16, zero failures/skips, native exit 0** at author source `8bdbf91aad34b6215c9617089418f1a9f373e78b`. The same native environment reproduced **14 passed/two failed** at d343f23d at19:53:09UTC. This uses the full SHA-verified d343 source archive plus its complete two-file correction diff and existing native Node24.20/OpenClaw9.2 dependencies. Both PDF verifier cases now reach their intended assertions, and all ten lifecycle tests still pass. The fixture also needed real upstream launcher/lifecycle files behind the original missing-helper error; those files are copied from the actual dependency, without modifying it. Production code is unchanged. Independent Claude review is running; no installer or installed recovery is claimed. Private before/after and file identities: `artifacts/ga-fable-20260908/windows-lab/native-moe27-fixture/`.

## Reviewed fixture correction integrated — 20:01 UTC

Independent Claude review APPROVE at8bdbf91a: six fresh PDF-verifier tests, focused lint/task validation, pending-package marker-retention control and weakened-launcher falsifier all pass; production files unchanged. Reviewer typecheck could not complete until the generated extension bridge exists in its clean checkout; full hosted preflight remains required. The review's early native-after-pending note is superseded by root's16/16 native receipt above. No private report is in candidate ancestry.

Root integrated the exact reviewed tree as `4c023f7ea3d16f4da0205308e93839ac08be26b9` with a normalized Lore commit, preserving both author/reviewer refs and all failed evidence. Version remains0.4.3-moe.27 because the previous attempt produced no installer. [Build 34272395270](https://github.com/dmvevents/clawx-pilot/actions/runs/34272395270) started20:01:49UTC from the clean pushed pilot branch with keyless-public profile and no publication inputs. Next: successful full preflight/package, artifact hashes and absent lifecycle markers, normal standard-user install with40-minute observer budget set before start and captured native exit0, then shortcut readiness and ordinary chat. Local scripts are prepared; no replacement guest install has run.

## Corrected installer verified — 20:19 UTC build

Build34272395270 at4c023f7e succeeded20:19:37UTC:2,319 native tests passed,0 failed,57 skipped, all9 artifact harness rows and the new actual-launcher controls pass. The build explicitly reports package lifecycle already complete because package scripts ran at install. Downloaded artifact verification passes36 checks and172 compiled files against the native receipt; both lifecycle markers are absent in extracted OpenClaw2026.9.2. Installer SHA256 `a5d080fa02ffe582652f05dc4459bbc439c131a48d44ca3c0f1395edd9ab38f3`,476769020 bytes; ASAR `83e82f2850d13f97bbac40e6533600111e6023cec4d5ae1ede462420de0986eb`; EXE `2749c2fe080a8370690b0968d9c00f355f57cce9129753f6b7238a8cc57c70b6`. Manifest published=false; no user-side postinstall workaround or marker deletion was applied.

Root is transferring this identified installer through the existing private uniform-access bucket for the original Windows QA profile. Actual installed readiness/chat remains pending; this artifact pass does not close CLWX-135. Native installer creation took399s versus391s on the preceding successfulmoe26 run; no material installer-stage slowdown is indicated by these two samples. Original failure and all intermediate attempts remain preserved.

At20:27:32UTC the original Windows guest verified the exact installer SHA and476769020-byte length through its existing workload identity. Standard-user taskClawXMoe27Install verifiedPT40M before start; backups of61 app-data and634 OpenClaw files all match, restricted to the QA account. Installer PID6616, created20:28:49UTC, matches the identified staged executable in Session2. The observed license and per-user location screens were reviewed; Install was invoked at20:34:58UTC and its UI task completed0. A20-minute Finish observer runs separately with40-minute task budget. Native installer exit and startup remain pending. NSIS buttons report as UIAutomation Panes here; their observed names/IDs, native Button class and check state are used rather than relying on ControlType alone. No user data reset or automatic second installer run occurred.

At 20:49:10 UTC, moe.27 native installation completed with exit 0 and exact installed EXE/ASAR hashes matching run 34272395270. The main installer task also completed with result 0. The observer budget fix therefore supplies the previously missing native-exit evidence. Run was verified unchecked before Finish at 20:48:19 UTC. First launch used the actual standard-user desktop shortcut at 20:49:48 UTC, with the existing profile and Chrome session retained. Startup/chat acceptance remains pending. Private receipts: `native-install-exit-bounded-wait.json` and `first-shortcut-readiness-observation-2050.json` under `artifacts/windows-vm/20260908-moe27/`.

## Moe.27 installed upgrade failure — stale runtime survives (20:55 UTC)

This is a distinct second boundary failure. Native installer exit 0 and the new EXE/ASAR hashes are real passes, but do not establish complete loose-runtime replacement. At 20:52:03 UTC, installed `resources/openclaw/.openclaw-lifecycle-pending` still existed, 8 bytes, created 18:26:45 UTC and modified 17:40 UTC; SHA256 `52d26753462488ad21852bc6718e21b84835f53765304d0a1e1b89d05a2a71b1` matches moe.26. The extracted moe.27 package contains no marker. Installed `openclaw.mjs` and `package.json` hashes match the new payload. This establishes old/new runtime mixing during upgrade. The exact installer cleanup branch remains under investigation; do not infer a specific lock, permission or UAC failure without proof.

The first shortcut observer ended `NOT_READY`, task/native result 1, 159 samples, last sample 359321 ms. Gateway stderr after this launch repeatedly reports the lifecycle error and code 1. No ordinary chat, inbox, Forms or document journey was dispatched. Root used the verified idle renderer's graceful quit at 20:56:16 UTC; the quit task completed 0, app/listeners were absent at 20:57:11 UTC, and all 11 QA Chrome processes remained. The original pending marker and failed payload are preserved; no manual marker removal or profile reset was applied.

Private evidence under `artifacts/windows-vm/20260908-moe27/`: `installed-lifecycle-marker-investigation-2052.json`, `gateway-lifecycle-redacted-2055.json`, `first-shortcut-final-result.json`, `native-first-startup-readiness.json`, `native-first-shortcut-launch.json`, `native-install-result.json`, and `graceful-quit-and-preservation-result.json`. Raw Gateway logs are private and may contain credentials; only the fixed-message redacted JSON is suitable for another agent. A first SCP attempt failed on the spaced source path; copying a read-only snapshot into the existing private QA run directory supplied a safe, successful transfer without changing the source log.

Claude task `moe27-upgrade-author`, session `8cf8dcb0-cb83-43f7-9b20-7a1fa9438095`, owns the narrow installer repair at `/private/tmp/clawx-moe27-upgrade-20260908`, base `4c023f7e`. Root owns native tests and board; independent review follows. Required additional acceptance: reproduce an arbitrary stale runtime sentinel surviving the baseline; successful corrected upgrade replaces the owned runtime completely while preserving external profiles/siblings; locked or failed cleanup cannot claim successful installation; then re-run native installer, actual shortcut startup and ordinary chat on the identified candidate. A fresh install alone cannot close this upgrade regression. Existing GA/client/Microsoft criteria remain unchanged.

### Confirmed directory-predicate defect (21:02 UTC)

The real NSIS 3.0.4.1 probe on the existing Windows installation returns `directory=present`, `trailing=absent`, `wildcard=present`, native exit 0. The current installer uses `IfFileExists "$INSTDIR\"` before replacement and similarly tests stale/skills directories. That trailing-backslash test misses a directory that exists, so the replacement branch can be skipped. This supersedes treating the existence check as an untested hypothesis. [NSIS documents the directory wildcard form](https://nsis.sourceforge.io/Reference/IfFileExists); [Microsoft documents the trailing-backslash failure](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-findfirstfilew). The preceding Win32 control independently returns success for the directory and wildcard, error 2 for trailing-backslash only. No installed file was changed by either probe.

Private exact probe source/executable and receipt: `native-nsis-directory-pattern.nsi`, `.exe`, and `native-nsis-directory-pattern-result.json` in the same moe.27 evidence directory. The first local compiler invocation lacked `NSISDIR` and produced no executable; the successful attempt sets it to the existing pinned compiler root. The first Win32 control built a malformed PowerShell array; its five-row output is excluded, and the corrected three-row probe is retained in `native-installer-directory-pattern-control-corrected.json`. These operator failures are distinct from the confirmed product defect. Root has not modified the installer source; the author still owns the reviewed repair and behavioral upgrade/locked-path harness.

### CLI execution checkpoint (21:40 UTC)

The first author session ended `TIMED_OUT` after 903.95 seconds (SIGINT, 44 tool calls), with no source diff or accepted patch. A child exit 0 did not make the error result a success. Final usage confirms Fable 5 / Bedrock. The transcript shows repeated flag-definition discovery; root preserved it and resumed the same inspected session with the confirmed native cause and a smaller source-only deliverable. `moe27-upgrade-author-resume` owns the installer/task spec; independent `moe27-upgrade-test-author` owns `tests/windows/nsis-upgrade/` and its task spec. Their interface is the actual `ClawXPrepareInstallDirectory` macro and exact `ClawXStaleInstallDir` rollback path. Review and VM execution follow the source/harness pair; neither author approves its own work.

Root verified auto-d identity 4908385059495321872, authenticated access, 8 CPUs/32 GiB and 75.2 GiB free at 21:40:45 UTC, and created only `C:\Users\clawxlab\clawx-dev\nsis-upgrade-20260909` for synthetic native fixtures. No installed application, customer profile, browser or cloud configuration changed. Private native receipt: `artifacts/ga-fable-20260908/windows-lab/native-moe27-upgrade/native-upgrade-stage-preflight.json`. Final installer/native acceptance remains pending; GA stays RED.

### Source checkpoint and draft harness interface control

The resumed source author completed clean `2841f8c0` with compiler/task-spec checks. Final CLI receipt confirms Fable 5 on Bedrock, native process exit0, success result; independent source review `moe27-upgrade-independent-review` is now running. The author flags an elevated UAC inner-instance path that may bypass the current pre-extraction hook; the reviewer must establish coverage before approval. A source compile is not installed proof.

Root compiled the test author's in-progress fixture against the actual production macro before Windows execution. It failed with `ClawXPrepareInstallDirectory requires 0 parameter(s), passed 2` (compiler exit1). The draft fixture passed explicit arguments while the agreed macro consumes `$INSTDIR` and the executable define. This is a test-interface defect, not a native Windows result. Private receipt: `artifacts/ga-fable-20260908/moe27-upgrade-test-evidence/root-interface-check.json`. The test author retains ownership; final actual-source compilation, independent safety review and native execution are still required. No test pass or card promotion is claimed from a stand-in macro.

The first test-author run subsequently reached its 900-second deadline (902.73s receipt, 32 tool calls, error result despite child exit0). Partial source, compiler outputs and file hashes are retained privately. Root resumed the same session as `moe27-upgrade-test-resume`, bounded to actual zero-argument macro integration, missing destination cases, real lock refusal, exact stale-sibling collision checks and completion evidence. No Windows harness execution occurred on the defective draft.

### Independent rejection and native scan-failure correction

Independent review of `2841f8c0` returned REQUEST_CHANGES: B1, the enabled elevated UAC inner instance skips `customCheckAppRunning` and therefore the only replacement call; `perMachine:false` does not remove all-users/elevation choices. B2, `FindFirst` failure is cleared and treated as an empty destination, allowing old/new payload mixing. `bc561eb3` adds preparation to both unconditional post-uninstall hooks, retains the exact rollback pointer across calls while rechecking the destination, rejects scan failures, checks creation/attributes, and corrects recovery messages. Compile and actual task-spec validation pass; independent correction review remains running. No integration/build approval is inferred.

Root confirmed B2 on the original Windows Server2022 VM under **standard QA user ClawXFresh0908** using a fresh synthetic Downloads folder and the actual NSIS macro. At **22:07:09 UTC**, source2841 reports native enumeration denied, but exits0, copies the new payload and leaves the arbitrary old sentinel. At **22:10:26 UTC**, frozen sourcebc561 under the same denied-listing condition exits2; only `prepare-start` is reached, old sentinel remains, and no new payload is copied. ACL restoration passes in both; observer tasks finish0 independently of the fixture's expected nonzero exit. This is an installed-OS filesystem regression test, not an installed application or fresh Windows client pass.

Private proof: `artifacts/windows-vm/20260908-moe27/acl-standard-control-readback-v1.json` and `acl-standard-after-readback-v1.json`; frozen source/NSIS wrapper/compiler/executable identities in `artifacts/ga-fable-20260908/moe27-upgrade-test-evidence/acl-before-native-enumeration-2841f8c0/` and `acl-after-bc561eb3/`. The helper registers bounded Limited/Interactive tasks; the app, Chrome and existing user data were not targeted.

Controls and failed attempts remain explicit. At21:58:33UTC auto-d's owned child-file handle without Delete sharing blocks parent rename (HRESULT-2147024891), preserves the sentinel and releases the handle. The same operator account's deny-listing ACL did not deny .NET or native NSIS enumeration; those two attempts are excluded as ACL-negative proof. The first diagnostic-wrapper generation matched a comment as well as the invocation, failed its assertion and produced no executable; the subsequent transfer failed. The corrected generation anchors the actual invocation and completes dependent compile/transfer steps sequentially. These operator errors did not execute the failed control or mutate installed files.

The independent harness author completed `1ed5e30f`: eight scenarios, actual macro compilation, real lock-refusal control, sibling/profile and rollback checks, and structured failure evidence. All full-suite native cases remain NOT_RUN. `moe27-upgrade-harness-regressions` now adds denied-listing and actual post-uninstall/repeated-hook coverage using the proven standard-user pattern. `moe27-upgrade-correction-review` reviews sourcebc561 and the eight-case harness in parallel; final committed additions require a bounded delta review before the full native run.

### Rollback cleanup correction and final validation checkpoint

The second independent review resolved B1/B2/N1 and approved the existing eight-case harness for root execution, but raised blocking B3: `cmd.exe` expands `%VAR%` even inside a quoted rollback path and can redirect recursive deletion to another sibling. Root's read-only Windows echo control at **22:14:52 UTC** changes `literal%USERNAME%._stale_0` to `literalclawxlab._stale_0`; no deletion ran. `c5c8590b` removes delayed rollback deletion and retains the exact previous tree with a disk-space notice. Preparation/hooks executable behavior is unchanged frombc561. The retention author wrote the small patch but repeated solved compiler-flag discovery; root verified the exact owned PID/parent/group, interrupted it, and finished the existing NSIS wrapper and task-spec checks before committing. Its CLI_FAILED/interruption receipt remains preserved; this is not a successful author-run claim.

Harness `6a6a4268` extends1ed5e30f to eleven cases: actual inner-only hooks, repeated hooks with exact pointer retention, and ACL-denial rejection. Its final receipt is **MODEL_MISMATCH**: initial Fable5 work was followed by Opus5 checks/commit/report; final usage identifies both models on Bedrock. No alias equivalence or fully Fable-only run is claimed. A fresh pinned `moe27-upgrade-final-review` reviews the actual final deltas before acceptance. The earlier mismatch is retained even if fresh validation later succeeds.

Frozen review assembly `b8423f22` combines sourcec5c8590b and harness6a6a4268. Root actual-source fixture compile passes at22:25:30UTC; executable SHA256 `aa98d17c7339d5dd3953f7d57b0ca70f48c7a3afaee3c832f5043f162f73ab22`. The runner SHA256 `8fc9cc0ac7f47cc08e19be3ff54864e6d88663d69496e8634ce896aed85c0efd` parses with zero errors in native Windows PowerShell5.1.20348.5499. Source/fixture identities, compiler output and staged observer live privately under `artifacts/ga-fable-20260908/nsis-upgrade-final-b8423f22/`. Staging is complete; the full native suite has not started and remains gated on final independent safety approval. [The canonical lab procedure](../testing/WINDOWS_REPEATABLE_LAB.md#native-installer-upgrade-regression-clwx-135) documents the standard-user requirement, eleven cases and literal-path operator cleanup after installed acceptance.

### Native harness target mismatch — September 8, 22:33 UTC

Fresh final review APPROVE with final-usage Fable 5 / Bedrock proof resolves B3 and approves the eleven-case harness for a standard-user run. Root started the hash-bound assembly `b8423f22` fixture under `ClawXFresh0908` at22:33:32.669 UTC. The first case selected an OS-confirmed unmapped `Q:\` root; fixture native exit4 recorded only `target-mismatch`. The `macro-was-reached` assertion failed, suite native exit3, task result1, no timeout, streams closed, and ten remaining scenarios were NOT_RUN. The suite stopped safely; this is a harness failure before product execution, not a source regression result. The original failed moe.27 app and Chrome profile were untouched.

Receipt: `artifacts/windows-vm/20260908-moe27/nsis-suite-b8423f22-readback-v1.json`; exact scenario output is retained in the sibling root-mismatch detail receipt. Target normalization/argument handling is under diagnosis, not yet a confirmed cause. CLI `moe27-upgrade-target-fixture-fix` owns only the existing harness and its task spec in `/private/tmp/clawx-moe27-upgrade-tests-20260909`. Keep production source unchanged; retain the actual macro rejection assertion, unmapped-root boundary and honest safety stop. Exit: reviewed minimal harness correction, new fixture hashes and eleven native outcomes, then source integration and a new identified installer. No blind replay or accepted target-mismatch fallback.
