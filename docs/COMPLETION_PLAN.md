# Completion plan

Reconciled 2026-09-07 against source **58d04eb0**, the September 7 board export, Claude project JSONL and read-only stakeholder messages. This is the current execution order; dated sprint plans and the state vector remain history. Update this file when evidence changes the next outcome, not after every tool call.

## Independent assessment

**Native Windows preflight:** the first moe.21 hosted attempt on reviewed public source `7cfad9bd` failed before producing an installer. The shared CRLF loader cause was reproduced, and checkout/subprocess repairs passed a real autocrlf checkout plus 55 focused checks. The [runtime report](evidence/WINDOWS_RUNTIME_RECOVERY_2026-09-07.md#native-windows-preflight--september-7) records the evidence; the second native run cleared those failures, with 199 files passing and only two five-second integration-test timeouts remaining. Those cases now have bounded execution budgets; another native build and installed acceptance remain required.

The project has useful working capabilities and real fixes. The main failure is incomplete integration: source fixes, test verdicts, installed artifacts and stakeholder outcomes are treated as if they were interchangeable. Repeated card-sized work cannot close that gap unless it continues through an installed user journey.

**Readiness: RED for an unrestricted pilot/GA claim.** The latest static report is green only within its limited scope. Do not describe this guidance reconciliation as a product fix or release approval.

| Evidence at this snapshot | What it establishes | What it does not establish |
|---|---|---|
| [September 7 gate run 5](evidence/GA_GATE_2026-09-07_run5.md): 7 pass, 0 fail, 3 skip | Source hygiene and selected tooling checks passed | Renderer E2E, live email/forms and VM rows were skipped; no full acceptance verdict |
| [moe.19 manifest](release-manifests/0.4.3-moe.19.json): generated September 6, `published:false` | A recorded Windows artifact exists; stale Mac moe.10 trees were excluded | September 7 runtime fixes are not thereby present in that installer; manifest lacks source-commit provenance |
| [September 7 Windows VM continuation](evidence/WINDOWS_VM_TESTING_2026-09-07.md) | After owner reauthentication, the existing VM was started; authenticated SSH, RDP protocol and closed guest-port control passed. Installed moe.19 app hash matched, Office round-trips and the Electron bridge passed | Reused Server 2022/admin evidence; startup showed a deferred Gateway restart and prolonged reconnecting. No new install/current-source, client Windows, tenant or microphone acceptance |
| CLWX-123, [state-vector evidence](GA_SPRINT_STATE_VECTOR.md) under “sixteenth tick” | Four live evals agree: 16 pass / 1 fail / 1 skip; draft recipient assertion fails 4/4 | Not a random eval failure; exact DOM cause is still a hypothesis |
| CLWX-95/96 and CLWX-117 | Channel/run-lifecycle failures remain open; local turn can stall at `sessions_yield` | A changed channel chip or acknowledged file write is not proof of the executing model or a terminal answer |
| Karunesh, September 3–4 WhatsApp | Document interactions improved; email attach still failed after the Chrome MCP install | Dev-machine browser success does not resolve his fresh-machine regression |
| Board: 123 cards, 62 Ready, 13 In Progress, 8 Todo, 28 Backlog, 12 Cancelled | Work inventory and recorded dispositions | Card counts do not measure usable product completion |

## Why the iterations are not converging

1. **Conflicting entrypoints.** Before this reconciliation, `AGENTS.md` started with June guidance; `CLAUDE.md` mixed September with May defaults; `CURRENT_WINDOWS_RC.md` called June moe.10 current. The state vector had 2,316 lines. Agents could obey different “current” instructions.
2. **Work selection rewards small available tasks.** The scheduled Claude prompt requests one atomic/high-leverage card step and a static gate pulse. The September 6–7 transcript records 1,158 Bash calls and 396 Edit calls. Those counts show activity, not wasted time by themselves; the missing installed acceptance is the material evidence of non-completion.
3. **The test oracle has failed alongside the product.** The repo records false greens from tunnel TCP acceptance, a chip mistaken for an answer, and note-text-based verdicts. Recent typed verdict extraction and negative controls are valid corrections. They still need a release gate that cannot pass while required lanes are skipped.
4. **Ownership breaks across layers.** Runtime configuration and active-run state can disagree. Outlook's fill and assertion paths disagree over the same recipient field. Fix the owner contract and shared interpretation, then test consumers; another local guard cannot prove the whole transition.
5. **Requirements are disconnected from acceptance.** The liaison route excluded Karunesh's ClawX reports by person. Fast responses, unaided email access, a current demo build, deployment scope and a continuous rehearsal need explicit evidence alongside feature tests.

### Preserve the structural fixes already present

The channel route already supports a no-restart degrade path and the app's exported `src/stores/chat.ts` waits for a session-model acknowledgement before resend. Outlook's driver already tracks tab ownership and current-view-first lookup. Packaged Gateway setup propagates `CLAWX_APP_RESOURCES`, and statutory demo defaults are operator-controlled. These are useful boundary repairs; do not restart their implementation from old notes.

Inspect the actual exported chat path: `src/stores/chat.ts` owns the running store, while `src/stores/chat/internal.ts` and `runtime-send-actions.ts` also contain a split implementation. Their consumer relationships must be traced before choosing a fix/test target; they were not proven dead by this audit. Normalize ownership after coverage, without a speculative pre-demo store migration.

The on-device trim is already active in source (`shared/feature-flags.ts`, `electron/main/local-provider-seed.ts`). The existing deny-list in `electron/utils/ondevice-tool-policy.ts` does not include `sessions_yield`. The owner hold on branch `7add864b` does not mean no trim is present; CLWX-117 still needs its specific terminal-state reproduction and installed proof.

## Stakeholder requirements verified from the source

Source: local `whatsapp-local` MCP configuration → `whatsapp-mcp/whatsapp-bridge/store/messages.db`, opened with SQLite `mode=ro`; contact identity resolved through `whatsapp.db`. No messages sent, attachments downloaded or credential links opened. Times below are the source thread's `-04:00` time. No raw message exports belong in git.

| Source locator | Requirement/disposition | Existing work |
|---|---|---|
| Raj, 2026-05-27 16:22 | Response time interfered with a demo; latency is a product requirement | CLWX-43, CLWX-47; measure and improve independently of final numeric budget approval |
| Raj, 2026-06-27 16:03 | School Code of Conduct should be available without repeatedly locating the file | CLWX-42, CLWX-115; retain version/citation fidelity and installed-app QA |
| Raj, 2026-08-29 11:37 and 2026-09-02 05:23 | Principal admin tasks: forms, email and responses; stated deployment ambition is **450 laptops**, preceded by a demo and hands-on session | CLWX-110/116; do not substitute an assumed 20-device scope or confuse the 20-video experiment with ClawX rollout |
| Karunesh, 2026-09-03 16:20 | A demo version was requested for “Monday” | CLWX-107/108. Following that message, Monday is **2026-09-07**; September 8 is Tuesday. Confirm any revised appointment from newer evidence; historical filenames are not scheduling authority. |
| Karunesh, 2026-09-03 16:22 / 21:09 and 2026-09-04 15:54 | File interactions worked; email still could not attach after Chrome MCP installation and had worked in a previous build | CLWX-73 plus fresh-box email acceptance; requires his scenario on the new packaged build |

Raj also discusses the separate video project. Classify each message by content. Earlier “2000 laptops” ceremony context is not a confirmed rollout acceptance target. Ministry APIM/Entra/Forms deployment dependencies remain in CLWX-31/7; this session did not probe those services or assume placeholder values are usable.

## Ordered completion workstreams

Keep one active engineering workstream through its exit. Independent read-only research, fixture preparation and review may run alongside it. Reuse existing cards; do not create a second competing backlog.

| Order | Outcome and owner | Concrete work | Falsifiable exit |
|---|---|---|---|
| **1 — Trust the acceptance result** | CLWX-106/90; `scripts/ga-gate.mjs`, `scripts/ga-gate-verdict.mjs`, `scripts/eval-verdict.ts`, existing eval/harness | Separate development health from demo/pilot release profiles. Enumerate required journeys/lanes. Required SKIP/BLOCKED/NOT_RUN prevents release success; missing or stale evidence cannot be promoted by prose. Include negative controls on the actual verdict module. | A deliberately missing required lane or broken fixture yields non-success. A release result identifies source revision, artifact hashes, lane, criterion IDs and dated evidence. Static-only execution is visibly insufficient. |
| **2 — Reliable email on a principal's machine** | CLWX-73/118/120/121/123 and 58; Outlook browser service + app-facing tools | Start with CLWX-123's reproducible fill/assert mismatch. Probe only redacted DOM shape; unify recipient normalization and compose identity used by fill, review and dispatch. Preserve tabs/drafts and bind confirmation to the reviewed draft. Validate attach through the product; installing a developer MCP is not the product recovery path. | New message, reply, forward and attachments either work or fail with an accurate recoverable reason. Wrong recipient/subject, multiple/cross-tab drafts and changed compose refuse dispatch. Current packaged Windows build passes the reported attach/read/draft scenario on a clean user and then Karunesh's machine; authorized sandbox dispatch proof recorded separately. |
| **3 — One coherent turn lifecycle** | CLWX-95/96/117; channel router, Gateway, chat runtime events | Trace requested channel → persisted intent → acknowledged runtime route → run events → terminal result. Choose and test one acknowledgement contract at the Main/Gateway boundary. Cover queued sends, mid-turn network loss, model switch, restart, cancellation and late events. Preserve effect identity across retries; do not replay writes on ambiguous failure. | Each accepted turn reaches answer, explicit failure or cancellation within its defined bound. UI route reflects observed execution. No duplicate write/send/submit on retry. Offline recovery and the next turn work without app restart on the packaged build. |
| **4 — Principal workflows, performance and integration** | CLWX-42/43/47/61–67/102/113–115; domain services + existing tests | On one candidate, run email → document extraction → form review, create/parse-back a useful document, answer the stakeholder policy questions and deliver/defer a visible reminder. Reconcile UI specs with approved product copy. Measure the three demo prompts on the actual demo/tester machine; expose long-running progress and cancellation. | Structured content assertions plus app observations for each journey; missing data and unauthorized submission refuse. No stale-build or mock-only substitutions. Record p50/p90 and sample size; proposed budgets remain proposals until accepted. |
| **5 — Freeze, rehearse and hand off** | CLWX-85/107/108/110; packaging/release owner | Build after the integrated fixes; bind manifest to source. Stage authorized Mac/Windows candidates and repeat required checks against those hashes. Rehearse the three demo moments consecutively on the real machine/account; prepare recovery steps and named recording backstops. Draft a realistic pilot/450-laptop deployment answer. | Installed artifacts match tested hashes; no release-critical FAIL or unexplained missing lane; unaided tester rerun and rehearsal evidence exist. Distribution and owner-reserved installation/publication actions occur only with their recorded authority. |

**Local CLWX-106 implementation:** strict gate scoring now uses fixed required criterion IDs and rejects absent/non-PASS evidence. Packaging records source before compilation, records hashes of `dist` and `dist-electron` after compilation, and checks both source and compiled outputs before/after electron-builder; direct builder publication is disabled. Missing receipts, stale entrypoints and changed compiled bytes prevent packaging through this wrapper. Each artifact retains its own recorded source, and plain rehashing cannot certify unknown or changed bytes. A published manifest's complete artifact set is frozen. Installed evidence is derived from Windows producer files, including installer/app hashes, runtime readiness, safe chat and Office output. The run binds an exact portable file inventory; Gateway acceptance output contains status metadata, with raw Gateway logs retained privately on the guest. The gate saves a portable evidence bundle and publication rechecks its observations, source and staged installer bytes. These checks are part of CLWX-106, not closure: the earlier integration checkpoint had no rebuilt candidate. The subsequent moe.20 local build is recorded in the runtime recovery report; the full candidate-bound collection/publication flow remains unverified on Windows/CI. The installed smoke now requires a redacted pre-mutation environment profile; Server/client, privilege and prior-state scope accompany the result. Source context and artifact hashes are not a reproducible-build attestation.

The current acceptance profile supports exactly one Windows x64 installer. Other platform installers require their own installed evidence profile. `pnpm release` stages outputs; it does not upload. Generic automatic Ministry GitHub/OSS publication is disabled. The owner has authorized staged GA publication after validation; the required evidence and remaining specific holds still apply.

**Evidence bundle and publication boundary:** `GA_GATE_INSTALLED_EVIDENCE=<vm-run-dir>` supplies the measured Windows run to `GA_GATE_RELEASE=1 pnpm ga:gate`. `GA_GATE_MANIFEST` and `GA_GATE_ARTIFACT_DIR` select its candidate manifest and a directory containing only the intended installer set. Existing live-action flags and authorization remain required; strict mode does not enable them. A complete run writes `artifacts/release-evidence/<timestamp>/release-evidence.json`, `manifest.json`, command logs and copied installed producer files. Keep raw evidence private; the bundle is a review artifact, not public release notes.

Before authorized publication, run `pnpm release:evidence:check --report <bundle>/release-evidence.json --manifest <bundle>/manifest.json --dir <staged>`. `release:manifest:publish` also requires `--evidence <bundle>/release-evidence.json` and validates the staged directory before changing publication state. The manual Windows workflow requires the same bundle in a `ga-release-evidence` artifact from `evidenceRunId`, the installer run ID, matching source checkout and exact tag `v<version>`. The checked-in `release-evidence-manual.yml` producer downloads the Windows installer and build provenance, runs strict acceptance with the actual Windows evidence directory, revalidates the resulting bundle and uploads `ga-release-evidence` only after success. It requires a dedicated trusted self-hosted macOS controller labeled `clawx-acceptance` with the existing live test-account/Chrome/PowerShell prerequisites; the application under test remains the Windows VM installation. A September 7 read-only GitHub API check returned zero repository self-hosted runners. Controller registration and this CI run remain pending; direct Windows VM testing has resumed independently after GCP reauthentication. SEND and NSCC remain explicit dispatch opt-ins. GitHub documents the [runner label contract](https://docs.github.com/en/actions/how-tos/manage-runners/self-hosted-runners/use-in-a-workflow) and [artifact transfer behavior](https://github.com/actions/upload-artifact). The publication workflow uploads only the checked installer and current review documents, without replacing existing assets or updating June notes.

**Next product work:** CLWX-123 at the recipient write/read/verify boundary. Local analysis reproduced exact-email rejection when the fixture exposes only a display-name chip; that is insufficient evidence of which condition causes the live failure. A redacted live DOM observation must distinguish hidden email metadata from wrong-field selection. Preserve exact recipient assertions and wrong-contact refusal. The restored VM exposed a prerequisite under workstream 3: pre-start configuration writes queue a deferred Gateway restart and the persisted on-device route has no running Ollama listener. Establish a usable packaged chat path before the installed email journey; the [Windows testing report](evidence/WINDOWS_VM_TESTING_2026-09-07.md) records current observations and the next regression scope. Physical pilot access and tenant proof remain separate. Do not restart broad bug mining or manufacture more cards to avoid a failed core journey.

Workstream 1 does not need live-account access to prove fail-closed semantics. Workstreams 2–4 have substantial local diagnosis/test work even when the final live lane is blocked. Capture blocked proof honestly and continue independent work within the plan; do not declare the product complete.

## Owner/Ministry decisions with prepared deliverables

- **Recorded authority and holds:** on September 7 the owner authorized GitHub synchronization, GA publication after completed validation, and stakeholder notification once released. The earlier publication hold is superseded. Installing over the owner's Mac app, trim branch `7add864b`, CLWX-18/19 and VM shutdown remain held. Prepare candidate evidence/reviewable changes and protect embedded credentials before distribution; authorization does not make a failed release gate pass.
- **Latency:** provide current measurements and the exact workload before requesting a final p50/p90 budget. Existing notes conflict on p90; neither 30s nor 45s is silently adopted here.
- **Production Forms/identity/infrastructure (CLWX-7/31):** consolidate the concrete destinations, delegated permissions and architecture decisions needed from Ministry. A working test clone is not production authorization. Keep the desktop product versus server/fleet deployment decision explicit.
- **Rollout/agreement (CLWX-109/110/116):** prepare scope, support, deployment stages and a factual data-flow inventory for the agreement review; do not claim agreement compliance or fleet capacity from pilot evidence.
- **External tester:** prepare the exact artifact and concise reproduction checklist; a fresh external rerun is still required for Karunesh's attach complaint.

## How to resume and stop

Read `PROJECT_CONTRACT.md`, this plan and the selected card. Inspect the actual diff. Load one domain skill. State the selected exit criterion; execute until its evidence exists or the next dependency is concretely blocked. Preserve assigned independent reviews, with no repeated reviews of an unchanged diff merely to fill a tick.

The existing local `.claude/scheduled_tasks.json` sprint task had its **prompt only** aligned to this contract and workstream order; cadence, ownership and firing metadata were preserved. A rollback copy is in `/tmp/clawx-alignment-20260907/scheduled_tasks.before.json`. No task was added, fired or cancelled. An already-running session may cache its prior task/instructions; runtime reload was not verified, so a resumed session must reread the files. The later Plane synchronization refreshed the live board, posted redacted readback-verified comments on CLWX-22, CLWX-25, CLWX-95 and CLWX-106, and moved CLWX-95 to In Progress as the active Gateway lifecycle workstream. No stakeholder was messaged.

Stop the **guidance alignment task** when entrypoints agree, latest pointers are accurate, stakeholder routing is corrected and structural checks/review pass. Stop the **product completion effort** only when the chosen demo/pilot/fleet acceptance bar is evidenced or the remaining specific decisions/access are named; source-only green does not meet that bar.

## Audit coverage and limits

- Reviewed `~/.claude.json` MCP configuration metadata, project `.claude/settings*.json`, the scheduled-task prompt and send-guard implementation. The Bash-only hook scans command text; it does not inspect native MCP sends or a payload read from a file, and an invocation ledger does not prove delivery. No hook/security behavior changed here.
- Indexed the 11 newest root project JSONL files under `~/.claude/projects/-Users-antonalexander-Github-moe-tt-ClawX/`; parsed user requests, tool counts and signal records. Main reviewed session: `f1d35eb7-1db5-44a4-b660-b557ef533bff.jsonl`, 2026-09-06 12:30Z through 2026-09-07 04:34Z, 21,515 records at scan time. Line 21216 asks for a concise handoff and retrospective. `996e9e7d-7bfc-4ed6-a7a0-a5c7fa1a6eb6.jsonl` covers August 25–September 5. Compaction summaries are secondary evidence, not new owner authorization.
- WhatsApp contact threads scanned: Raj's old May–July thread (345 records), newer July–September thread (239), and Karunesh May–September thread (221). Read relevant incoming textual requests/errors; unrelated personal/video content, media, credential-bearing messages and full outbound audit were outside this review. Latest relevant tester message found: September 4; store-wide freshness does not prove every contact is synchronized.
- Read the committed September 7 board JSON, recent commit history, current/historical plans, gate reports, release manifest and runtime-owner audit. The initial September 7 read-only Windows access probe required GCP reauth and could not determine VM status; physical pilot SSH timed out. The later owner-authorized VM continuation restored access and ran installed-app observations, as recorded below. The live Plane board was subsequently refreshed and synchronized through the project-scoped API path; the local mirror in `docs/plane-board/` reflects that board update. No new VM install, external send/submit, network change or comprehensive historical/subagent-transcript audit was performed.
- This change aligns guidance/harness specs and adds a fail-closed release verdict to the existing gate. It introduces no app-runtime fix, dependency, workflow engine or relocation of implementation folders. The three README development sections now document the gate command and pinned pnpm; principal-facing product behavior is unchanged.

## Alignment verification — 2026-09-07

- `pnpm test`: **189 files passed; 1,783 tests passed, 6 skipped**. The strict gate's 30 tests include fixed required criteria, missing/skipped evidence, override resistance and executed runner checks proving release mode does not silently enable live sends.
- `pnpm typecheck` and `pnpm lint:check`: passed; lint reports existing warnings and no errors.
- `pnpm harness:ci`: passed, including 16 harness tests, 5 direct Office checks and 61 eval checks. The eval reports 9 skips; its proxy/offline evidence does not prove installed-app or live-tenant behavior.
- Task validation and selected-flow dry run passed with `pnpm harness validate --spec harness/specs/tasks/align-release-acceptance.md --since 58d04eb0` and `pnpm harness run --spec harness/specs/tasks/align-release-acceptance.md --since 58d04eb0 --dry-run`. The baseline is the task-start commit, so the actual working diff is checked without absorbing unrelated branch history.
- Local Markdown references, edited YAML/TOML/JSON and `git diff --check` passed. The scheduled-task comparison confirms only the prompt changed; runtime adoption remains unverified.
- Separate guidance/workflow and gate-code reviewers approved the final changes with no remaining findings. This closes the alignment task only: no live gate, installed Windows/Mac rehearsal, send/submit or release was performed, and CLWX-106 remains partial as described above.

## Integration verification — 2026-09-07

Scope: the subsequent CLWX-106 source → staged artifact → installed evidence → publication integration, on the uncommitted task diff against `58d04eb0`. The preceding review file `docs/evidence/CLWX-106_release-gate_review_2026-09-07.md` covers the earlier strict-verdict change only.

- Final full `pnpm exec vitest run --silent`: **195 files passed; 1,879 tests passed, 6 skipped**. Two EventEmitter listener warnings were emitted; the run exited 0. Tests exercise synthetic Windows producer files, temporary artifact directories and mocked builder execution; they are not Windows installation evidence.
- Final `pnpm typecheck` and `pnpm lint:check`: passed, with 0 lint errors and 52 existing warnings. `pnpm lint:ps` found user-local **pwsh 7.6.5** and analyzed 52 PowerShell files: 0 gating findings, 124 warning and 13 information advisories. This supersedes the earlier assumption that PowerShell tooling was unavailable; guest execution remains untested.
- `pnpm harness:ci` passed during integration: 16 harness tests, 5 direct Office checks and 61 eval checks, with 9 eval skips. After the final code changes, task validation, selected-flow dry run against `58d04eb0`, and `git diff --check` passed. The full unit suite includes the final receipt, portable inventory and CI contract changes.
- Independent native reviewers approved the source/compiled-output receipt, published artifact-set freeze, CI evidence producer/consumer contract, and final Windows collector/adapter privacy and inventory changes, with no remaining findings in those bounded scopes. Reproductions confirmed modified ignored build outputs stop before builder execution and private extra evidence entries fail validation.
- The requested local Claude review could not run: the CLI returned HTTP 403, `Bearer Token has expired`, with zero model input/output tokens. The attempt is recorded privately in `.omx/artifacts/ask-claude-clwx106-release-review-20260907.md`; native independent review was used. Do not describe this continuation as Claude-approved.
- **At this integration checkpoint:** no real package/build, Windows installation or runtime rehearsal, live strict GA gate, CI collection, test-account dispatch or publication had run. GCP reauthentication blocked VM access at that time; the later continuation below supersedes that access blocker. The GitHub repository reported zero self-hosted runners. CLWX-106 installed acceptance and GA remain open.

## Windows testing continuation — 2026-09-07

The current [testing entrypoint](../windows-pilot/vm-testing/README.md) now covers actual IAP protocol/control checks, environment fidelity, state backup, installed-app probes and native app-window video capture with host hash/frame verification. Codex and Claude smoke skills point to the same procedure. The [dated Windows report](evidence/WINDOWS_VM_TESTING_2026-09-07.md) separates live observations, source-test validation and missing deployment coverage. VM access is restored; runtime readiness and representative acceptance are the remaining work.

## Runtime and board continuation — 2026-09-07

The live Plane API was verified and existing CLWX-95/106 are In Progress. CLWX-25 remains In Progress; no card moved to Ready or Done. The [API guide](PLANE_BOARD_API.md) records project identity, state semantics, compatibility and readback procedures. The runtime prerequisite is the active product work; CLWX-123 remains the following email boundary issue.

Source repair converges cloud/local providers and channel configuration before the first Gateway start without scheduling a deferred reload; later live refresh still works. Startup retains bundled browser configuration and disables ACP/ACPX only when neither has an existing policy/entry. Principal tools use native plugins and Host API. The ordinary non-static gate now exits nonzero for blocked required T1 checks, including an explicitly requested send check with a missing tab; static-only exclusions and unset dispatch opt-ins remain explicit. This corrects the residual ordinary-gate gap identified in the live CLWX-106 acceptance, without treating the stricter release profile as a substitute.

The moe.20 local candidate completed the interactive VM upgrade with matching installed hashes. First ready was observed after 233.6 seconds with no deferred restart; Office helper round-trips passed. The first chat exposed a concrete lifecycle race: a cloud answer completed while degradation awaited session cutover, then a stale payload replayed on the unavailable local model. The driver incorrectly returned success before that replay. Repair the shared terminal-success/replay ownership and the acceptance oracle before rebuilding; preserve legitimate recovery and side-effect identity. Follow [the runtime recovery report](evidence/WINDOWS_RUNTIME_RECOVERY_2026-09-07.md) for candidate identity and installed evidence.

GitHub synchronization and publication after successful validation are authorized. The pilot repository is public, so hosted artifacts must be keyless; the existing `requireCloudGatewaySeed=false` flag did not prevent injection of available secrets. Build-profile validation and the 24-hour publication freshness check passed independent review before any hosted upload. This does not resolve the held credential-rotation or post-install provisioning decisions.

The pre-push inventory found 277 commits not reachable from the local pilot remote refs, including unpublished desktop recordings and tenant evidence. Preserve that history locally. The authorized public delivery uses a separate reviewed source snapshot based on `pilot/main`, excluding private evidence/media and retaining deliberately redacted summaries. A checked-in synthetic PDF replaces the bundle gate's dependency on private historical evidence; missing fixture now fails. Source version `0.4.3-moe.21` is allocated for the next build; moe.20 remains the latest installed candidate until new artifact hashes exist.

The source repair now makes transient fallback session-scoped. User toggles own global channel configuration; a failed turn may pin only its current session to the acknowledged fallback model. This removes the global mutation/rollback race with later user intent. Preserve same-session retry and explicit channel-choice semantics, then repeat installed Online acceptance with the stricter terminal-state driver.


The startup CPU profile then identified 143 seconds inside the bundled pricing refresh, which normalized the remote OpenRouter catalog through runtime provider plugins. The pinned bundle patch limits normalization to configured pricing candidates while preserving provider aliases, wrapper references, exact-match priority and LiteLLM fallback. Its independent review and actual bundle verifier pass. A pricing-only Windows diagnostic reduced warm launch-to-ready from 191.1 to 84.2 seconds; this is measured on a temporarily patched moe.20 installation and does not certify moe.21. The next installer also removes the unsupported Windows shell-completion child and carries the session lifecycle repair.


## Source freeze validation — September 7

The completed lifecycle repair separates the user’s global channel choice, an acknowledged session model pin and pending recovery operations. A cloud final cancels stale fallback replay. Clearing an obsolete pin requires the bundled Gateway’s actual acknowledgement and preserves newer pin/notice ownership. Pending recovery remains visible when a new turn replaces its notice. Independent pricing and lifecycle reviews are clear.

Final source checks passed: **201 test files, 1,989 tests passed and 6 skipped**; typecheck passed; lint has **0 errors and 52 existing warnings**. Communication replay and comparison passed with no baseline regression. Harness CI passed its 16 harness, 5 direct Office and 61 eval checks; 9 eval skips remain explicit. Task validation and selected-flow dry run passed against the task-start commit. The frontend build and focused Electron test for held clear followed by a new send passed. The actual local OpenClaw bundle verifier passed, including the synthetic PDF and pricing patch guards.

These results freeze the source for the next Windows build. They do not establish GA readiness: no moe.21 installer exists at this checkpoint, and installed chat, representative Windows, tenant and full release acceptance remain outstanding.


## Outlook compose boundary verification — September 7

CLWX-123 now has a measured root cause and a bounded source repair. The live Outlook editor preserved the requested multiline body, but `textContent` concatenated adjacent HTML blocks. Body verification and draft readback now use rendered text, with the original native keyboard entry preserved. Recipient proof is restricted to the exact active-compose To/Cc/Bcc bucket and excludes suggestion/chrome decoys. Repeated exact metadata for one bucket is accepted; conflicting bucket labels refuse.

The focused 99-test Outlook safety suite, typecheck and targeted lint pass. A signed-in local product-path diagnostic created one owned synthetic draft with the original multiline input; recipient, subject and normalized body readback matched and Send was not clicked. Raw mailbox evidence remains private. Saved-draft reopen activation was not established by that diagnostic and is not claimed as passed. The three README files were reviewed; this restores the documented draft behavior without introducing a new user flow or interface.

The next native Windows build includes this source correction and the bounded integration-test budgets recorded in the native preflight evidence. No moe.21 installer or installed Windows Outlook proof exists at this checkpoint. CLWX-123 remains In Progress, and the release verdict remains RED.

Final reviewed public source preflight passed: **201 files, 1,994 tests passed and 6 skipped**; typecheck, lint (0 errors, 51 warnings), PowerShell checks (0 gating findings), agent configuration checks and 5 direct Office checks passed. The scoped harness diff validation/dry run and communication comparison also passed.

**Post-release feature:** owner-requested CLWX-124 tracks durable action history, interruption reconciliation and cleanup of proven agent-owned drafts. It is in Backlog, linked to CLWX-70/28/94, and is excluded from this release scope. Intentionally prepared drafts awaiting review must remain distinguishable from abandoned residuals.


## Hosted keyless package acceptance — September 7

The public build profile deliberately excludes cloud and Azure Speech credential seeds. The installed-evidence adapter previously required the cloud seed files unconditionally, so it could reject a correctly built public package. The release controller now carries the downloaded build profile through source/receipt validation, installed evidence and portable publication replay. Set `GA_GATE_BUILD_PROFILE` to `build-provenance/.tmp/release-build-profile.json` with its source and output-receipt companions intact.

For a validated source-bound keyless profile, the installed producer must explicitly prove that all four cloud/Azure config and key paths are absent under the actual running app's installation. Missing or malformed observations do not prove absence. A run without a profile retains the legacy seeded requirements. Installer and app hashes, package inventory, environment, live Gateway, Electron safe chat and Office checks remain mandatory; keyless packaging does not establish post-install provisioning or GA readiness.

Final controller review is clear. The five focused controller/gate files pass **135 tests**, including rejection before live commands when build provenance is invalid. Full public packaging preflight passes **201 files, 2,012 tests and 6 skips**, typecheck, lint (0 errors, 51 warnings), PowerShell (0 gating findings), agent checks and 5 direct Office checks. Full reviewed-source verification reports 0 mismatches across 1,280 included paths; 361 private/generated paths remain excluded. Native Windows packaging and installed acceptance remain pending.
