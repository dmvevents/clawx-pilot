# Completion plan

Updated 2026-09-07. **GA remains RED.** Native run [34134725489](https://github.com/dmvevents/clawx-pilot/actions/runs/34134725489) succeeded packaging from reviewed Windows source `34e951dc9d986ce82ef44396d440e9a02bc35242` (`0.4.3-moe.21`) in 13m14s. Preflight passed 203 files / 2,053 tests with 11 skips; the keyless-public staged package seed scan passed. Artifact verification PASS binds the x64 installer, build provenance, source/profile/compiled receipts, release manifest, extracted ASAR and packaged EXE to run9. Installer SHA256 is `d0da3062156e2faaac1df5967f03710736693f8fc7571adfccf254865579b00f`; ASAR SHA256 is `8905aef127a3f1e443069b484a54481e76caa8a152902b85da3d778079f2cc94`; packaged EXE SHA256 is `b60a4000d657085d4940f0f97ccc05132b04061b88891e2d4b6c34842a0c3bbe`. The blockmap was downloaded and structurally validated at 347,786 bytes / SHA256 `44ac9fa6deebf3b600cc86252a2d45bd3fdd50210c0de9673d0dd5dc7db75111` with 17,319 chunks covering the installer; no differential update execution was run. **moe.21 cannot be accepted or sent to testers:** required `resources/bin/ffmpeg.exe` is absent from the fresh run9 package and the second clean application-directory install. The first assisted upgrade had a legacy `ffmpeg.exe` in the old application directory, which masked the package gap. The second install completed at 2026-09-07T16:04:02Z with exit 0, matching EXE/ASAR, existing profile retained, cloud/Azure credential seed rows absent and cold readiness after 86,838ms with 20,176ms quiet / 11 stable samples, but video/recording is blocked by missing ffmpeg. Fresh ordinary Online v2 remains a scoped PASS: sent 2026-09-07T15:33:19Z, answered in 32,467ms, terminal stable for 30s, no tool calls, no error and no degrade. The installed cancel-next observation also remains scoped PASS at 15:34:50-15:35:57, with exact runtime abort not proven by that run. On-device remains a current installed FAIL: the first run was blocked by a stopped existing Ollama service after VM restart, then a fresh uncontaminated rerun with local model `qwen2.5:3b-instruct` present produced `NO_RESPONSE` with `degradeNoticeTrue` and no terminal answer. Overlapping document/on-device attempts are excluded. Public candidate source `aeb54555bb7b242930463f5277b83cf69fb23ec6` on `release/moe22-package-complete` contains the FFmpeg packaging repair and current-turn graph-reader capture. Package review approved the repair after focused source validation; the graph-reader driver has 29 focused tests. An isolated native FFmpeg downloader/functional diagnostic passed on Windows from 2026-09-07T16:33:48Z to 16:34:04Z, producing only the four expected helper/provenance files with no leftover smoke WAV. This is not installed acceptance. No moe.22 hosted build, installer hash or installed evidence exists yet. The current moe.22 candidate now also includes document title discovery and native image-grounding repair: focused unit coverage, direct P1-P5 harness, TypeScript, lint, task-spec validation and the package-fast artifact matrix passed. This is source/package-staging evidence only; installed acceptance remains pending.

This is the canonical completion order for Codex and Claude Code. The [machine-readable state vector](completion-state.json) records each outcome's status, owner, dependencies, evidence and exit criterion. It is a planning snapshot, not a replacement release-verdict engine. Dated plans, the large [historical state vector](GA_SPRINT_STATE_VECTOR.md), old card descriptions and old GREEN reports are evidence only.

## What completion means

The Ministry principal must install the identified Windows build without manually assembling dependencies, complete ordinary email/document/form/policy/reminder journeys, and recover from cancellation or connectivity loss without silent or duplicate work. The declared on-device/offline behavior must work. Release acceptance must prove the exact artifact and advertised scope, with the reported tester failures rerun.

The user authorized autonomous execution, GitHub synchronization, GA publication after validation, and stakeholder completion notification afterward. Continue without routine approval handoffs. Existing action confirmation rules and specifically held Mac installation, trim branch, credential/security changes and VM shutdown remain governed by the [project contract](PROJECT_CONTRACT.md). This authorization is not evidence that a missing test or Ministry decision has passed.

## State vector

Statuses describe the scope in each row. PARTIAL is not release success; required FAIL, BLOCKED or NOT_RUN prevents a GA claim. Card counts are not a completion percentage.

| Outcome | State | Existing owner cards | Current evidence and gap |
|---|---|---|---|
| requirements | PASS | CLWX-22, CLWX-107, CLWX-110 | Current project-relevant stakeholder messages, board and contract reconciled; historical titles are not current acceptance. |
| build process | PASS | CLWX-25, CLWX-106 | Reviewed workflow, cache, summary, procedure, planning artifacts and aligned agent/skill pushed as ae391338. PR #18 is merged; 32 process checks pass. Native run9 build/artifact identity passed, but later package completeness failed because required `ffmpeg.exe` was not shipped. Public candidate source on `release/moe22-package-complete` repairs helper packaging, graph-reader evidence capture, document title discovery and native image-grounding; no hosted moe.22 build exists yet. |
| native installer | FAIL | CLWX-25, CLWX-77 | Run9 native build/artifact identity PASS against clean public source `34e951dc9d986ce82ef44396d440e9a02bc35242`: installer `d0da3062156e2faaac1df5967f03710736693f8fc7571adfccf254865579b00f`, ASAR `8905aef127a3f1e443069b484a54481e76caa8a152902b85da3d778079f2cc94`, EXE `b60a4000d657085d4940f0f97ccc05132b04061b88891e2d4b6c34842a0c3bbe`. Direct VM download matched the GitHub archive metadata and installer hash in about 32s. Blockmap structural validation passed. Package acceptance FAIL because required `resources/bin/ffmpeg.exe` is absent from the fresh artifact and second clean application-directory install. |
| installed candidate | FAIL | CLWX-25, CLWX-85, CLWX-106 | Second clean application-directory install completed at 2026-09-07T16:04:02Z with exit 0, matching EXE/ASAR, existing profile retained, cloud/Azure credential seed rows absent and cold readiness at 86,838ms with 20,176ms quiet / 11 stable samples. Package acceptance FAIL because required `ffmpeg.exe` is missing; video/recording is blocked. Office imports/write-readback, forms.list, Online v2, observable cancel-next, reminder history/job execution and D0 answer each have scoped evidence, but moe.21 cannot be accepted. |
| turn lifecycle | PARTIAL | CLWX-94, CLWX-95, CLWX-96 | Current source passes 117 focused tests, six Electron interactions and independent review: owned-progress watchdog, exact old-run abort, strict Main-owned saved-provider proof, acknowledged inactive-pin recovery and per-episode retry accounting. Installed moe.21 fresh ordinary Online v2 passed in 32,467ms with 30s terminal stability and no tools/errors/degrade; observable cancel-next passed at 15:34:50-15:35:57, but exact runtime abort was not proven by that installed run. Package acceptance is blocked by missing `ffmpeg.exe`, and full lifecycle/fault recovery remains pending on a repaired package. |
| on device | FAIL | CLWX-117, CLWX-47 | Current source denies sessions_yield and terminalizes stalled tool/partial-stream runs using owned progress. Installed moe.21 first on-device run is valid failed prerequisite evidence because the existing Ollama service was stopped after VM restart. Ollama 0.32.14 service/model was restored without new dependency installation; the overlapping second attempt is excluded. A fresh uncontaminated installed on-device rerun then failed with local model `qwen2.5:3b-instruct` present: `NO_RESPONSE`, `degradeNoticeTrue`, no terminal answer. |
| documents policy reminders | PARTIAL | CLWX-22, CLWX-42, CLWX-77, CLWX-115 | Native package parsers pass. Second clean install Office imports PASS_WITH_LIMITATIONS and Word/Excel write-readback PASS. D0, P1, P2 and P4 have scoped installed evidence; P2 Word output parsed back and root full XML review preserved the original agenda/deadlines; P4 Excel output parsed back with six expected average/grade rows and unchanged source hashes. Installed moe.21 P3 PDF title prompt failed by asking for pasted text with no tool call, and P5 failed because the image tool returned truncated JSON text instead of a native image block. Current moe.22 source repairs both boundaries: `document.find` can discover the title through package/harness paths, and `document.read_image` now returns metadata plus a native `image/png` content block without base64 duplication in text/details. The second natural reminder job fired, recorded history, cleaned up the exact owned job and was opened by exact job ID with the marker visible at 2026-09-07T16:48:05Z; this proves natural scheduler plus retained in-app history only, not manual run, desktop notification or ordinary chat creation. Complete installed chat journeys remain unproved. |
| microsoft journeys | BLOCKED | CLWX-61, CLWX-73, CLWX-123 | Recipient/body boundary source tests and one Mac compose check pass. Second clean moe.21 controlled `forms.list` passed on reused Server 2022/admin, but it is not Forms preview evidence. VM Outlook still requires sign-in; external tester attach regression remains open. The Microsoft account-holder question remains unanswered. |
| representative windows asr | BLOCKED | CLWX-25, CLWX-67 | Physical pilot SSH timed out at 12:52Z. Available Server 2022/admin/reused-profile VM lacks a microphone and does not prove a Windows 10/11 principal installation. moe.21 also lacks packaged `ffmpeg.exe`, so video/recording/ASR acceptance is blocked until a repaired package exists. |
| latency rehearsal | PARTIAL | CLWX-43, CLWX-107 | Second moe.21 install reached cold readiness on reused Server 2022/admin after 86,838ms with 20,176ms quiet / 11 stable samples, but package acceptance failed because `ffmpeg.exe` is missing. Root helper warm-launch observations are not product warm-launch evidence. |
| production identity delivery | BLOCKED | CLWX-7, CLWX-8, CLWX-28, CLWX-31, CLWX-110 | Outbox exists and has local/stub evidence; stale anchor text saying unbuilt is superseded. Production hostname, Entra identity, write/dedupe API, quotas and rollout decisions are not established by those tests. |
| external acceptance | BLOCKED | CLWX-73, CLWX-107, CLWX-110 | Latest tester complaint remains September 4; no unaided external retest exists. moe.21 is not tester-ready because the fresh package is missing required `ffmpeg.exe`. Deployment ambition is 450 laptops, not an assumed 20-device release. |
| ga publication | NOT_RUN | CLWX-22, CLWX-106 | Owner authorized release and completion notice after verification. Run9 build/artifact identity passed, but installed package acceptance failed because `ffmpeg.exe` is missing. moe.21 cannot be published or sent to testers. Public candidate source on `release/moe22-package-complete` exists with helper packaging, document.find and native-image repair validated locally, but moe.22 has no hosted build, installer hash, installed acceptance or release gate evidence yet. |
| fleet rollout | BLOCKED | CLWX-22, CLWX-29, CLWX-31, CLWX-110 | Pilot readiness and fleet production readiness are distinct under the existing contract. The 450-laptop ambition has no current production integration/capacity acceptance. |
| action journal | DEFERRED | CLWX-124 | Explicitly post-GA: durable action history, restart reconciliation and cleanup of proven unchanged agent-owned drafts. No drafts deleted. |

## Analysis: why work has not converged

The main gap is integration and acceptance, not a need to rewrite the application. Source fixes, proxy tests, installed binaries and stakeholder outcomes have repeatedly been treated as equivalent. Native builds then expose assumptions late, and the next iteration starts before the entire failed boundary is understood.

Eight native build attempts plus a checkout-only dispatch now distinguish the causes and final boundary: checkout/process portability, cold integration-test budgets, a PDF fixture outside permitted roots, overbroad plugin diagnostics, fixture assumptions, Windows short-path canonicalization, an abbreviated-ref dispatch error before run8, unmocked diagnostics routes crossing into real Chrome/OS probes, and then a successful package on run9 followed by a package-completeness blocker: the fresh artifact does not ship required `ffmpeg.exe`. The fourth run passed **201 files / 2,011 tests**, with 11 platform skips, native bundle verification and seven staged artifact checks. Its remaining transport row ran the OpenClaw all-plugin diagnostics path to inspect one plugin and exceeded 120 seconds. Local diagnosis enumerated 102 plugins through that path; the real scoped loader loaded the target alone. The corrected native check now confirms that diagnosis: the cold no-Host-API row passed in 42,413ms and the full-Host-API row in 6,094ms, using the existing 120-second limit. Exact host/guest script hashes agree. These runs used a private copy of the installed pinned OpenClaw bundle with current plugin source; they are focused diagnostics, not new-installer acceptance. This does not establish the product Gateway is broken or repaired. Preserve the real plugin-host assertion while removing the unrelated sweep.

The fifth run passed 2,048 tests with 11 platform skips but failed two new fixtures: a Windows 8.3 temporary path and a pnpm mock that assumed POSIX. The corrected tests now use native path canonicalization and explicit platform mocks. All 75 tests in the two affected files passed on Windows under a forced short TEMP alias, with exact host/guest file hashes. This native check caught and corrected an insufficient first fixture repair before another hosted run. Product code and strict artifact checks were unchanged.

The sixth run, [34131398263](https://github.com/dmvevents/clawx-pilot/actions/runs/34131398263) from source `afd7a94d246639563880a3f7fc3cc13167606401`, passed 203 files / 2,050 tests with 11 skips, plus compile, bundle verification and seven artifact rows. It still failed before installer creation: the real transport artifact row completed quickly, but the assertion compared an 8.3 short TEMP staged path to the native long path and falsely rejected it. The source now canonicalizes the stage before deriving roots while preserving strict source equality. Private native evidence records 77 focused Windows tests with short TEMP, five file-hash matches, and two pinned-OpenClaw `2026.4.23` rows passing in 83 seconds and 5 seconds under a full 8.3 stage path. Run [34133768880](https://github.com/dmvevents/clawx-pilot/actions/runs/34133768880) then failed at checkout from abbreviated ref `18bd7d7a`; it ran no build checks. Run [34133858590](https://github.com/dmvevents/clawx-pilot/actions/runs/34133858590) failed source preflight before installer creation: 202 files / 2,049 tests passed, 3 diagnostics-routes tests failed, 11 skipped, and all 72 artifact tests passed. The failures were test-boundary defects: unmocked `diagnoseChromeCdp` calls reached real OS/browser/network PowerShell probes and each hit the 5-second timeout. Test-only source `34e951dc` (original fix `b963a521`) mocks the external boundary, verifies payload propagation and existing 500 handling, and leaves production unchanged. Private native diagnostics record 4 Windows tests passing in 127ms under the unchanged 5-second default with 8 source hashes matching; 12 route/Chrome service tests also pass locally, with ESLint/diff/root review clear. Run [34134725489](https://github.com/dmvevents/clawx-pilot/actions/runs/34134725489) succeeded packaging that full clean source in 13m14s. Private artifact verification then passed for the x64 installer, build provenance, source/profile/compiled receipts, release manifest, extracted ASAR and packaged EXE. Direct VM artifact download verified the GitHub archive digest and matching installer hash in about 32 seconds. Blockmap structural validation passed: 347,786 bytes, SHA256 `44ac9fa6deebf3b600cc86252a2d45bd3fdd50210c0de9673d0dd5dc7db75111`, 17,319 chunks, covers the installer; no differential update execution was run. Installed package acceptance then failed because required `resources/bin/ffmpeg.exe` is absent from the fresh artifact and the second clean application-directory install. The first visible assisted upgrade had a legacy `ffmpeg.exe` in the old application directory, which masked the package gap. The second install completed at 2026-09-07T16:04:02Z with exit 0, matching EXE/ASAR, existing profile retained, cloud/Azure credential seed rows absent and cold readiness after 86,838ms with 20,176ms quiet / 11 stable samples, but video/recording is blocked. Fresh ordinary Online v2 and observable cancel-next passed before later overlapping attempts. On-device remains a current installed failure: after the stopped-Ollama prerequisite failure and excluded overlap, a fresh root-controlled rerun with the local model present produced no terminal response and a degrade notice. This is not GA acceptance: moe.21 cannot be accepted or sent to testers, and moe.22 repair has public source but no build/hash.

The build process changes expose phases, retain the dependency cache after a later failure, remove duplicate Windows uv preparation and produce an escaped, allowlisted diagnostic summary. They do not bypass verification or establish a measured speedup. See the [build procedure and native-run retrospective](build/windows-build-pipeline.md).

Scope is explicit: the current target is the **Windows single-school pilot release using the existing broker and approved test tenant**. The existing project contract separates this from fleet rollout; [Ministry asks](MINISTRY_ASKS_2026-09-02.md) and the [finish sprint](GA_FINISH_SPRINT_2026-09-03.md) retain production integration as a separate milestone. CLWX-22 also tracks the broader production identity, durable backend delivery, economics and Ministry decisions. Some descriptions are stale: the outbox exists and has local/stub tests even though the anchor still says unbuilt. Its production integration remains unproved. Passing the desktop/pilot gates cannot close all eight fleet-level key results or authorize a 450-laptop rollout. Production milestones remain BLOCKED and visible; they are not silently marked passed or imposed as new pilot-release prerequisites.

## Shortest execution path

1. **Close the demonstrated boundaries before another full build.** The scoped transport repair is committed; 70 focused regressions, actual diff-aware task validation, and both native inventories pass. The process agent/skill/procedure/workflow is pushed in [merged PR #18](https://github.com/dmvevents/clawx-pilot/pull/18). CLWX-117/96 is implemented and independently reviewed: 117 focused tests and six Electron interactions pass, including repeated fallback/recovery episodes, exact old-run abort and ignored heartbeat progress. Failed provider checks cannot consume the session-clear retry budget; three failed clear acknowledgements remain bounded within the same episode, with explicit channel reset available. Partial installed proof now exists. The demonstrated source boundaries are now closed for the next build: bundled helpers, graph-reader capture, `document.find` title discovery and native image grounding have focused local evidence. Root owns integration and all external mutations.
2. **Freeze and build once from the reviewed source.** Run focused regressions first, then type/lint, required communication/harness checks and full integration checks once. Prove the changed transport row natively on Windows before dispatching another full package. Sync only reviewed source, preserving private history locally. Freeze an exact clean public SHA and build keyless-public. No cached compiled output or generated credentials. Stop at a failed phase, preserve the first causal error and reproduce it; an unchanged rerun is not the default recovery.
3. **Accept the exact installer on Windows.** Validate run/source/profile/compiled receipts and manifest; extract and hash installer, ASAR and EXE. Transfer with hash readback. Use the backed-up VM's interactive desktop for the assisted installer. Record first launch before diagnostic warm-up, then warm launch, Gateway/composer readiness and tool inventory. Record OS, privilege, prior profile and microphone capability. Existing Server/admin evidence cannot be relabeled fresh client/standard-user evidence.
4. **Run one installed acceptance matrix through ordinary prompts.** Use the matrix below on the same artifact. Source/helper tests support it; they do not replace actual model selection, visible output and terminal-state observations. Keep test-only steering out of ordinary prompts. Preserve user drafts and require actual authorization for sends/submissions. Fault tests must restore their controlled configuration and prove the next ordinary turn works.
5. **Complete the independent external dependencies in parallel.** Prepare the exact tester package/checklist and Ministry decision packet while the Windows lane runs. Account-holder sign-in, a reachable client/microphone lane and the unaided external rerun remain pilot dependencies. Real Ministry identity/backend behavior belongs to the separate production/fleet milestone. Do not revisit an unchanged failed access probe repeatedly. Measure latency on the candidate before settling the conflicting historical budgets; do not silently choose 30 or 45 seconds.
6. **Rehearse, gate, publish and verify the handoff.** Perform a continuous timed rehearsal of all three demo moments on the demo machine/account, with named fallback recordings. Run the canonical strict gate with the real build profile, manifest and installed evidence inventory. Required missing/skipped lanes prevent success. Verify the principal-facing pilot criteria as well; explicitly exclude Ministry production connectivity and fleet capacity from the scoped release notes. Publish only the accepted artifact set, re-download and verify hashes, update current release pointers and board evidence, then send and verify the authorized stakeholder completion notice.

## Installed completion and testing matrix

| Journey | Test and observable exit | Evidence limit / dependency |
|---|---|---|
| Assisted installation | Visible installer completes, exit 0, correct shortcut and complete packaged helpers; installer/EXE/ASAR hashes agree; zero manual dependency steps | Fresh client/standard-user lane required in addition to reused Server upgrade |
| Online lifecycle | Fresh ordinary prompt produces a terminal answer; wait through the late-event observation window; cancellation terminates; a second send succeeds | Read-only prompts first; a channel chip or tool-start event is not an answer |
| Connectivity recovery | Controlled mid-turn provider failure ends once in an answer or visible failure, preserves user intent, and next Online turn succeeds after restoration without restarting | No replay of uncertain writes; restore the exact network/config state |
| On-device/offline | Ordinary no-tool prompt answers without top-level `sessions_yield`; local document grounding works offline with no non-loopback egress; stalled loop terminates visibly | Fresh installed moe.21 rerun failed with no response and a degrade notice after the model was confirmed present; do not use old probe instructions to prompt around the defect |
| Five document prompts + discovery | Word read/write, PDF, Excel, image and folder discovery choose shipped document tools and yield correct content; generated output parses back | No developer Python install or helper-only substitution; include user Desktop/OneDrive shape |
| Outlook attach/read/draft | Authenticated user Chrome attaches; read is correct; To/Cc/Bcc/body/subject survive review and reopen; reply/forward target the intended item; changed/ambiguous compose refuses | Needs authenticated tenant session; preserve drafts; separately authorized dispatch proof required by the strict release gate |
| Forms | Both supported forms show a complete, correctly mapped preview and remain unsubmitted; missing/invalid source data has an accurate refusal | Listing form names or using an unapproved clone is not production preview evidence |
| Policy/reminder | Relevant school policy answer cites the correct version/source; a visible reminder occurs at the recorded time | Verify through installed app and supported delivery path |
| ASR | Actual microphone input yields usable transcription and failure/recovery behavior; packaged helpers present | No microphone on current Server lane; helper presence alone is insufficient |
| Performance/rehearsal | Record cold/warm ready time separately; repeat the three prompts, report first-output/terminal p50/p90 and sample size; continuous rehearsal completes | Numeric product latency budget needs an explicit recorded disposition; functional 300/360s probe limits are not a UX target |
| Production/fleet milestone (separate) | Real Entra identity, configured quotas, backend replay dedupe and Ministry hostname/redirect/caching decisions; staged rollout/support plan | Does not block scoped pilot publication under the existing contract; desktop/stub tests do not prove it complete |
| Release integrity | Strict acceptance within its freshness window binds the same source/artifact/profile/installed inventory; downloaded published bytes match | Missing evidence cannot be approved by editing this planning JSON or board prose |

Use existing commands and domain workflows in [AGENT_SKILL_INTEROPERABILITY.md](AGENT_SKILL_INTEROPERABILITY.md). The local controller can run canonical acceptance where available; the CI acceptance workflow also needs its configured runner. Do not equate source implementation of that workflow with a successful CI evidence artifact.

## First-run provisioning boundary

The public build is keyless. The current application supports an already configured account or the documented post-install `cloud-gateway.json` plus key drop-in; it does not implement automatic activation-code exchange or Entra-native Online onboarding. A fresh profile therefore cannot be represented by the reused VM's working credentials. Record the provisioning prerequisite and verify both the unprovisioned state and supported provisioned first turn. On-device first turn also requires its local runtime/model to be present. Reuse the [provisioning plan](RELEASE_PROVISIONING_PLAN.md); do not add a new authentication platform to this release or embed keys in the public installer.

## Stakeholder and history coverage

The September 7 refresh at **13:03:23Z** succeeded through the configured WhatsApp bridge's SQLite **read-only** storage; no callable WhatsApp MCP is exposed in this Codex session. The latest relevant principal stakeholder thread activity remains September 3 and external tester report September 4. Store-wide message freshness reached September 7; that does not prove every contact is fully synchronized. No messages were sent or media downloaded during intake.

Confirmed requirements remain principal forms/email/responses, working file interactions, reliable email attach on the tester's machine, acceptable response time, a demo/hands-on session and a stated **450-laptop** ambition. The September 3 request for Monday refers to September 7, not the Tuesday implied by one historical filename. No newer appointment was found. The separate curriculum-video work is excluded by message content, not by sender identity.

The live Plane API was refreshed under the verified CLWX project: **124 cards**, 62 Ready, 18 In Progress, 8 Todo, 24 Backlog and 12 Cancelled. Ready is an agent evidence state; Done remains human closure. Card descriptions must be read with subsequent comments and current source. Use [PLANE_BOARD_API.md](PLANE_BOARD_API.md) for identity checks, pagination and double readback; preserve full acceptance descriptions.

Earlier history analysis indexed 11 newest root Claude project JSONL files and parsed the main September 6–7 session's 21,515 records at scan time. The 1,158 Bash calls and 396 edits show activity, not a direct measure of waste. Relevant instructions, tool outcomes and handoffs were reviewed; this was not an exhaustive read of every historical/subagent transcript. The main evidence of non-completion is still missing installed/current-user acceptance. Raw transcripts and stakeholder exports stay private.

## Integration verification — 2026-09-07

The earlier guidance/gate integration passed scoped tests and independent review. Later source changes added candidate/compiled receipts, keyless profile validation, portable installed evidence, lifecycle acknowledgement/ownership corrections and the rendered Outlook compose boundary. The final CLWX-117/96 delta passed 117 focused tests, six Electron interactions, typecheck/lint, communication regression, actual public-diff task validation and independent review. Earlier combined preflight passed 203 files / 2,047 tests with six platform skips; final changed behavior then passed targeted validation. Their detailed evidence is retained in the [runtime recovery report](evidence/WINDOWS_RUNTIME_RECOVERY_2026-09-07.md), [Windows testing report](evidence/WINDOWS_VM_TESTING_2026-09-07.md) and [release evidence index](GA_RELEASE_EVIDENCE_MANIFEST.md). The fourth-run transport failure and sixth-run short-path false rejection now have native-verified source repairs. The fifth-run fixture failures have 75 passing Windows tests; run7 was a checkout-only dispatch error, run8 exposed the diagnostics-route test-boundary defect, and run9 produced verified x64 installer/provenance artifacts. Installed package acceptance now fails for moe.21 because `ffmpeg.exe` is missing. The valid Server 2022/admin Online and cancel-next observations remain scoped evidence only.

The existing moe.20 installation reproduced the late-recovery defect. A pricing-only diagnostic reduced one comparable warm readiness observation from 191.090 to 84.171 seconds; it is not moe.21 acceptance or a population latency estimate. All temporary VM code patches were restored and the app stopped. Current-source fixes have initial installed hash/readiness, controlled forms-list, fresh ordinary Online proof, observable cancel-next proof, scoped document/reminder evidence and a rejected image/PDF document lane that prevents journey acceptance. moe.21 package acceptance fails on missing `ffmpeg.exe` and still needs lifecycle/fault, on-device/offline, warm-latency, journey and external-client acceptance after a repaired build.

## Scope control and stop condition

CLWX-124 is explicitly **post-GA**: durable action history, interruption reconciliation and cleanup of proven unchanged agent-owned drafts. Existing prepared drafts are not automatically abandoned; no drafts have been deleted in this continuation. Do not expand this release with that feature, migrate the chat architecture, reorganize runtime directories or add dependencies merely to make the plan look complete.

Reuse unchanged evidence with its source and scope. Run broader checks only after relevant changes, failures or at integration boundaries. Update this plan/state vector and existing card comments once per meaningful delta, not after every command. The final release claim requires both automated acceptance and the declared stakeholder scope. When all independently actionable work is complete and an external dependency has no available recovery, record its exact owner and missing evidence instead of generating unrelated micro-fixes.
